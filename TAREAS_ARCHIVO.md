# Historial de tareas finalizadas

### 67. Sincronización en tiempo real (Supabase Realtime)
- Finalizada: 2026-07-17. Cambio de esquema: sí (una publicación de Realtime, idempotente); pendiente de que el usuario lo aplique en Supabase.
- Contexto: punto 2 del análisis UX/arquitectura del 2026-07-17. La queja original ("los cambios de un dispositivo no aparecen de inmediato en otro") se diagnosticó como puramente de propagación: `updated_at` ya lo estampa el servidor por trigger `registrar_modificacion` (`before insert or update`), sin problema de reloj; el motor de sync (`src/lib/sync.ts`) solo descargaba por sondeo (evento `online`, `visibilitychange` y un `setInterval` de 2 minutos en primer plano), sin Supabase Realtime, así que un cambio tardaba hasta 2 minutos o hasta que el otro dispositivo volviera a primer plano.
- Enfoque (el de menor riesgo de las alternativas del análisis; se descartó aplicar el payload del evento directamente por el riesgo de fuga de credenciales vía RLS): Realtime se usa SOLO como señal. Cuando un compañero cambia algo, el evento dispara una descarga (`programarSync`, agrupada), no aplica datos. Así la descarga sigue el mismo camino de siempre, que respeta RLS por consulta: nunca llega a un dispositivo un dato que no debería ver (una credencial sin permiso de bóveda), aunque el evento lo mencione. El sondeo de 2 minutos queda como red de seguridad.
- Solución (cliente): `src/lib/sync.ts`. Nuevo campo `tiempoReal: boolean` en `EstadoSync`. Funciones `suscribirRealtime`/`desuscribirRealtime`: un solo canal `cambios-equipo` con `on('postgres_changes', { event: '*', schema: 'public' }, ...)` (sin `table`: escucha todas las tablas de la publicación con un único canal, para no multiplicar conexiones, relevante en el plan gratuito de Supabase) que en cada evento llama `programarSync()`; el callback de `subscribe` actualiza `tiempoReal` y, al (re)conectar (`SUBSCRIBED`), dispara una `sincronizar()` para recuperar lo perdido mientras el canal estuvo caído. El canal vive atado a la sesión: en `iniciarSync` se registra `supabase.auth.onAuthStateChange` que suscribe cuando hay sesión (su evento inicial cubre abrir la app ya autenticado) y corta al cerrar sesión; supabase-js renueva solo el token del canal al refrescar. `desuscribirRealtime` usa `removeChannel`. `src/components/IndicadorSync.tsx`: el tooltip del punto verde muestra "Sincronizado en tiempo real" cuando `tiempoReal` es true, sin cambiar colores ni comportamiento (fallback honesto a "Sincronizado" cuando el canal no está conectado).
- Solución (servidor): `supabase/schema.sql`, nueva sección 6. Bloque `do $$ ... $$` idempotente que crea la publicación `supabase_realtime` si falta y agrega solo las tablas que aún no estén (categorias, articulos, dispositivos, credenciales, adjuntos, historial, conexiones, diagnosticos, ejecuciones_diagnostico, accesos_boveda, perfiles, boveda_meta). No necesita `REPLICA IDENTITY FULL`: las eliminaciones de contenido son borrados suaves (update de `eliminado_en`), que se propagan como UPDATE, y las políticas RLS de las tablas protegidas usan la función `puede_ver_boveda()` (no columnas de la fila borrada), así que se evalúan bien sobre la fila nueva.
- Pruebas: `npx tsc -b`, `npm run lint` (oxlint), `npm run test -- --run` (402, sin nuevas: la lógica de suscripción es puro efecto de borde sin parte pura extraíble más allá de la ya probada) y `npm run build` (`tsc -b && vite build`, service worker incluido) en verde.
- Verificación local: en navegador real (dev server, sesión simulada, datos sembrados en IndexedDB, offline). Confirmado que la app carga y funciona con el código de Realtime activo al arranque, sin crash y sin errores de consola (en particular no aparece el mensaje "No se pudo iniciar la sincronización" del try/catch de `main.tsx`, ni excepción del callback de auth). El indicador muestra "Sincronizado" (no "en tiempo real") porque con el token falso el canal no llega a `SUBSCRIBED`: es el fallback correcto y honesto. No se pudo probar la propagación real entre dos dispositivos ni el estado `SUBSCRIBED` en este entorno, porque exigen un backend real con la publicación aplicada y dos sesiones válidas.
- Pendiente (usuario): (1) aplicar el `schema.sql` actualizado en el proyecto real de Supabase (crea la publicación `supabase_realtime`; sin ella el canal no recibe eventos y la app cae al sondeo de 2 minutos, comportamiento correcto que no rompe nada); (2) verificar la propagación real: editar una solución en la PC y ver que aparece en el celular en uno o dos segundos, y que el indicador dice "Sincronizado en tiempo real". Ojo con el límite de conexiones concurrentes de Realtime del plan gratuito (200): un solo canal por dispositivo y 5 técnicos quedan holgados.
- Documentación: ARQUITECTURA.md sección 7 (Funcionamiento offline y sincronización) actualizada con el modelo de propagación en tiempo real y la decisión "señal, no fuente de datos".

### 66. Entrada rápida de tareas dentro de los pasos
- Finalizada: 2026-07-17. Sin cambios de esquema en Supabase ni en el modelo de datos.
- Contexto: punto 1 del análisis UX/arquitectura del 2026-07-17 (alternativa B, la recomendada frente a un rediseño total del modelo Paso/Tarea, descartado por alto riesgo). Al revisar el código real se encontró que `PasosEditor.tsx` ya soportaba Enter para crear la siguiente tarea y pegar varias líneas para repartirlas en tareas; lo que faltaba era: arrancar un paso nuevo listo para escribir sin clics previos, poder intercalar una advertencia en medio de una secuencia de tareas sin bajar hasta la barra "Agregar" y volver a subir, y aligerar el peso visual de los tres selectores minoritarios (credencial, subprocedimiento, solución) que hoy siempre están a la vista aunque casi nunca se usan.
- Descripción: tres cambios de interacción, todos dentro del mismo modelo de datos existente.
  1. **Auto-foco al crear un paso**: "+ Agregar paso" ahora crea el paso con una tarea vacía ya puesta y el cursor listo en su input, sin pasar primero por la barra "Agregar".
  2. **Inserción en línea**: cada bloque (tarea, advertencia o imagen) suma un botón "+" junto a los controles de mover/eliminar que abre un mini menú "+ ☑ Tarea aquí debajo" / "+ ⚠ Advertencia aquí debajo"; inserta el bloque nuevo justo después del actual y lo enfoca, sin salir del punto donde se estaba escribiendo. Antes solo Enter en una tarea lo lograba (y únicamente para agregar otra tarea); ahora también funciona después de una advertencia o una imagen, y permite intercalar una advertencia sin dejar el teclado.
  3. **"Más opciones del paso"**: los selectores de credencial, subprocedimiento y solución vinculados quedan colapsados detrás de un botón "+ Más opciones del paso", salvo que el paso ya tenga alguno puesto (entonces arrancan visibles, para no esconder datos ya cargados). Reduce el "peso" visual de un paso simple a solo título, objetivo y tareas.
- Solución: `src/features/soluciones/PasosEditor.tsx`. Estado `pasoRecienCreadoId` en `PasosEditor` para rastrear el paso a auto-enfocar; el botón "+ Agregar paso" crea el paso con `bloques: [crearBloqueTarea()]` y guarda su id. `ContenidoEditor` suma las props `focoInicialId`/`onFocoInicialConsumido`: inicializa `focoId` con el bloque a enfocar y una función `consumirFoco` centraliza el consumo (usada tanto por el foco inicial como por el normal de Enter/"+ Tarea"). Se generalizó `insertarTareaDespues` en una función común `insertarDespues(indice, bloque)`, reutilizada también por la nueva `insertarAvisoDespues`. `FilaBloque` suma las props `onInsertarTareaAqui`/`onInsertarAvisoAqui` y un estado local `menuInsertarAbierto` para el mini menú del botón "+". Nuevo componente `MasOpcionesPaso` (envuelve los tres selectores existentes sin tocar su lógica interna) con estado `abierto` inicializado en `true` solo si el paso ya trae `credencialId`, `subArticuloId` o `solucionArticuloId`.
  - Decisión de alcance: la inserción posicional del botón "+" solo ofrece tarea y advertencia, no imagen (subir una imagen siempre necesita abrir el selector de archivos del sistema, así que la interrupción del flujo es inevitable en ese caso); las imágenes se siguen agregando desde la barra "Agregar" al final del cuerpo del paso, reordenables con las flechas como ya funcionaba.
- Pruebas: `npx tsc -b`, `npm run lint` (oxlint), `npm run test -- --run` (402 pruebas, sin pruebas nuevas: es un cambio de interacción sin lógica pura extraíble más allá de la ya probada `insertarDespues`/`crearBloque*`) y `npm run build` (`tsc -b && vite build`, incluye el service worker) en verde.
- Verificación: en navegador real (dev server, sesión simulada, categoría y perfil sembrados en IndexedDB, offline). En un artículo nuevo tipo Instalación: (1) clic en "+ Agregar paso" dejó el foco directamente en el input de la primera tarea vacía (confirmado por `document.activeElement`); (2) se escribió "Instalar drivers", Enter creó la siguiente tarea, se escribió "Reiniciar equipo"; (3) el botón "+" de la fila "Instalar drivers" abrió el mini menú, "+ ⚠ Advertencia aquí debajo" insertó una advertencia justo entre las dos tareas (orden verificado en el DOM: Instalar drivers → advertencia → Reiniciar equipo) y dejó el foco en su textarea; (4) el botón "+ Más opciones del paso" apareció colapsado por defecto (el paso nuevo no tiene credencial/subprocedimiento/solución) y desapareció al hacer clic, confirmando el cambio de estado a expandido. Sesión y datos de prueba eliminados al terminar.
- Documentación: sin cambios en ARQUITECTURA.md (el modelo JSON `procedimiento`/`bloques` no cambió, solo la interacción del editor).

### 65. Nueva categoría "Software"
- Finalizada: 2026-07-17. Sin cambios de esquema estructural (la tabla `categorias` no cambió); solo una fila nueva sembrada por SQL.
- Contexto: punto 4 del análisis UX/arquitectura del 2026-07-17. El usuario pidió agrupar procedimientos de software, plataformas y aplicaciones corporativas (Intranet, GLPI, SIPRO, Office 365, Active Directory, Antivirus, herramientas web) que hoy no tenían categoría propia, y eligió entre "Plataformas" y "Sistemas". Se recomendó "Software" como nombre más amplio y escalable (las categorías son planas, sin jerarquía ni subcategorías, así que conviene un nombre paraguas que no quede corto).
- Solución: `supabase/schema.sql`, bloque `insert into public.categorias` (~línea 560): se agregó `('Software', 12, false)` al `values` existente (protegido por el mismo `on conflict (nombre) do nothing`, así que aplicar el schema.sql de nuevo en Supabase no duplica ni pisa categorías ya creadas). No hizo falta tocar `src/features/soluciones/iconosSoluciones.ts`: `iconoDeCategoria` ya tenía el mapeo por palabra clave `n.includes('software')` → ícono `Code` desde antes (línea 81), así que la categoría nueva ya sale con ícono propio sin código adicional.
- Pruebas: `npx tsc -b` y `npm run lint` (oxlint) en verde (el cambio es solo SQL, no toca TypeScript).
- Verificación: en navegador real (dev server, sesión simulada, categorías "Software" e "Impresoras" sembradas directamente en IndexedDB para simular el resultado del insert, offline). En `/soluciones` el chip "Software" aparece junto a "Impresoras"; se confirmó por el `outerHTML` del SVG dentro del botón que el ícono renderizado es `Code` (el trazado de los símbolos `< >` de código), no el ícono neutro de respaldo `BookOpen`. Sesión y datos de prueba eliminados al terminar.
- Pendiente (usuario): aplicar el `schema.sql` actualizado en el proyecto real de Supabase (o insertar la fila a mano desde el panel) para que la categoría exista en producción; el equipo no puede crear categorías desde la app todavía (no hay UI para eso, queda como posible tarea futura si se pide).
- Documentación: sin cambios en ARQUITECTURA.md (agregar una fila a una tabla de datos iniciales no es un cambio de modelo).

### 64. Etiquetas más ágiles (pegar o escribir varias separadas por coma o salto de línea)
- Finalizada: 2026-07-17. Sin cambios de esquema en Supabase ni en el modelo de datos (las etiquetas ya se guardaban como `text[]`).
- Contexto: punto 3 del análisis UX/arquitectura pedido por el usuario el 2026-07-17 (rediseño del sistema de tareas en pasos, sincronización, etiquetas y nueva categoría). Al revisar el código real se encontró que `EtiquetasEditor` ya soportaba Enter, coma y Shift+Enter para confirmar una etiqueta, y ya ofrecía autocompletar (`<datalist>`) con las etiquetas ya usadas; el único hueco real era que pegar o escribir una lista completa separada por comas ("impresora, zebra, etiquetas, red") creaba una sola etiqueta con todo el texto adentro en vez de repartirla.
- Descripción: al pegar o confirmar (Enter/coma/blur) un texto con varias etiquetas separadas por coma o salto de línea, ahora se crean todas de una sola vez, descartando vacías y ya puestas (sin distinguir mayúsculas). Además, Backspace con el campo de texto vacío borra la última etiqueta agregada (mismo atajo que GitHub, Notion y Gmail).
- Solución: `src/features/soluciones/ArticuloForm.tsx`, función `EtiquetasEditor` (~línea 692). Nueva función `agregarTexto(bruto)` que divide por `/[,\n]/`, limpia y deduplica contra las ya puestas; `agregar()` ahora delega en ella. `onKeyDown` del input suma el caso `Backspace` con `texto === ''` y `etiquetas.length > 0` (borra la última con `slice(0, -1)`). Nuevo `onPaste`: si el texto pegado contiene coma o salto de línea, se previene el pegado por defecto y se reparte con `agregarTexto`; un pegado de una sola etiqueta sigue el flujo normal (queda en el input para editar antes de confirmar). Placeholder actualizado para reflejar que acepta varias separadas por coma.
- Pruebas: `npx tsc -b` y `npm run lint` (oxlint) en verde; `npm run test -- --run` con las 402 pruebas existentes en verde (sin pruebas nuevas: es un cambio de interacción en un componente sin lógica pura extraíble, verificado en navegador real en su lugar).
- Verificación: en navegador real (dev server, sesión simulada, categoría y perfil sembrados en IndexedDB, offline). En el formulario "Nuevo manual": (1) escribir "impresora, zebra, etiquetas, red" de una vez y confirmar con Enter creó los 4 chips correctamente divididos (no un solo chip con comas adentro); (2) Backspace con el campo vacío borró el último chip ("red"); (3) simular un pegado real (evento `paste` con `ClipboardEvent`/`DataTransfer`) con "switch, cisco\nconfiguracion" agregó los 3 chips de una sola vez. Sesión y datos de prueba eliminados al terminar.
- Documentación: sin cambios en ARQUITECTURA.md (el modelo de etiquetas no cambió, solo la interacción de captura).

### 63. Rediseño IT Brain: pantalla Soluciones re-autorizada (handoff Nocturne)
- Finalizada: 2026-07-17. Sin cambios de esquema en Supabase.
- Contexto: nuevo handoff Nocturne "Herramienta IT para técnicos-handoff.zip" (mismo tipo que el de la tarea 58, ya en tema oscuro; su `_ds/` es `nocturne-3b8f2a23...`, no la línea de tema claro descartada). Su README apunta a `Soluciones.dc.html` como el diseño a implementar. Es una re-autoría refinada de la pantalla Soluciones que ya existía en Nocturne (tarea 59), así que se ACTUALIZÓ `SolucionesPage.tsx` en vez de crear una pantalla nueva. El zip se extrajo a una carpeta temporal solo para leerlo y se borró al terminar (no queda en el repo; el .zip sigue en disco, ignorado por git).
- Descripción: el diseño reemplaza la rejilla de categorías por una fila de chips (con conteo), añade subfiltros por tipo dentro de una categoría, colorea el icono de cada tipo según su tono, y al buscar agrupa los resultados por categoría con el término resaltado. La búsqueda ahora también coincide por etiqueta y por tipo, no solo por título y categoría.
- Solución (icono nuevo): `src/components/iconos.tsx` suma `XCircleFill` (Phosphor x-circle-fill, trazado 256), usado en el botón de limpiar del buscador (el diseño pide `ph-x-circle-fill`).
- Solución (tono por tipo): `src/features/soluciones/iconosSoluciones.ts` suma `claseTonoDeTipo(tipo)`, que traslada los tonos del handoff a los tokens del sistema: instalación/configuración/conexión toman el acento (`text-noct-accent bg-noct-accent/[.12]`), problema frecuente el ámbar (`noct-precaucion`), mantenimiento el verde (`noct-exito`) y manual queda neutro (`noct-neutral-400`), siempre con el fondo del recuadro al 12 %.
- Solución (pantalla): `src/features/soluciones/SolucionesPage.tsx` reescrita. Cabecera fija con desenfoque (`sticky top-0 z-20 bg-noct-bg/[.92] backdrop-blur-[12px]`, borde inferior divisor) que agrupa título "Soluciones", botón "Crear", buscador y chips. Chips de categoría en scroll horizontal (barra oculta) con "Todos" + una entrada por categoría, cada una con su icono y su conteo, estado activo en acento. Subfiltros de tipo (chips más pequeños) solo al navegar dentro de una categoría, en el orden fijo de `TIPOS_ARTICULO` y limitados a los tipos realmente presentes. Buscador con borde/icono en acento cuando hay texto y botón de limpiar (`XCircleFill`). Lista: al navegar, una sola lista plana ordenada por orden de categoría y luego título; al buscar, un grupo por categoría con encabezado (icono + rótulo + conteo). Cada fila lleva el icono de tipo coloreado (36px, `claseTonoDeTipo`), título (con el tramo coincidente resaltado en `bg-noct-accent/[.18] text-noct-accent-300` vía el helper puro `partirTitulo`, que compara en texto normalizado pero corta el original con acentos), metadatos (tipo · reloj + tiempo cuando el procedimiento lo trae) y las etiquetas Borrador/Obsoleto. Estado vacío "Sin coincidencias" con mensaje contextual y botón "Limpiar búsqueda"; se conserva además un estado de primera vez ("Aún no hay artículos") para la base recién creada, que el prototipo (con datos de muestra) no contemplaba.
- Decisiones tomadas (revisables): el botón "Crear" del prototipo es estático y no resuelve que crear un artículo necesita una categoría (ruta `soluciones/:categoriaId/nuevo`, no hay ruta genérica). Se conservó el comportamiento ya establecido en la tarea 59: habilitado y enlazado cuando hay un chip de categoría activo, deshabilitado con tooltip cuando está en "Todos" o buscando; el estilo pasa a secundario, como pide el diseño. La búsqueda no borra la categoría seleccionada (solo la oculta y la ignora mientras hay texto), igual que el modelo de estado del handoff: al limpiar el texto, el filtro de categoría vuelve.
- Pruebas: `npm run build` (`tsc -b && vite build`) y typecheck en verde; oxlint sin hallazgos en los archivos tocados. Sin pruebas unitarias nuevas (cambio de presentación; la única lógica pura nueva, `partirTitulo`, se verificó en el navegador con el resaltado real).
- Verificación: en navegador real (dev server, sesión simulada y datos sembrados en IndexedDB con las mismas 6 categorías y 16 artículos del prototipo; offline, sin tocar el backend). Navegar: lista plana de los 16 ordenada por categoría y título; chips con conteos; al elegir "Impresoras" la lista baja a 4, aparecen los subchips de tipo (Instalación/Configuración/Problemas frecuentes/Mantenimiento) y "Crear" se habilita hacia `/soluciones/c-impresoras/nuevo`; el subfiltro "Problemas frecuentes" deja 1. Buscar "red": "7 resultados" agrupados en Impresoras/Punto de venta/Redes, con el término resaltado solo en los títulos que lo contienen y coincidencias correctas por nombre de categoría ("Redes") y por etiqueta ("red" en un POS). Buscar "xyzq": estado "Sin coincidencias" con el mensaje contextual; "Limpiar búsqueda" restablece query, categoría y tipo. `getComputedStyle` confirmó los tonos (acento #9184d9, ámbar oklch(0.8 0.1 84), resaltado #d2cefd = accent-300) y la cabecera fija (sticky, top 0, z 20, fondo bg al 92 %, blur 12px, borde divisor). La captura de pantalla volvió a agotar el tiempo por el entorno (como en tareas 10, 58, 59 y 62); se verificó con `get_page_text`, `read_page` y `getComputedStyle`. Sin errores de consola; sesión y datos de prueba eliminados al terminar.
- Documentación: comentario de cabecera de `SolucionesPage.tsx` actualizado (describe la re-autoría). No toca ARQUITECTURA.md (no cambia el modelo de datos ni el esquema).

### 62. Rediseño IT Brain: sidebar de escritorio en el shell Nocturne
- Finalizada: 2026-07-17. Sin cambios de esquema en Supabase.
- Contexto: nuevo handoff de Claude Design con `Soluciones.dc.html` abierto. Ese zip ("Prompt Maestro para Claude Design-handoff.zip") es la línea de TEMA CLARO ya descartada (D-006 la reemplaza por Nocturne) y la pantalla Soluciones ya estaba implementada en Nocturne (tarea 59). Antes de tocar código se le planteó la ambigüedad al usuario (reimplementar en claro contradiría una decisión suya registrada y duplicaría trabajo); eligió "mantener Nocturne pero traer el layout responsive de escritorio con sidebar de 240px, que hoy no existe en el shell móvil".
- Descripción: la única pieza del diseño de tema claro que Nocturne aún no tenía era el `AppShell` de escritorio (sidebar fija de 240px a partir de >=1024px). El shell Nocturne era solo móvil (columna de 448px + pestañas inferiores). Se hizo responsive tomando la estructura del `AppShell.tsx` claro ya existente, pero con tokens `noct-*`.
- Solución: `src/app/ShellNocturne.tsx` pasa a ser un flex row en `lg`. Nueva `<aside>` `hidden ... lg:flex`, sticky a pantalla completa, 240px (`w-60`), fondo `noct-surface`, borde `noct-divider`: marca "IT Brain" (glifo de cerebro inline, reusado del `AppShell` claro, en `noct-accent`), navegación con los mismos 5 destinos del bottom nav (Bóveda solo con `puedeVerBoveda`) usando los iconos outline/fill ya existentes, estado activo con `NavLink` en `bg-noct-accent/[.12]` + texto acento (no solo color, regla de 08_ESTILO), y pie con el perfil real (nombre + correo, enlace a `/cuenta`; no hay campo "rol" en `Perfil`, así que se muestra el correo como en el `AppShell` claro). La columna de contenido se centra a `max-w-md` en móvil (igual que antes) y a `max-w-[816px]` en escritorio (720 de contenido + 48 de margen a cada lado, exacto al diseño). El bottom nav queda `lg:hidden`; el móvil no cambia en nada. La firma pública del shell (`{ children }`) no cambia, así que ninguna ruta se tocó.
- Solución (páginas consumidoras, 2): `SolucionesPage.tsx` y `ArticuloPage.tsx` (las únicas que usan `ShellNocturne`) llevan su propio `<main>`, así que se les añadieron overrides `lg:` de padding para escritorio: en Soluciones `lg:px-12 lg:pb-16 lg:pt-10` (48/40px del diseño) y la rejilla de categorías pasa de `grid-cols-2` a `grid-cols-2 lg:grid-cols-4` (equivalente al `auto-fill minmax(150px,1fr)` del diseño sobre 720px); en Ficha de Procedimiento, `lg:px-12 lg:pb-16` en el `<main>` y `lg:px-10 lg:pt-4` en su cabecera para alinear el botón "Volver". En móvil todos los valores originales (`px-4`, `pb-[116px]`, etc.) se conservan.
- Decisiones tomadas (revisables): (1) el rótulo del sidebar es "IT Brain" (lo que dicta el diseño); sigue pendiente la decisión del usuario de si vuelve a "Soluciones IT" (misma nota abierta que ya estaba en el tablero). (2) No se añadió la cabecera superior móvil que el diseño también muestra (logo + avatar): el usuario pidió explícitamente solo el sidebar de escritorio, y el móvil Nocturne ya trae su propia cabecera por pantalla; añadirla sería scope creep. (3) El pie del sidebar muestra correo (dato real) en vez del "rol" placeholder del diseño, porque `Perfil` no tiene campo de rol.
- Pruebas: build (`npm run build` = `tsc -b && vite build`) y typecheck en verde; oxlint sin hallazgos. Sin pruebas unitarias nuevas (cambio puramente de presentación en el shell).
- Verificación: en navegador real (dev server, sesión simulada y datos sembrados en IndexedDB: 6 categorías + 13 artículos; offline, sin tocar el backend). Escritorio 1280x900: sidebar visible a 240px, fondo `noct-surface` (#232532), borde `noct-divider`, position sticky, marca "IT Brain", los 5 destinos (Bóveda incluida por `puedeVerBoveda`), "Soluciones" activo en acento #9184d9, pie con "Ana Torres" + correo, bottom nav oculto (`display:none`), contenido centrado a 816px con padding 48/40px y rejilla de categorías en 4 columnas. Ficha de Procedimiento en escritorio: sin ErrorBoundary, sidebar presente, `<main>` con padding 48/64px y cabecera 40px. Móvil 375x812: sidebar `display:none`, bottom nav en grid de 5 columnas, contenido a 448px, padding y rejilla (2 columnas) originales, sin cambios. La captura de pantalla volvió a agotar el tiempo por el entorno (como en tareas 10, 58 y 59); se verificó con `get_page_text` + `getComputedStyle`. En el camino, un `ErrorBoundary` en Ficha de Procedimiento resultó ser un hueco del seed de prueba (el artículo sembrado sin campo `contenido`, que la página lee con `.trim()` en la línea 215), no del cambio: se re-sembró el artículo con `contenido` y la página renderizó bien. Sin errores de consola nuevos; sesión y datos de prueba eliminados al terminar.
- Documentación: comentario de cabecera de `ShellNocturne.tsx` actualizado (ahora describe el layout responsive). No toca ARQUITECTURA.md (el shell no cambia el modelo de datos ni el esquema).

### 61. Base de conocimiento inteligente, fase N4 (fichas 360° y línea de tiempo)
- Finalizada: 2026-07-16. Sin cambios de esquema en Supabase.
- Descripción: fase N4 de [PROPUESTA_BASE_CONOCIMIENTO.md](PROPUESTA_BASE_CONOCIMIENTO.md) (sección 12): ficha de categoría con el esqueleto estándar (artículos + dispositivos + diagnósticos de la categoría + historial) y componente "Línea de tiempo" que fusiona cronológicamente lo que hoy se ve por separado. La ficha de UBICACIÓN que también menciona la fila de la tabla de fases queda fuera de esta tarea a propósito: `ubicaciones` no existe como entidad todavía (es la fase N3, con esquema, bloqueada por las 4 decisiones abiertas del usuario).
- Solución (ficha de categoría): `CategoriaPage.tsx` suma línea de contadores en la cabecera ("N artículos · N equipos · N diagnósticos"), sección "Dispositivos de esta categoría" y "Diagnósticos de esta categoría" (ambas consultan por `categoriaId`, ya existente en esas tablas, sin tocar esquema) enlazando a sus fichas reales, y `<Historial entidadTipo="categoria" entidadId={categoriaId}>` al final (ya soportaba `'categoria'` en `TipoEntidadHistorial`, solo faltaba montarlo).
- Solución (línea de tiempo unificada): al investigar se descubrió que los "cambios de campos + intervenciones manuales" y, sorpresa, también los "cambios de cableado" de un dispositivo YA convivían en la tabla `historial` (una conexión creada o eliminada ya escribe una entrada con `campo:'conexion'` en el historial de AMBOS extremos, `repositorio.ts` función `entradasCambio`/`entradasEliminacion`, patrón de la tarea de conexiones); no había nada que fusionar ahí. Lo que sí faltaba, confirmado revisando `ArticuloPage.tsx` y `CredencialPage.tsx` (ninguna de las dos mostraba esto en ningún lado): las ejecuciones de diagnóstico que usaron un artículo (tabla `ejecuciones_diagnostico`) y los accesos de auditoría de una credencial (tabla `accesos_boveda`, hasta ahora solo se calculaba un "último acceso" suelto en la cabecera). Nuevo `src/features/historial/lineaDeTiempo.ts` (lógica pura, con pruebas): `combinarEventos(historial, ejecuciones, accesos)` fusiona y ordena por fecha descendente sin importar la fuente; `ETIQUETA_ACCION_BOVEDA` traduce las 6 acciones de auditoría a texto natural; `etiquetaResuelto` y `formatearDuracion` para las ejecuciones. `Historial.tsx` (el mismo componente que ya usan las 5 entidades, sin cambiar su firma en ningún call site) ahora consulta también `ejecuciones_diagnostico` (solo si `entidadTipo === 'articulo'`) y `accesos_boveda` (solo si `entidadTipo === 'credencial'`), combina con `combinarEventos` y despacha cada evento a `EntradaItem` (ya existía), o a los nuevos `EjecucionItem` (enlaza al diagnóstico, muestra resuelto/no resuelto/abandonado + duración + solución propuesta si la hay) y `AccesoItem` (usuario + acción + fecha). Así "Línea de tiempo" no es un componente nuevo que cada pantalla tenga que aprender a usar: es el `Historial` de siempre, con más contenido donde corresponde.
- Bug encontrado y corregido en el camino: `npx tsc --noEmit` (usado durante todo el desarrollo) dio en verde, pero `npm run build` (que corre `tsc -b`, modo proyecto) falló con un error real de tipos en los dos `useLiveQuery` nuevos (el `Promise.resolve([])` del lado "no aplica" del ternario se inferá como `Promise<never[]>`, incompatible con `Promise<EjecucionDiagnostico[]>`/`Promise<AccesoBoveda[]>` del otro lado). Corregido anotando el tipo de retorno del callback (`(): Promise<EjecucionDiagnostico[]> =>`) y el valor por defecto (`[] as EjecucionDiagnostico[]`). Lección: `tsc --noEmit` solo no basta para confiar en el build; hay que correr `npm run build` antes de cerrar una tarea.
- Pruebas: 402 en verde (9 nuevas en `lineaDeTiempo.test.ts`: fusión de las 3 fuentes, orden cronológico correcto mezclando fuentes, caso solo-historial sin fuentes extra, caso vacío, formato de duración en sus 3 variantes, las 3 etiquetas de resuelto, las 6 etiquetas de acción de bóveda). Lint, typecheck y build en verde.
- Verificación: en navegador real (dev server, sesión simulada; offline, sin tocar el backend). Ficha de categoría: verificada con categoría con 1 artículo + 1 dispositivo + 1 diagnóstico (las 3 secciones y sus enlaces reales aparecieron, contador correcto) y categoría vacía (sin la línea de contadores, mensaje "Todavía no hay nada en esta categoría"). Línea de tiempo del artículo: sembrada 1 entrada de historial (creación, hace 1 hora) + 1 ejecución de diagnóstico (ahora, resuelto, 185 s), el desplegable mostró "Historial (2)" con la ejecución primero ("Ejecutó el diagnóstico [enlace]... Resuelto · 3 min 5 s") y la creación después, en el orden cronológico correcto y con el enlace al diagnóstico funcionando. Accesos de bóveda: NO se pudo verificar en vivo dentro de `CredencialPage.tsx` porque desbloquear la bóveda real exige la criptografía de la contraseña maestra, que no se puede simular sin credenciales genuinas (misma limitación conocida que la tarea 60); se dio por suficientemente cubierto con las 9 pruebas unitarias de `combinarEventos`/`ETIQUETA_ACCION_BOVEDA` y la verificación en vivo del mismo patrón de fusión para el caso hermano (ejecuciones en el artículo, mismo `EventoItem`/`combinarEventos`). Sin errores de consola nuevos; sesión y datos de prueba eliminados al terminar.
- Documentación: ARQUITECTURA.md sección 5, bullet `historial` (nota sobre cambios de cableado ya incluidos) y bullet nuevo "Línea de tiempo unificada".

### 60. Base de conocimiento inteligente, fase N2 (buscador y contexto)
- Finalizada: 2026-07-16. Sin cambios de esquema en Supabase.
- Descripción: fase N2 de [PROPUESTA_BASE_CONOCIMIENTO.md](PROPUESTA_BASE_CONOCIMIENTO.md) (sección 12, tabla de fases), elegida por el usuario tras quedar bloqueada la línea Nocturne por falta de handoff nuevo. Cuatro piezas, todas sin esquema: (1) categorías y adjuntos como resultado propio del buscador + filtro por tipo; (2) generalizar `dispositivosAfectados` a todo tipo de artículo; (3) propiedades sugeridas por categoría; (4) creación contextual (parte de la sección 7, punto 1: "ningún dato visible en la pantalla de origen se vuelve a escribir en el formulario de destino").
- Solución (buscador, sección 9 de la propuesta): `src/features/busqueda/useIndiceBusqueda.ts` suma 2 tipos de resultado (`TipoResultado` ahora incluye `categoria` y `adjunto`): categorías indexadas por nombre (excluye eliminadas) enlazando a `CategoriaPage`; adjuntos indexados por nombre de archivo desde dos fuentes (tabla `adjuntos` de articulo/dispositivo, con mapa dueño->título/ruta; y la galería inline de cada paso de un procedimiento, `paso.adjuntos`, con subtítulo "Artículo · Paso N" y sin ancla propia porque la ficha no tiene navegación por paso), con miniatura si el archivo es una imagen. `src/features/busqueda/ResultadosBusqueda.tsx` suma los 2 grupos nuevos y chips de filtro por tipo ("Solo soluciones", "Solo dispositivos"...), selección única alternable (mismo patrón que las tarjetas de categoría de Soluciones/Dispositivos), ocultos automáticamente cuando la respuesta cabe en un solo grupo. 2 pruebas nuevas en `busqueda.test.ts`.
- Solución (dispositivosAfectados generalizado, sección 6 de la propuesta): en `ArticuloForm.tsx`, `DispositivosAfectadosEditor` salió del bloque exclusivo de `tipo === 'problema_frecuente'` (que conserva solo síntomas/causas, propios de una incidencia) y ahora se muestra siempre en "Antes de comenzar", con etiqueta dinámica por prop (`etiqueta`): "Dispositivos afectados" en una incidencia, "Equipos donde aplica" en el resto. En la ficha del dispositivo, nuevo inverso `src/features/dispositivos/procedimientosDeDispositivo.ts` + `ProcedimientosDelEquipo.tsx` ("Procedimientos de este equipo", cualquier tipo salvo incidencia, que ya tiene el suyo en `problemasDeDispositivo.ts`/`ProblemasDelEquipo.tsx`), montado junto a él en `DispositivoPage.tsx`. 4 pruebas nuevas en `procedimientosDeDispositivo.test.ts` (mismo patrón que las de `problemasDeDispositivo`).
- Solución (propiedades sugeridas por categoría, sección 7 punto 2): en `DispositivoForm.tsx`, un `useMemo` (`propiedadesSugeridas`, reusando el helper `valoresUnicos` ya existente para marca/ubicación/estado) junta las CLAVES de `detalles` de los demás equipos de la MISMA categoría y las ofrece en un `<datalist>` sobre el campo "Campo" del editor de propiedades personalizadas; se recalcula al cambiar la categoría. Es la plantilla por categoría que se había pedido, pero aprendida del uso real en vez de configurada (sin mantenimiento al aparecer una categoría nueva).
- Solución (creación contextual, sección 7 punto 1): "+ Reportar incidencia de este equipo" y, solo con `puedeVerBoveda`, "+ Guardar credencial de este equipo" nuevos en `DispositivoPage.tsx`, junto al inverso de procedimientos. El primero enlaza a `/soluciones/<categoriaId>/nuevo?tipo=problema_frecuente&dispositivoAfectado=<id>&dispositivoNombre=<nombre>`; `ArticuloForm.tsx` lee esos parámetros (solo al crear desde cero, nunca al editar ni al duplicar) para preseleccionar el tipo y precargar el equipo ya vinculado en `dispositivosAfectados`. El segundo enlaza a `/boveda/nueva?titulo=<sugerido>&categoria=<categoría del equipo>`; `CredencialForm.tsx` lee esos parámetros para precargar título y categoría (editables, el técnico puede borrarlos). Sin vínculo credencial↔dispositivo por id: esa columna es la fase N3, con esquema; por ahora solo se precargan los dos campos de texto. "+ Conexión" desde un switch ya funcionaba antes de esta tarea (`ConexionesFicha`, no tocado).
- Bug encontrado y corregido en el camino: al escribir `propiedadesSugeridas` se declaró el `useMemo` ANTES de `const [categoriaId] = useState('')`, un `ReferenceError: Cannot access 'categoriaId' before initialization` en tiempo de ejecución (zona muerta temporal) que rompía TODA la pantalla "Nuevo dispositivo"/"Editar dispositivo" tras el ErrorBoundary. `tsc --noEmit` y oxlint NO lo detectaron (es un error de orden de ejecución, no de tipos); solo apareció al verificar en navegador real. Corregido moviendo el `useMemo` después de la declaración del estado. Lección para la próxima vez: con hooks que dependen de estado declarado más abajo en el mismo componente, verificar en navegador ANTES de dar por buena la tarea, no solo confiar en typecheck/lint.
- Pruebas: 393 en verde (6 nuevas: 2 de buscador, 4 de `procedimientosDeDispositivo`). Lint y build en verde; typecheck limpio.
- Verificación: en navegador real (dev server, sesión simulada, categorías/dispositivos/artículos/adjunto de paso sembrados en IndexedDB; offline, sin tocar el backend). Buscador: "impresoras" devolvió los 3 grupos nuevos (Categoría, 2 Soluciones, el adjunto de paso) con sus chips; clic en "Solo categorías" filtró a 1 resultado; "manual_zebra" encontró el adjunto (además de artículo/dispositivo por el token compartido "zebra", comportamiento esperado del fuzzy search); "pos" con un solo grupo no mostró chips. Ficha del dispositivo: los 2 botones contextuales aparecieron con los `href` correctos (query params bien codificados); "Procedimientos de este equipo" listó el artículo de configuración y "Problemas frecuentes" el de incidencia, sin cruzarse. Formulario de incidencia abierto con los parámetros de contexto: título "Nuevo problema frecuente", tipo preseleccionado, chip del equipo ya vinculado bajo "Dispositivos afectados"; sin parámetros, etiqueta genérica "Equipos donde aplica" y tipo por defecto "manual". Propiedades sugeridas: categoría Impresoras (con datos) ofreció "puerto"/"switch" en el datalist; categoría POS (sin datos) no mostró datalist. La precarga de `CredencialForm` (título/categoría) se verificó por código y por el `href` generado; no se pudo probar en vivo porque desbloquear la bóveda real exige la criptografía de la contraseña maestra, que no se puede simular sin credenciales genuinas (limitación conocida, no relacionada con este cambio). Sin errores de consola tras la corrección del bug; sesión y datos de prueba eliminados al terminar.
### 59. Rediseño IT Brain: pantalla Soluciones en Nocturne (handoff de Soluciones trasladado)
- Finalizada: 2026-07-16. Sin cambios de esquema en Supabase.
- Descripción: rediseño de la lista de Soluciones (`SolucionesPage.tsx`) siguiendo el diseño `Soluciones.dc.html`. Ese diseño venía en el zip "Prompt Maestro para Claude Design-handoff.zip", que es la línea de TEMA CLARO ya descartada (D-001 interno del zip; D-006 del proyecto la reemplaza por Nocturne). Por eso NO se implementó tal cual: el usuario eligió "traducir a Nocturne" (tomar estructura, layout y contenido del diseño, pero en el sistema oscuro ya vigente), consistente con la Ficha de Procedimiento de la tarea 58. La pantalla antigua solo listaba categorías; el rediseño añade buscador en el sitio, tarjetas de categoría con icono y contador que filtran, y una lista unificada de artículos con icono por tipo, "categoría · tipo · tiempo" y estado.
- Solución (pantalla): `src/features/soluciones/SolucionesPage.tsx` reescrita en Nocturne dentro de su propio `ShellNocturne`: cabecera con título en pregunta ("¿Cómo realizo este procedimiento?") + botón primario "Crear"; buscador delineado (icono lupa, foco en acento) que filtra por título y nombre de categoría (sin acentos); rejilla de 2 columnas de categorías (icono en tesela + nombre + "N artículos"), selección única alternable que resalta en acento (borde + tinte 8% + tesela de icono en acento/15); sección de lista con título dinámico ("Todos los artículos" o la categoría), "Quitar filtros" cuando hay filtro, filas con tesela de icono por tipo, título truncado, metadatos y badges Borrador (borde discontinuo, precaución) / Obsoleto (neutro), separadas por divisor; estado vacío de búsqueda (con "Quitar filtros") distinto del estado "aún no hay artículos". La lista mezcla todas las categorías ordenadas por `orden` y alfabético dentro (mantiene juntos los artículos de una misma categoría, como el mock agrupado).
- Solución (fundaciones reutilizables): `scripts/generar-iconos.mjs` ampliado y `src/components/iconos.tsx` regenerado con 10 iconos Phosphor nuevos (46 en total): MagnifyingGlass, Plus, DownloadSimple, Sliders, PlugsConnected, WarningCircle, Wrench (buscar/crear + tipos de artículo) y Printer, Storefront, VideoCamera (categorías). `src/features/soluciones/iconosSoluciones.ts` (nuevo): `iconoDeTipo` (mapa de los 6 TipoArticulo) e `iconoDeCategoria` (por palabras clave del nombre, con BookOpen de respaldo, porque la columna `icono` de categorías llega vacía del seed y las categorías son dinámicas) + `normalizarTexto` (minúsculas sin acentos con `\p{Diacritic}`). Ruta `soluciones` movida fuera del `Layout` oscuro heredado en `src/App.tsx` (trae su propio ShellNocturne, como la ficha de la tarea 58). `@phosphor-icons/core` se instaló temporalmente (--no-save) solo para regenerar y se desinstaló (package.json/lock sin cambios).
- Decisiones tomadas (revisables): (1) "Crear artículo" es contextual a la categoría: enlace a `/soluciones/<cat>/nuevo` cuando hay una categoría seleccionada, y deshabilitado al 45% (regla de 08_ESTILO) cuando no la hay, porque la creación en el modelo real siempre necesita una categoría (la ruta `/nuevo` la exige y el formulario no tiene selector propio); evita misfilar silenciosamente. (2) Las tarjetas de categoría FILTRAN en el sitio (como el mock) en vez de navegar a `CategoriaPage`; `CategoriaPage`/`ArticuloForm` (aún en slate) siguen accesibles por sus rutas. (3) La lista muestra artículos de cualquier estado con su badge (paridad con el `CategoriaPage` actual y con el mock); el buscador en pantalla es un filtro local del navegador, no el buscador global con minisearch (donde los borradores sí se excluyen). (4) Icono por categoría heurístico por nombre (no bespoke por id, que rompería con categorías nuevas); el campo `icono` del esquema queda sin usar por ahora.
- Pruebas: 387 en verde (sin nuevas: presentación y helper trivial). Lint (oxlint) y build en verde; typecheck limpio.
- Verificación: en navegador real (dev server, sesión simulada y datos sembrados en IndexedDB: 6 categorías y 14 artículos replicando el contenido del diseño; offline, sin tocar el backend). Confirmado por estilos computados en móvil 375x812 (la captura de pantalla volvió a agotar el tiempo por el entorno, como en las tareas 10 y 58): fondo Nocturne #161826, sin desborde horizontal, pestaña "Soluciones" activa en acento con Bóveda visible (perfil con `puedeVerBoveda`). Contadores correctos (POS 3, Impresoras 4, Cámaras 1, Computadores 2, Redes 3, Software 1; total 14); orden por categoría; metadatos con tiempo ("Impresoras · Configuración · 8 min") y sin tiempo cuando el artículo no tiene procedimiento con pasos; badges Borrador y Obsoleto en sus filas. Interacciones: clic en Impresoras filtró a 4 filas, cambió el título a "Impresoras", mostró "Quitar filtros" y convirtió "Crear" en enlace a `/soluciones/c-impr/nuevo` con borde de acento en la tarjeta (verificado con reflow forzado: `border-noct-accent` = #9184d9; la transición CSS de 150ms se congela en el navegador en segundo plano, artefacto de medición ya conocido, no del código); búsqueda "zebra" dejó 1 resultado; búsqueda sin coincidencias mostró el estado vacío con "Quitar filtros". Sin errores de consola; sesión y datos de prueba eliminados al terminar.

### 58. Rediseño IT Brain: Ficha de Procedimiento en Nocturne (handoff nuevo de Claude Design)
- Finalizada: 2026-07-16. Sin cambios de esquema en Supabase.
- Descripción: primera pantalla del handoff "Herramienta IT para técnicos-handoff.zip" (diseño "Ficha de Procedimiento.dc.html"), que estrena el sistema visual Nocturne (oscuro: fondo #161826, un solo acento #9184d9 delineado, Inter, iconos Phosphor, densidad alta). Según las decisiones D-006 y D-008 del proyecto de diseño, Nocturne reemplaza la dirección de tema claro del handoff anterior; las pantallas claras (tareas 55 a 57) se re-autorizarán después. Esta pantalla fija el shell móvil Nocturne y los patrones nuevos: panel de aviso por tono, insignia de paso, casilla de tarea, bloque protegido de credencial y tarjeta de vínculo a otra entidad.
- Solución (fundaciones, reutilizables por las próximas pantallas Nocturne):
  - Tokens `@theme` de Tailwind v4 en `src/index.css` (prefijo `noct-`): colores base, rampas neutral/acento 100-900 y estados en OKLCH exactos de 08_ESTILO.md; más el foco/selección en acento y la regla "fila fundida" (divisor que se desvanece 48px en los extremos, firma de Nocturne) acotados al shell con la clase `.nocturne`.
  - Fuente Inter variable local (`src/assets/inter-latin-wght-normal.woff2`, subconjunto latino de @fontsource instalado temporalmente con --no-save): sin Google Fonts (rompería el offline); entra al precache del service worker por el patrón woff2 ya existente. Solo se aplica dentro del shell (`font-inter`).
  - `src/components/iconos.tsx` (nuevo, generado): 36 iconos Phosphor (MIT) inlineados como componentes (regular + variantes fill de las pestañas), 3.8 kB gzip. Sin CDN (offline) ni dependencia pesada; regenerable con `@phosphor-icons/core` temporal (mismo patrón que el ícono de la app con sharp).
  - `src/app/ShellNocturne.tsx` (nuevo): columna 448px + 5 pestañas inferiores fijas con blur y safe-area; Bóveda solo con `puedeVerBoveda` (mismo patrón que BottomNav/AppShell). `src/components/nocturne.tsx` (nuevo): TituloSeccion, TagNeutral y clases de botón .btn del sistema.
- Solución (pantalla): `ArticuloPage.tsx` reescrita: cabecera compacta (volver contextual a la categoría; acciones Asistente delineado en acento + Editar + menú "···" con Compartir, Duplicar, Reiniciar progreso y Eliminar, regla 4 de 08_ESTILO.md); bandas de estado borrador/obsoleto con los textos del diseño (fieles al comportamiento real de exclusión del buscador); etiquetas de metadatos (categoría, dificultad, tiempo con reloj, chip de estado); línea "tipo · vX.Y · Actualizado el D mmm por Autor"; relacionados e inversos como filas del diseño (libro + caret + regla fundida); incidencia (síntomas/causas/afectados) y pie de etiquetas en el mismo lenguaje. `ProcedimientoVista.tsx` reescrita: stepper vertical con TODOS los pasos visibles (sin acordeón), insignia 28px (completo: tinte acento + check; actual: número con glow; pendiente: divisor) unida por línea conectora, barra de progreso pegajosa arriba con blur (nivel 0; prop `progresoPegajoso` la apaga en la vista previa del formulario, que tiene su propia cabecera pegajosa), casillas de 18px con tachado, etiqueta "Verificación" en las comprobaciones, verificación final siempre visible y deshabilitada hasta completar los pasos ("Se habilita al completar los N pasos"), banner de completado en color de éxito, y tarjetas de vínculo con kicker ("Continúa en" acento para subprocedimientos, "Solución" en precaución para soluciones y decisiones), conservando el contador X/Y, la expansión inline de un solo nivel y toda la lógica de ejecución (useProcedimientoEjecucion intacto; el avance ahora hace scroll al siguiente paso pendiente). `tonos.ts` con los 5 tonos en colores de estado Nocturne e iconos Phosphor (el selector del editor ahora lista solo etiquetas, sin emoji). `CredencialEnPaso.tsx` como bloque protegido del diseño (borde discontinuo, candado, kicker "Datos protegidos", contraído por defecto) y `CampoSecreto.tsx` como fila etiqueta + valor mono + iconos de copiar/mostrar (verde al copiar). Ruta del artículo fuera del Layout oscuro en `src/App.tsx`.
- Decisiones tomadas (revisables): (1) "Modo asistente" no existe en el diseño pero es función clave: queda como botón primario "Asistente" en la cabecera (08_ESTILO permite 2 acciones + menú); (2) "Reiniciar progreso" vive en el menú "···" (el diseño no dibuja reinicio); (3) las imágenes reales usan object-contain con alto máximo en vez del recorte de 180px del mock (una captura técnica no debe recortarse) y conservan el visor con zoom; (4) el desbloqueo de la bóveda muestra el campo de contraseña maestra directamente (el mock solo dibuja el botón, pero el flujo real necesita el campo); (5) los componentes compartidos con pantallas aún no re-autorizadas (Historial, ReferenciadoPor, Adjuntos, DialogoEliminar, IndicadorVencimiento) conservan su estilo slate hasta que sus pantallas se rediseñen; BloqueVista/CampoSecreto/CredencialEnPaso sí pasaron a Nocturne y se ven así también dentro del asistente y la bóveda (ambos oscuros, transición aceptable).
- Pruebas: 387 en verde (sin pruebas nuevas: cambios de presentación; la lógica de ejecución no se tocó y ya está cubierta). Lint y build en verde; la ficha pesa 4.2 kB gzip y los iconos 3.8 kB.
- Verificación: en navegador real (dev server, sesión simulada y datos sembrados en IndexedDB replicando el contenido del diseño: Zebra ZT411 con 5 pasos, credencial, subprocedimiento y 2 relacionados; offline, sin tocar el backend). Confirmado en móvil 375x812 por estilos computados (la herramienta de captura de pantalla volvió a fallar por el entorno, como en la tarea 10): fondo #161826, Inter Variable cargada, columna 448px, h1 22/500, barra pegajosa con blur, insignia actual con borde acento y glow 35%, casillas 18px radio 4, 5 pestañas con hairline y blur. Interacciones: marcar las 2 tareas del paso 1 lo completó (check + "1 de 5 pasos" + tachado); insignias completan pasos; verificación final pasó de deshabilitada (con nota) a marcable y apareció "Procedimiento completado"; "Reiniciar progreso" del menú volvió a 0 de 5; el bloque de credencial expandió al estado "bóveda bloqueada" con campo de contraseña maestra y volvió a contraerse; completar el subprocedimiento anidado (2/2) completó solo el paso 5 y retiró el aviso; artículo sin procedimiento sin botón Asistente ni "Reiniciar progreso", con Markdown e inverso "Aparece como relacionado en" enlazando de vuelta; banda y chip de Borrador en precaución 30%/10% exactos. Sin errores de consola; sesión y datos de prueba eliminados al terminar.
- Documentación: ARQUITECTURA.md sección 3 (subsección "Sistema visual Nocturne").

### 57. Rediseño IT Brain: pantalla Dispositivos (handoff de Claude Design)
- Finalizada: 2026-07-16. Sin cambios de esquema en Supabase.
- Descripción: tercera pantalla del rediseño en tema claro del mismo handoff (`Dispositivos.dc.html`), reutilizando el AppShell y los helpers ya construidos. `DispositivosPage.tsx` reescrita: encabezado en forma de pregunta ("¿Qué sabes sobre este equipo?"), chips de categoría (selección única, alternable) en vez del desplegable anterior, un solo buscador (nombre, IP, serial, ubicación), resumen de estados sobre el total y filas con icono por tipo, "categoría · ubicación", IP en escritorio y punto + etiqueta de color de estado. Mantiene sin cambios las categorías de red excluidas (se muestran en Red, no aquí; decisión ya existente antes del rediseño, no tocada).
- Solución (reuso): icono de fila con `IconoNodo`/`tipoDeNodoVisual` y estado con `estadoConEtiqueta`, los mismos helpers de las tareas 55 y 56. `src/components/MiniaturaPortada.tsx` ganó un prop `className` opcional (con el valor anterior como valor por defecto, sin romper sus otros 4 usos) para poder mostrar la foto de portada del dispositivo dentro de la tesela de 38px de la fila cuando existe, cayendo al icono de categoría si no hay foto: conserva una función real (vista previa de foto) que el diseño no contemplaba pero que ya existía en el código.
- Solución (acciones del encabezado): "Escanear" y "Crear dispositivo" tal cual el diseño (con destino real a `/escaner` y `/dispositivos/nuevo`). Las dos acciones que el diseño no dibuja pero que ya existían (Etiquetas QR, Importar) se movieron a un menú "···" nuevo (botón con panel flotante, se cierra al hacer clic fuera), siguiendo la regla 4 de 08_ESTILO.md ("acciones visibles solo las importantes; el resto en menú ···") en vez de descartarlas.
- Solución (rutas, `src/App.tsx`): solo el listado `dispositivos` sale del Layout oscuro; el formulario de creación/edición, el importador y la ficha del dispositivo siguen en el Layout hasta que se rediseñen (la ficha es la próxima candidata, junto con "Ficha de Dispositivo.dc.html").
- Decisión tomada (revisable, anotada en TAREAS.md): igual que en Red, el resumen de estados se calcula sobre el total de equipos generales sin filtrar (vista general de un vistazo, tal como hace el mock del diseño), no sobre el resultado filtrado por categoría o búsqueda.
- Pruebas: 387 en verde (sin pruebas nuevas: la pantalla reutiliza helpers ya probados en las tareas 55 y 56; `MiniaturaPortada` no tiene lógica propia que probar, solo una prop de estilo). Lint y build en verde.
- Verificación: en navegador real (dev server, sesión simulada y 7 dispositivos + 4 categorías sembrados en IndexedDB, offline, sin tocar el backend; una de las 7 es de categoría "Redes" para confirmar que queda excluida). Confirmado en escritorio (1280px): 3 chips de categoría (sin "Redes"), 6 dispositivos generales listados (el de red no aparece), resumen "6 equipos · 3 operativos · 1 en mantenimiento · 1 fuera de servicio". Filtro por categoría "POS" dejó solo los 2 terminales POS. El menú "···" mostró los enlaces reales a Etiquetas QR e Importar. Sin errores de consola; sesión y datos de prueba eliminados al terminar.

### 56. Rediseño IT Brain: pantalla Red (handoff de Claude Design)
- Finalizada: 2026-07-16. Sin cambios de esquema en Supabase.
- Descripción: segunda pantalla del rediseño en tema claro del mismo handoff (`Red.dc.html`), reutilizando el AppShell y los helpers construidos en la tarea 55. `RedPage.tsx` reescrita: encabezado en forma de pregunta ("¿Cómo está conectada la infraestructura?"), tarjeta destacada hacia la Topología, buscador único (sin los desplegables de categoría/estado de la versión anterior), y los equipos de red agrupados por ubicación con icono por tipo, "categoría · marca modelo", IP en escritorio y punto + etiqueta de color del estado.
- Solución (reuso, `src/features/red/IconoNodo.tsx` nuevo): se extrajo el renderizador de iconos que antes vivía dentro de `TopologiaPage.tsx` a un componente compartido, y se le agregó el tipo `ups` (categoría del diseño de Red que no existe en las categorías por defecto del esquema; se agrega igual por si el equipo la crea). `TopologiaPage.tsx` ahora importa `IconoNodo` en vez de definirlo.
- Solución (`src/features/red/topologiaVisual.ts`): `tipoDeNodoVisual` reconoce `ups` (o "regulador"); nueva función `estadoConEtiqueta` (clase del punto + clase de texto a juego + etiqueta), que `puntoDeEstado` ahora reutiliza por dentro para no duplicar las cuatro reglas de color.
- Solución (`RedPage.tsx`): agrupación por ubicación (texto libre) en orden alfabético natural, con "Sin ubicación" siempre al final; el buscador ya cubre categoría, ubicación, IP, marca y modelo (antes eran dos `<select>` de categoría y estado). Fila de equipo con tesela de icono de 38px, nombre truncado, línea "categoría · marca modelo", IP en monoespaciado solo en escritorio (`lg:`), y punto + etiqueta de estado en el color a juego. Estado vacío distingue "aún no hay equipos" de "no se encontraron equipos" (según haya o no texto de búsqueda), con "Quitar búsqueda" cuando aplica.
- Solución (rutas, `src/App.tsx`): `red` sale del Layout oscuro al lado de `red/topologia`, con su propio Suspense.
- Decisiones tomadas (revisables, quedan anotadas en TAREAS.md): (1) el botón primario del diseño decía "Crear conexión"; como hoy no existe una pantalla dedicada a crear una conexión suelta (las conexiones se crean desde la ficha de un dispositivo ya existente, `ConexionesFicha.tsx`), se mantuvo el destino real y funcional de la versión anterior (`/dispositivos/nuevo`) con la etiqueta honesta "Nuevo equipo" en vez de prometer una acción que no existe. (2) Se retiran los filtros de categoría y estado en desplegable: el diseño resuelve todo con un solo buscador (coincide con 08_ESTILO: "un objetivo por pantalla"); el buscador ya filtra por esos mismos campos escribiendo el texto.
- Pruebas: 387 en verde (3 nuevas: `ups` en `tipoDeNodoVisual` y 2 en `estadoConEtiqueta`, que confirman que coincide con `puntoDeEstado` para los mismos estados). Lint y build en verde.
- Verificación: en navegador real (dev server, sesión simulada y 6 dispositivos + 8 categorías sembrados en IndexedDB, offline, sin tocar el backend; incluye una categoría "UPS" no default para probar el icono nuevo). Confirmado en escritorio (1280px): sidebar con Red activa, tarjeta de Topología, 3 grupos por ubicación en orden alfabético (Bodega, Rack principal, Sala de ventas), iconos correctos incluido UPS, IP visible, "En mantenimiento" en ámbar. Buscador: un texto sin coincidencias mostró "No se encontraron equipos" con "Quitar búsqueda", que al pulsarlo restauró la lista completa. Sin errores de consola; sesión y datos de prueba eliminados al terminar.

### 55. Rediseño IT Brain: pantalla Topología (handoff de Claude Design)
- Finalizada: 2026-07-16. Sin cambios de esquema en Supabase.
- Descripción: primera pantalla del rediseño en tema claro entregado como handoff de Claude Design (`Prompt Maestro para Claude Design-handoff.zip`, diseño `Topologia.dc.html` del proyecto "Prompt Maestro para Claude Design"). La Topología de red se reescribió con la especificación visual exacta del diseño y quedó como la primera pantalla que estrena el patrón AppShell del rediseño; el resto de la app sigue en el Layout oscuro y migrará pantalla por pantalla (decisión D-002 del proyecto de diseño).
- Solución (shell, `src/app/AppShell.tsx` nuevo): shell claro responsive del rediseño. Escritorio (>=1024px): sidebar fija de 240 px con marca, los 5 módulos (activo con fondo azul suave y texto primario) y bloque de usuario al pie (nombre y correo reales del perfil, enlaza a /cuenta). Móvil (<1024px): cabecera compacta con marca y avatar de iniciales (enlaza a /cuenta) más pestañas inferiores fijas con blur y safe-area. La Bóveda solo aparece con `puedeVerBoveda`, igual que en BottomNav. Paleta del diseño en clases estándar de Tailwind (zinc para neutros, blue-600 primario), sin media queries en JS: todo con `lg:`.
- Solución (pantalla, `src/features/red/TopologiaPage.tsx` reescrita): breadcrumb Red / Topología (con tercer nivel cuando se abre desde un equipo), título de 24 px con subtítulo, botones "Expandir todo" y "Contraer", leyenda de estados (verde/ámbar/rojo), tarjeta del árbol con scroll horizontal (min-width 480 px), filas con toggle o punto de hoja, icono del tipo de equipo en tesela de 30 px, nombre en 13.5 px seminegrita, línea de detalle "categoría · marca modelo · puerto · medio" truncable, punto de estado de 7 px con title y chevron de navegación; hover de fila y pie de ayuda del diseño. Estado de expansión con base + inversiones: la base es 'inicial' (dos primeros niveles abiertos, comportamiento de la fase R1), 'todo' o 'nada' según los botones, y cada toggle individual se guarda como inversión sobre la base con una clave de camino única por instancia (el mismo equipo puede aparecer en dos ramas). Se conservan las funciones existentes que el diseño no dibuja: buscador con expansión automática de ancestros, resaltado (azul suave) y scroll al primer resultado; vista desde un equipo (`/red/topologia/:dispositivoId`); marca de ciclo (↺, ahora ámbar); estado vacío con borde discontinuo.
- Solución (helpers puros, `src/features/red/topologiaVisual.ts` + test): `tipoDeNodoVisual` (nombre de categoría a icono, insensible a mayúsculas y acentos, con orden de reglas que evita falsos positivos: "Puntos de red" no es POS ni router, "punto de venta" sí es POS; genérico como respaldo), `puntoDeEstado` (estado sugerido a color del tema claro, gris neutro y "Sin estado" como respaldo) y `detalleDeNodo` (compone la línea de detalle omitiendo partes vacías y sin repetir el medio cuando ya es la via). Los 7 iconos del diseño (router, switch, AP, punto de red, PC, impresora, POS) más rack, cámara, servidor y genérico para las categorías reales del esquema, todos en trazo 1.8 estilo Lucide.
- Solución (rutas, `src/App.tsx`): `red/topologia` y `red/topologia/:dispositivoId` salen del Layout oscuro y se montan con Suspense propio, porque la pantalla trae su AppShell.
- Decisiones tomadas (revisables): (1) el rótulo del shell dice "IT Brain" como en el diseño, aunque el nombre sigue listado como decisión abierta de PROPUESTA_MODULOS; cambiarlo a "Soluciones IT" es una línea en `src/app/AppShell.tsx`. (2) La apertura inicial del árbol conserva los dos niveles de la fase R1 en vez del "todo expandido" del prototipo, porque el diseño trae los botones para ampliar y los datos reales pueden ser grandes. (3) El chip de via con emoji se reemplazó por la línea de detalle del diseño (08_ESTILO: nunca emoji en la interfaz); `iconoDeVia` sigue en uso en ImpactoYDependencias.
- Pruebas: 384 en verde (13 nuevas en `topologiaVisual.test.ts`). Lint y build en verde.
- Verificación: en navegador real (dev server, sesión simulada y datos sembrados en IndexedDB, offline, sin tocar el backend; 11 dispositivos y 10 conexiones replicando el árbol del diseño). Confirmado en escritorio: sidebar con los 5 módulos y Red activa, breadcrumb, leyenda, árbol con iconos por tipo, detalles compuestos y puntos verde/ámbar/rojo. Interacciones: toggle individual (SW-BODEGA-02 abre sus 2 hijos), "Expandir todo" (los 11 equipos en el orden del recorrido), "Contraer" (solo la raíz), buscador con el árbol contraído ("imp-bodega" expandió la cadena RT a SW-RACK a SW-BODEGA a PR-B1-07 y resaltó IMP-BODEGA-01). En móvil (375x812): cabecera con avatar AT, pestañas inferiores con Red activa y scroll horizontal del árbol. Vista desde un equipo (`/red/topologia/sw2`): breadcrumb de 3 niveles, título "Topología desde SW-BODEGA-02" y subtítulo propio. Sin errores de consola; sesión y datos de prueba eliminados al terminar.

### 54. Base de conocimiento inteligente, fases N0 y N1
- Finalizada: 2026-07-15. Sin cambios de esquema en Supabase.
- Descripción: primeras fases de [PROPUESTA_BASE_CONOCIMIENTO.md](PROPUESTA_BASE_CONOCIMIENTO.md), el rediseño arquitectónico hacia el principio "cada dato existe una sola vez y todo lo demás lo referencia". N0: vocabularios derivados (autocompletar). N1: el grafo de referencias derivado, el inverso universal "Referenciado por", el aviso de impacto antes de eliminar y la regla de referencia viva.
- Solución (lógica pura, con pruebas):
  - `src/lib/referencia.ts` (+ test): la regla de referencia viva. `textoVivo(vivo, copia)` prefiere el dato vivo y cae a la copia congelada solo si el vivo está vacío (fila no sincronizada o eliminada); `mapaDeTextos`/`nombreVivo` la aplican sobre una lista de filas locales excluyendo las eliminadas. Documenta la excepción deliberada: los registros inmutables (historial, ejecuciones, accesos) NO la usan, guardan textos congelados a propósito por ser fotos del pasado.
  - `src/lib/grafo.ts` (+ test): `construirGrafo(datos)` recorre los datos locales (procedimientos con sus pasos y bloques vía `normalizarProcedimiento`, relacionados, dispositivos afectados, opciones de diagnóstico y conexiones) y produce las aristas tipadas del sistema (del que referencia al referenciado), con el título del origen VIVO por construcción. `referenciasHacia` (inverso universal, con filtro opcional por tipo de relación y sin contar una entidad como referencia de sí misma), `origenesDistintos` (colapsa un origen que referencia varias veces) y `resumenImpacto` (frase para el aviso de eliminación, "Se usa en 1 procedimiento y 1 diagnóstico"). Decisión clave: el grafo es DERIVADO, nunca almacenado; se reconstruye de los datos locales como el índice de búsqueda, así que no puede desincronizarse, no necesita esquema ni sincronización nueva y es lógica pura probada. NO se filtra por estado del artículo: un borrador que referencia algo es un vínculo real que se rompería al eliminarlo.
- Solución (interfaz):
  - `src/components/useGrafo.ts` (hook) y `src/components/ReferenciadoPor.tsx` (nuevos): el hook reconstruye el grafo desde los datos locales en vivo; el componente muestra las referencias entrantes agrupadas por tipo de relación con enlace a cada origen, y se oculta si no hay ninguna. Se limita por ficha para no duplicar bloques existentes.
  - `src/components/DialogoEliminar.tsx`: prop opcional `advertencia` que muestra un recuadro ámbar con el impacto antes de confirmar.
  - Fichas: `ArticuloPage` (bloque "Referenciado por" con subprocedimiento/solución/decisión/diagnóstico, excluyendo 'relacionado' que ya tiene su bloque, más el aviso de impacto), `CredencialPage` ("Usada en" con los procedimientos que la muestran, más aviso), `DispositivoPage` (solo el aviso de impacto: sus inversos ya se ven en Problemas del equipo/Conexiones/Impacto).
  - Referencia viva aplicada al caso estrella: `ConexionesFicha` resuelve el nombre del otro extremo contra los dispositivos vivos (antes mostraba `origenNombre`/`destinoNombre` congelados; renombrar un switch ya no deja las conexiones con el nombre viejo). `resumenConexion` de `lib/conexiones.ts` NO se tocó: alimenta el historial, donde la copia congelada es correcta.
  - N0: `DispositivoForm` con datalist de ubicación y marca (helper `valoresUnicos`, deduplica sin distinguir mayúsculas); `ArticuloForm` (`EtiquetasEditor`) con datalist de etiquetas ya usadas.
- Fuera de alcance en N1 (documentado): los títulos de paso desde un subprocedimiento y los títulos de artículo durante la ejecución de un diagnóstico quedan como puntos en el tiempo (refrescan al re-guardar); resolverlos en vivo metería consultas al camino caliente de ejecución por un caso borde. El resto del "Referenciado por" es vivo por construcción.
- Pruebas: 374 en verde (20 nuevas: 8 en `referencia.test.ts`, 12 en `grafo.test.ts`). Typecheck, lint y build en verde.
- Verificación: en navegador real con datos sembrados en IndexedDB y sesión simulada localmente (offline, sin tocar el backend). Confirmado: la ficha del subprocedimiento muestra "Referenciado por" con los dos inversos ("Ejecutado por el diagnóstico" y "Usado como subprocedimiento en") con título VIVO; el aviso de impacto al eliminar el subprocedimiento ("Se usa en 1 procedimiento y 1 diagnóstico") y al eliminar el switch ("Se usa en 1 conexión"); la ficha del AP muestra "Switch renombrado" (nombre vivo) donde la conexión guardaba "Switch VIEJO"; los datalists de ubicación y marca poblados con los valores derivados. Sin errores de consola; datos de prueba y sesión limpiados al terminar. La ficha de credencial no se recorrió porque la bóveda exige el verificador del servidor (no alcanzable en la simulación offline), pero su "Referenciado por" reutiliza el mismo componente y grafo ya verificados en el artículo.
- Documentación: ARQUITECTURA.md (nueva sección 15) y [PROPUESTA_BASE_CONOCIMIENTO.md](PROPUESTA_BASE_CONOCIMIENTO.md).

### 53. Fase D2 de la propuesta por módulos: "Probar diagnóstico"
- Finalizada: 2026-07-10. Sin cambios de esquema en Supabase.
- Descripción: fase D2 de [PROPUESTA_MODULOS.md](PROPUESTA_MODULOS.md): un botón en el editor de diagnósticos que abre el árbol en modo prueba (recorrerlo sin guardar ni registrar, sin ejecutar de verdad los procedimientos), más detectar respuestas que ejecutan un procedimiento ya no disponible.
- Solución (lógica pura, `src/lib/diagnostico.ts`):
  - Se extrajo la máquina de estados del recorrido a transiciones puras y probadas: `AvanceDiagnostico` (camino + estado + articulosEjecutados) con `avanceInicial`, `avanceAlResponder`, `avanceTrasArticulo`, `avanceAlRetroceder`. `progresoDiagnostico.ts` (el asistente real) se refactorizó para usarlas, conservando sus efectos (persistencia en `progresoDiagnostico` y `reiniciarProgreso` del procedimiento vinculado). Así "qué respuesta lleva a dónde" es idéntico en el asistente real y en el modo prueba, sin duplicar la lógica.
  - `procedimientosVinculadosRotos(nodos, disponible)`: dado un predicado "¿este articuloId sigue siendo ejecutable?", devuelve los problemas por respuestas que apuntan a un procedimiento eliminado/sin pasos/inexistente. Pura porque la consulta a la base (asíncrona) queda fuera.
- Solución (interfaz):
  - `PruebaDiagnostico.tsx` (nuevo): overlay a pantalla completa desde el editor. Recorre los nodos EN MEMORIA con las transiciones puras; al llegar a un estado 'articulo' NO ejecuta `AsistenteVista` sino que muestra una tarjeta ("aquí se ejecutaría X" si es ejecutable, o un aviso rojo de vínculo roto si no) con un botón "Continuar (como si estuviera completo)". Banda "Modo prueba", barra de progreso reutilizando `porcentajeDiagnostico`, Volver/Reiniciar, y manejo del caso "esta respuesta apunta a una pregunta que ya no existe".
  - `DiagnosticoForm.tsx`: botón "Probar" junto a "Guardar"; consulta `idsEjecutables` (artículos no eliminados con procedimiento, sin importar su estado, porque el asistente real los ejecuta igual) y muestra las advertencias de vínculos rotos en ámbar. Decisión de diseño: las advertencias NO bloquean el guardado (en una app local primero, un artículo puede estar solo sin sincronizar, no realmente borrado); se muestran como aviso, separado de los errores rojos de `validarNodos` que sí bloquean.
- Pruebas: 349 en verde (10 nuevas en `diagnostico.test.ts`: las 4 transiciones con sus casos límite y `procedimientosVinculadosRotos`; las 10 pruebas de `progresoDiagnostico.test.ts` siguen pasando tras el refactor, confirmando que el comportamiento real no cambió). Typecheck, lint y build en verde.
- Verificación: la máquina de estados y la detección de vínculos rotos (el corazón de la fase) quedan cubiertas por pruebas unitarias; el componente `PruebaDiagnostico` es una capa de presentación delgada sobre esas transiciones puras. NO se pudo hacer la verificación interactiva en navegador real en esta sesión porque la herramienta de navegador (`preview_eval`) cambió a media sesión y dejó de estar disponible; el dev server compiló los archivos nuevos sin errores y el build de producción (typecheck + bundle completo) pasó. Conviene una pasada manual rápida en el navegador cuando se retome: abrir un diagnóstico, "Probar", recorrer una rama con procedimiento y confirmar el aviso de vínculo roto.
- Documentación: ARQUITECTURA.md sección 5 (párrafo de diagnosticos).

### 52. Grupo de cambios de esquema de la propuesta por módulos
- Finalizada (código): 2026-07-10. Pendiente de un paso del usuario en Supabase (aplicar `schema.sql`, ver más abajo).
- Descripción: última fase pendiente de [PROPUESTA_MODULOS.md](PROPUESTA_MODULOS.md), sección 7: agrupa TODOS los cambios de esquema restantes de la propuesta en una sola actualización de `schema.sql`, para que el usuario solo tenga que intervenir Supabase una vez (S3 + B2 + D3 + B3 + foto de Dis2). Decisión del usuario: 3 estados de artículo (borrador/publicado/obsoleto, sin "en revisión").
- Solución (esquema, `supabase/schema.sql`):
  - `articulos` gana `estado` (check `borrador|publicado|obsoleto`, default `publicado`), `version` (texto, default `'1.0'`) y `relacionados` (jsonb, default `'[]'`).
  - `dispositivos` gana `foto` (jsonb, nullable).
  - `credenciales` gana `vence_en` (date, nullable, sin cifrar a propósito).
  - `ejecuciones_diagnostico` gana `motivo` (check con lista cerrada + `''`) y `solucion_propuesta` (text).
  - Tabla nueva `accesos_boveda` (auditoría, solo inserción, mismo patrón que `historial`/`ejecuciones_diagnostico`: cursor `recibido_en`, RLS de lectura e inserción restringida a `puede_ver_boveda`, sin políticas de UPDATE/DELETE).
  - Todo con `alter table add column if not exists` y `drop/add constraint`, ejecutable varias veces sin error, siguiendo el patrón ya establecido del archivo.
- Solución (modelo local, `src/lib/db.ts` + `src/lib/tablas.ts`): `EstadoArticulo`, `ArticuloRelacionado`, `MotivoNoResuelto`, `AccionBoveda`, `AccesoBoveda` (tabla local nueva, versión Dexie 9); mapeos locales↔remotos actualizados; `accesos_boveda` sumada a `TABLAS_SINCRONIZADAS` y excluida de `TablaEditable` (solo se agrega vía repositorio, nunca se edita).
- Solución (Soluciones — estado, versión y relacionados):
  - `src/lib/version.ts` (nuevo, puro y probado): `siguienteVersion(actual, cambioMayor)` sube la versión menor por defecto o la mayor con "Cambio mayor", tolerante a texto mal formado.
  - `ArticuloForm.tsx`: selector de Estado y checkbox "Cambio mayor" (solo visible editando un artículo ya publicado, con vista previa de qué versión resultará); la versión solo sube al editar un artículo que YA estaba publicado, nunca en un borrador ni al crear; duplicar un artículo copia los relacionados pero el estado vuelve a borrador y la versión a 1.0. Editor de relacionados (`RelacionadosEditor`, mismo patrón de chips + select que dispositivos afectados).
  - `ArticuloPage.tsx`: banda visual de estado (nada si publicado, ámbar si borrador, rojo si obsoleto), versión junto al tipo, y sección "Relacionados" con el inverso "Aparece como relacionado en..." calculado localmente sin guardarlo.
  - Un borrador u obsoleto se excluye del buscador global (`useIndiceBusqueda.ts`), de las rutas de inicio (`InicioPage.tsx`) y de los vinculables de procedimientos y diagnósticos (`PasosEditor.tsx`, `DiagnosticoForm.tsx`).
- Solución (Bóveda — vencimiento y auditoría):
  - `src/lib/vencimiento.ts` (nuevo, puro y probado): `estadoVencimiento` devuelve `'vencida'`, `'proxima'` (dentro de 30 días) o `null`.
  - `IndicadorVencimiento.tsx` (nuevo): chip ámbar/rojo, reutilizado en `BovedaPage.tsx` (listado), `CredencialPage.tsx` (ficha) y `CredencialEnPaso.tsx` (dentro de un procedimiento). Campo de fecha en `CredencialForm.tsx`.
  - `registrarAccesoBoveda` en `src/lib/repositorio.ts` (mismo patrón que `registrarEjecucionDiagnostico`). `CampoSecreto.tsx` gana un `onCopiado` opcional (el componente reutilizable no decide qué auditar; quien lo usa sí). Se registra: `consulto` al abrir la ficha o el vínculo en un paso (con guardia por ref para no duplicar en cada refresco), `mostro` al revelar la contraseña, `copio_usuario`/`copio_contrasena` al copiar, `modifico` al guardar una edición y `elimino` al borrar. `CredencialPage.tsx` muestra "Último acceso" (la entrada más reciente, sin contar la del propio "consulto" de esta visita).
  - De paso se corrigieron dos restos de la fase Dis1+B1 que quedaron con el nombre viejo: "Abrir en Notas" y "Ábrela en la sección Notas" en `CredencialEnPaso.tsx` ahora dicen "Bóveda".
- Solución (Diagnóstico — motivo y sugerencias):
  - `DiagnosticoRunPage.tsx`: al responder "No" en el resultado, pide el motivo (lista cerrada + "Otro") antes de cerrar, y si es "Encontré otra solución" un texto libre; `registrarEjecucionDiagnostico` guarda ambos.
  - `SugerenciasEquipoPage.tsx` (nuevo, `/diagnostico/sugerencias`, enlazado desde `DiagnosticosPage.tsx`): lista los textos con motivo "Encontré otra solución", para que quien mantiene la base los revise. Sin flujo de aprobación formal todavía.
- Solución (Dispositivos — foto):
  - `DispositivoForm.tsx` gana un id estable desde el inicio (mismo patrón que `ArticuloForm.tsx`) y un editor de foto (`FotoEditor`, subida comprimida con cola offline, idéntico patrón a la portada de un procedimiento). No se copia al duplicar (equipo físico distinto).
  - `DispositivoPage.tsx` (banner), `DispositivosPage.tsx` (miniatura en el listado) y el índice de búsqueda (`portadaRef`) muestran la foto; el escaneo de QR ya abría la ficha completa, así que hereda la foto sin cambios.
- Solución (mantenimiento relacionado): `scripts/respaldo-supabase.sh` y `supabase/RESPALDO.md` tenían la lista de tablas desactualizada desde antes de esta tarea (les faltaban `conexiones`, `diagnosticos` y `ejecuciones_diagnostico`); se corrigió de una vez y se agregó `accesos_boveda`.
- Pruebas: 339 en verde (16 nuevas: `version.test.ts` con 3, `vencimiento.test.ts` con 5, más las de fixtures actualizados). Typecheck, lint (sin advertencias, incluida una de `exhaustive-deps` resuelta con un guard por `ref` en vez de silenciarla) y build en verde.
- Verificado en navegador real (sesión simulada + datos sembrados en las 4 áreas, luego limpiados con `cambiosPendientes` en 0): estado "Publicado"→"Cambio mayor" subió la versión de 1.0 a 2.0 y guardó los relacionados en ambos sentidos; marcar un artículo como borrador mostró la banda y lo sacó del buscador global; la bóveda mostró el aviso de vencimiento próximo en el listado y la ficha, "Último acceso" tras una segunda visita, y las entradas de auditoría (`consulto`, `mostro`) quedaron en `accesos_boveda`; el flujo completo de diagnóstico con "No" → "Encontré otra solución" → texto libre quedó registrado y visible en "Sugerencias del equipo"; el campo de foto de dispositivo se mostró en el formulario y el modelo guardó/leyó correctamente (la subida real a Storage no se pudo probar en este entorno, pero reutiliza exactamente el patrón ya verificado de la portada de procedimientos).
- Pendiente (usuario): aplicar el `supabase/schema.sql` actualizado en Supabase. Hasta entonces estado/versión/relacionados/foto/vencimiento/auditoría/motivo funcionan localmente en cada dispositivo pero no se sincronizan con el resto del equipo (la sincronización de esas columnas y de `accesos_boveda` falla sin frenar el resto, mismo criterio que las tablas de diagnóstico).
- Documentación: ARQUITECTURA.md sección 5 (los 5 párrafos de tablas afectadas); RESPALDO.md y TAREAS.md (tarea 15) con la lista de tablas corregida.

### 51. Fase R1 de la propuesta por módulos: árbol de red enriquecido
- Finalizada: 2026-07-09. Sin cambios de esquema en Supabase.
- Descripción: cuarta fase de [PROPUESTA_MODULOS.md](PROPUESTA_MODULOS.md) (puntos 5, 6, 7, 8, 9, 10 y 12 del documento de Red). Enriquece el árbol de topología ya existente sin cambiar su arquitectura; deja pendiente el mapa interactivo (R2, mucho más costoso) para decidir después de usar esto.
- Solución (modelo, `src/features/red/arbol.ts`, lógica pura y probada):
  - `NodoTopologia` gana `estado`, `categoriaId`, `tipoConexion` (null en la raíz) y `medio`; `construirArbol`/`construirBosque` ahora reciben un `Map<string, InfoDispositivo>` (`infoDeDispositivos`, nuevo) en vez de solo nombres.
  - `contarImpacto(nodo)`: cuenta TODOS los descendientes del árbol ya construido, agrupados por categoría (no solo los hijos directos), para "si este equipo falla también quedan sin servicio: N POS...".
  - `caminoAscendente(id, conexiones, infoPorId)`: cadena de dependencia hacia arriba desde un dispositivo hasta la raíz (padre inmediato primero), vía la función simétrica `padreDirecto` (mismo patrón que `hijosDirectos` pero en sentido inverso); corta ante un ciclo.
  - `src/features/red/medios.ts` (nuevo): `iconoDeVia(tipoConexion, medio)` mapea el medio de una conexión a un icono (🗄 instalación, 🟣 fibra, 📶 inalámbrico, 🔌 UTP/cable, 🔗 genérico), insensible a mayúsculas.
- Solución (interfaz):
  - `TopologiaPage.tsx`: cada nodo muestra el icono de estado (reutilizando `infoDeEstado` de Dis1) y el icono del medio en el badge "via". Buscador propio (`busqueda`) que expande automáticamente las ramas ancestras de cualquier coincidencia, resalta los nodos encontrados (borde y anillo sky) y hace scroll suave al primero.
  - `RedPage.tsx`: filtro de estado (solo con los estados presentes en equipos de red) junto al de categoría ya existente; el badge de estado del listado pasó a usar `IndicadorEstado` (mismo componente de Dis1).
  - `ImpactoYDependencias.tsx` (nuevo, en `features/red`): construye el árbol con el dispositivo de la ficha como raíz y muestra "⚠ Impacto si este equipo falla" y "Depende de" (con enlaces a cada ancestro); se oculta solo si el equipo no participa de ninguna conexión. Integrado en `DispositivoPage.tsx`, que también gana "Última modificación" (mismo patrón que artículos y credenciales).
  - `IniciarDiagnosticoBoton.tsx` (nuevo, en `features/dispositivos`): enlaza a `/diagnostico?categoria=<id>`; se oculta si no hay ningún diagnóstico para esa categoría. `DiagnosticosPage.tsx` lee el parámetro `categoria` y preselecciona esa categoría con un aviso "Mostrando solo: ..." y "Ver todos" para quitar el filtro.
  - Historial resumido (punto 11): ya estaba resuelto por el componente `Historial` existente (colapsado, ordenado del más reciente al más antiguo); no requirió cambios.
- Pruebas: 331 en verde (8 nuevas: `contarImpacto` con descendientes anidados y sin hijos, `caminoAscendente` con cadena, sin dependencias y cortando un ciclo, `iconoDeVia` con sus 3 pruebas). Typecheck, lint y build en verde.
- Verificado en navegador real (sesión simulada + rack/switch/2 POS con conexiones de fibra y UTP, un diagnóstico de la categoría del POS, luego todo limpiado con `cambiosPendientes` en 0): iconos de estado (🟡🟢) y de medio (🗄🟣) correctos en el árbol; buscar "Caja 2" resaltó y expandió automáticamente la rama; la ficha del switch mostró "2 POS prueba R1" de impacto; la ficha del POS mostró "Depende de" con el switch y el botón "Iniciar diagnóstico" con el enlace correcto; `/diagnostico?categoria=...` preseleccionó la categoría con el diagnóstico visible; el filtro de estado en Red mostró solo el switch al elegir "En mantenimiento".
- Documentación: ARQUITECTURA.md sección 12.

### 50. Fase Dis1+B1 de la propuesta por módulos: Dispositivos y Bóveda
- Finalizada: 2026-07-09. Sin cambios de esquema en Supabase.
- Descripción: tercera fase de [PROPUESTA_MODULOS.md](PROPUESTA_MODULOS.md) (puntos 1, 2, 4 y 9 de Dispositivos; puntos 1, 2, 3 y 10 parcial de Bóveda). Incluye la decisión del usuario de renombrar completamente "Notas" a "Bóveda" (pestaña, rutas y textos), revirtiendo la discreción del nombre neutro decidida antes.
- Solución (Dispositivos):
  - Formulario en 5 bloques con nombre (`src/features/dispositivos/DispositivoForm.tsx`, componente `Seccion` compartido): Información general, Identificación, Ubicación, Conectividad, Información adicional. "Campos adicionales" → "Propiedades personalizadas".
  - Estados con color (`src/features/dispositivos/estados.ts`, nuevo): mapea los 4 estados sugeridos a icono/color (🟢 Operativo, 🟡 En mantenimiento, 🔴 Fuera de servicio, ⚫ De baja; comparación insensible a mayúsculas) y cualquier otro texto libre cae a un neutro gris sin romper nada. `IndicadorEstado` (nuevo) reutilizado en la ficha (`DispositivoPage.tsx`) y el listado (`DispositivosPage.tsx`).
  - Las propiedades personalizadas (`detalles`) se indexan en el buscador global (`src/features/busqueda/useIndiceBusqueda.ts`): un técnico que solo recuerda un dato propio del equipo (por ejemplo el usuario asignado) también lo encuentra.
- Solución (Bóveda, con la decisión de renombre):
  - Ruta interna `/notas` → `/boveda` en `src/App.tsx` (redirección inversa a la que existía: antes `/boveda` redirigía a `/notas`, ahora `/notas/*` redirige a `/boveda`). Pestaña, títulos y enlaces internos actualizados en `BottomNav.tsx`, `BovedaPage.tsx`, `BovedaGuard.tsx` (2 pantallas), `CredencialPage.tsx`, `CredencialForm.tsx`, `CredencialEnPaso.tsx`, `useIndiceBusqueda.ts` y `ResultadosBusqueda.tsx`. El campo de notas libres dentro de una credencial (`datos.notas`, texto sin relación con el nombre de la sección) se dejó intacto a propósito.
  - Formulario en 4 bloques (`src/features/boveda/CredencialForm.tsx`, mismo `Seccion` compartido): Información general, Credenciales (usuario, contraseña, notas), Ubicación (IP, URL), Propiedades protegidas (antes "Campos adicionales").
  - "Última modificación" (updated_at) visible en la ficha de la credencial (`CredencialPage.tsx`), mismo patrón que la ficha de artículo.
- Solución (compartida): `src/components/Seccion.tsx` (nuevo) extraído de la fase S1 (tarea 48) para no duplicar el patrón de bloques en tres formularios largos (artículos, dispositivos, credenciales); `ArticuloForm.tsx` se refactorizó para reutilizarlo.
- Pruebas: 323 en verde (sin pruebas nuevas: todo el cambio es de presentación y rutas, ya cubierto por la suite existente). Typecheck, lint y build en verde.
- Verificado en navegador real (sesión simulada + perfil con `puedeVerBoveda`, datos sembrados y luego limpiados con `cambiosPendientes` en 0): la pestaña dice "Bóveda" y apunta a `/boveda`; `/notas` redirige a `/boveda` con el título correcto; el indicador de estado con color se ve en la ficha y en el listado; la búsqueda global encontró un dispositivo por una propiedad personalizada única; los formularios de dispositivo y credencial muestran sus 5 y 4 bloques respectivamente con los nombres correctos ("Propiedades personalizadas", "Propiedades protegidas"); la ficha de una credencial muestra "Última modificación" con la fecha real.
- Documentación: ARQUITECTURA.md secciones 4, 5, 6 y 8, y el árbol de carpetas.

### 49. Fase D1 de la propuesta por módulos: pulido del editor de diagnósticos
- Finalizada: 2026-07-09. Sin cambios de esquema en Supabase.
- Descripción: segunda fase de [PROPUESTA_MODULOS.md](PROPUESTA_MODULOS.md) (puntos 1, 4, 5 y 13 del documento de Diagnóstico Inteligente). El resto de puntos del documento (respuestas dinámicas, destinos flexibles, integración con procedimientos y soluciones, "¿Quedó resuelto?", validación al guardar) ya estaban resueltos por la tarea 46; este análisis lo confirmó y quedó documentado en la propuesta.
- Solución:
  - "+ Diagnóstico" → "+ Crear diagnóstico" (`DiagnosticosPage.tsx`, botón y texto vacío).
  - `NodoDiagnostico` (`src/lib/db.ts`) gana `tituloInterno` opcional: organiza el editor y reemplaza el recorte a 40 caracteres de la pregunta en los selectores de destino y en la cabecera de la tarjeta; nunca se muestra al técnico que ejecuta el diagnóstico. Normalización y limpieza en `src/lib/diagnostico.ts` (`crearNodo`, `normalizarNodos`, `prepararNodosParaGuardar`).
  - "Termina aquí (mensaje o procedimiento final)" → "Destino: termina aquí (mensaje final o procedimiento)"; las opciones del selector ahora dicen "Destino: pregunta N (título interno o pregunta recortada)".
  - `duplicarNodo` (nuevo, puro, probado): copia el nodo con id nuevo y los ids de sus opciones también nuevos (nace sin destinos entrantes, por ser un id nuevo); sus destinos salientes se conservan porque siguen siendo la continuación lógica correcta; el título interno gana el sufijo " (copia)". Botón "⧉ Duplicar la pregunta N" en la tarjeta, inserta la copia justo después del original.
- Pruebas: 323 en verde (7 nuevas: `duplicarNodo` con y sin título interno, normalización y limpieza de `tituloInterno`, `crearNodo` sin título). Typecheck, lint y build en verde.
- Verificado en navegador real (sesión simulada + categoría sembrada, luego limpiada con `cambiosPendientes` en 0): "+ Crear diagnóstico" en la lista; título interno visible en la cabecera de la pregunta ("Pregunta 1 (inicio): Verificar alimentación") y en el selector de destino de la otra pregunta; "Destino: termina aquí" en el selector; Duplicar insertó la copia justo después con "(copia)" en el título, numeración recalculada y sin heredar "(inicio)"; guardado real de punta a punta con los títulos internos persistidos correctamente en `db.diagnosticos`.
- Documentación: ARQUITECTURA.md sección 5 (modelo de diagnósticos).

### 48. Fase S1 de la propuesta por módulos: constructor de Soluciones
- Finalizada: 2026-07-09. Sin cambios de esquema en Supabase.
- Descripción: primera fase de [PROPUESTA_MODULOS.md](PROPUESTA_MODULOS.md) (puntos 1, 2, 3, 6, 7, 8 y 10 del documento de Soluciones), aprobada por el usuario el 2026-07-09 junto con la decisión de reactivar las etiquetas.
- Solución:
  - Título dinámico por tipo: `tituloNuevo`/`tituloEditar` en `src/features/soluciones/tiposArticulo.ts` (sustantivo singular con género por tipo); la pantalla dice "Nueva instalación" o "Editar manual" en vivo según el tipo elegido.
  - Formulario reorganizado en 5 bloques con nombre y propósito (`Seccion` en `src/features/soluciones/ArticuloForm.tsx`): Información general (título, tipo, descripción, portada, etiquetas), Configuración (ruta de inicio, tiempo, dificultad), Antes de comenzar (incidencia si aplica, objetivo general, requisitos), Desarrollo (PasosEditor) y Finalización (verificación, notas, motivo).
  - Etiquetas reactivadas (decisión del usuario; retiradas el 2026-07-03): editor de chips (`EtiquetasEditor`, Enter o coma agregan, sin duplicados por mayúsculas), chips en la ficha del artículo y de vuelta al índice de búsqueda (`useIndiceBusqueda.ts`). La columna ya existía; los artículos viejos conservaban sus etiquetas intactas.
  - Vista previa (`src/features/soluciones/VistaPreviaArticulo.tsx`, nuevo, con `React.lazy` para no sumar react-markdown a la apertura del editor): overlay a pantalla completa con chip "Sin guardar", que renderiza portada, título, etiquetas, descripción, `ProcedimientoVista` interactivo y las notas Markdown con los datos EN MEMORIA del formulario. Usa el id efímero `vista-previa:<id>` y borra su progreso al cerrar (verificado).
  - Duplicar: botón en `ArticuloPage` → `/soluciones/:categoriaId/nuevo?copiarDe=<id>` (mismo patrón de dispositivos). `duplicarProcedimiento` en `src/lib/procedimiento.ts` (pura, probada): regenera los ids de pasos y bloques (el progreso local jamás se cruza), copia profunda, conserva las referencias de Storage (los archivos se comparten, no se copian) y los vínculos a otros artículos; `esRutaInicio` NO se copia. El formulario precarga todo con título "Copia de ...".
  - Plantillas (`src/features/soluciones/plantillas.ts`, nuevo, solo datos): estructura recomendada por tipo (instalación, configuración, conexión, problema frecuente y mantenimiento con requisitos/pasos/verificación de ejemplo; manual con esqueleto Markdown). Se ofrece solo en artículo nuevo con el desarrollo vacío; "Usar plantilla" precarga sin pisar lo escrito y "Empezar en blanco" la descarta por tipo.
  - Indicador de completitud (`src/lib/completitud.ts`, nuevo, puro y probado): 11 criterios de peso igual (descripción, portada, objetivo, requisitos, tiempo, dificultad, verificación final, etiquetas, pasos con tareas, alguna imagen, algún vínculo incluidos los de tarea individual); barra + porcentaje + sugerencias desplegables encima de Guardar. Nunca bloquea el guardado.
- Pruebas: 318 en verde (6 nuevas: 5 de `completitud.test.ts` y 1 de `duplicarProcedimiento`). Typecheck, lint y build en verde.
- Verificado en navegador real (sesión simulada + categoría y artículo sembrados, luego limpiados con `cambiosPendientes` en 0, incluida la fila de historial encolada por el guardado real de la copia): título dinámico al cambiar tipo; las 5 secciones; oferta de plantilla de instalación que precarga los 3 pasos, tareas, requisitos y verificación; chips de etiquetas por Enter; completitud 36 % con las 7 sugerencias correctas; vista previa interactiva (marcar una tarea creó el progreso efímero y cerrarla lo borró); duplicar precargó "Copia de ..." y al guardarla por el flujo real los ids de paso y bloque quedaron regenerados con el contenido intacto; el buscador encontró el artículo por una etiqueta única que no aparece en ningún otro texto (prueba concluyente frente a los sinónimos).
- Documentación: ARQUITECTURA.md secciones 5 y 6; PROPUESTA_MODULOS.md marcada como aprobada con las decisiones del usuario (S1 primero; etiquetas SÍ; "Bóveda" renombre completo en B1; el nombre sigue siendo Soluciones IT).

### 40. Vínculos por instrucción dentro de un paso (fase 1: credencial de la bóveda)
- Finalizada: 2026-07-09. Sin cambios de esquema en Supabase.
- Descripción: pedido por el usuario el 2026-07-07. Antes, los vínculos de un paso (credencial, subprocedimiento, solución) eran uno solo por paso completo; si un paso con varias instrucciones necesitaba mostrar datos solo en una de ellas (por ejemplo "Ingresar usuario y contraseña"), la única forma era crear un paso aparte. Alcance de esta fase, acordado con el usuario el 2026-07-09: solo el vínculo de credencial (el más pedido); IP/URL, nota técnica y subprocedimiento/solución por instrucción individual quedan para una fase posterior si se piden.
- Solución: `BloquePaso` (`src/lib/db.ts`) gana `credencialId`/`credencialTitulo`, opcionales en cualquier bloque `tarea` (independiente de su `tipoTarea`: accion, verificacion o decision pueden llevarlo). Normalización tolerante y limpieza al guardar en `src/lib/procedimiento.ts` (mismo patrón que el vínculo de decisión de la tarea 47: id + copia de referencia del título, título solo se conserva junto al id). Editor: `CredencialTareaSelector` en `src/features/soluciones/PasosEditor.tsx`, selector compacto debajo del input de la tarea (colapsado a "🔐 Datos de la bóveda: {título}" con botón Quitar cuando ya está vinculada, o un `<select>` con las credenciales disponibles cuando no). Vista: `BloqueVista` en `src/features/soluciones/ProcedimientoVista.tsx` ahora envuelve cualquier tarea (incluidas las de tipo decision) con el panel `CredencialEnPaso` reutilizado cuando `credencialId` está presente, con el mismo desbloqueo por contraseña maestra que ya protege el apartado "Datos" del paso completo; funciona igual en `AsistenteVista` porque ambas comparten `BloqueVista`.
- Pruebas: 312 en verde (6 nuevas: conserva/descarta el vínculo de credencial de una tarea, es independiente del tipoTarea, limpia el título al guardar y lo descarta si queda huérfano). Typecheck, lint y build en verde.
- Verificado en navegador real (sesión simulada, perfil con `puedeVerBoveda`, bóveda desbloqueada con verificador y credencial cifrada sembrados localmente, artículo con dos tareas en un mismo paso, solo una vinculada): la tarea sin vínculo no muestra ningún panel; la tarea con vínculo muestra "🔐 Datos: {título}" colapsado y, al abrirlo con la contraseña maestra, los datos descifrados (usuario, contraseña oculta); en el editor, vincular desde el `<select>` y quitar con el botón funcionan y persisten correctamente al guardar el artículo (verificado leyendo `db.articulos` tras el guardado real, con `cambiosPendientes` en 0). Ningún dato de prueba llegó a producción (confirmado por red: solo lecturas 401 de la sesión simulada, ninguna escritura). Datos de prueba y sesión limpiados al terminar.
- Pendiente (si el usuario lo pide más adelante): mismo patrón para IP/URL, nota técnica o subprocedimiento/solución por instrucción individual.

### 47. Mejoras de UX del editor de procedimientos (12 puntos del usuario, 2026-07-09)
- Finalizada: 2026-07-09. Tres commits, uno por fase, ya en `main` (despliega Vercel). Sin cambios de esquema en Supabase: todo lo nuevo vive en el JSON `procedimiento`.
- Descripción: segunda tanda de mejoras de UX del usuario para que el editor se sienta como un constructor guiado de procesos y no como un formulario.
- Fase A (puntos 1 a 8, renombres e iconografía) en `src/features/soluciones/PasosEditor.tsx` y `ArticuloForm.tsx`:
  - "Contenido del paso" pasó a "☑ Tareas"; "+ Aviso" a "+ ⚠ Advertencia" (el bloque nuevo nace con tono 'precaucion' en `crearBloqueAviso`, `src/lib/procedimiento.ts`); "+ Imagen" a "+ 🖼 Imagen explicativa" (el visor a pantalla completa con zoom ya existía, tarea 43); los tres botones quedaron agrupados en una barra "Agregar".
  - "Adjuntos del paso" pasó a "📎 Archivos relacionados", con botones "📷 Tomar fotografía" y "📎 Seleccionar archivos" y tipos aceptados ampliados a Word, Excel, presentaciones, OpenDocument, TXT y CSV (`TIPOS_ARCHIVO_ACEPTADOS`).
  - Cada paso sigue siempre la estructura Nombre, 🎯 Objetivo, ☑ Tareas, Agregar, 📎 Archivos relacionados, 🔐 Datos de la bóveda, 🔗 Procedimiento relacionado, 🛠 Solución relacionada, con etiquetas visibles y divisores entre grupos. Íconos consistentes también en el formulario (🎯 Objetivo general, ✅ Verificación final, 📝 Notas adicionales, 📖 Motivo del cambio), en las vistas de lectura y en `CredencialEnPaso` (🔐).
  - El botón "Guardar" ahora dice "Guardar procedimiento" (o "Guardar artículo" si no hay pasos).
- Fase B (puntos 9 y 10, descripción y portada):
  - `Procedimiento` ganó `descripcion` (¿cuándo usar este procedimiento?, distinta del objetivo general) y `portada` (formato `PasoAdjunto`) en `src/lib/db.ts`, con normalización y limpieza en `src/lib/procedimiento.ts` y campos nuevos en el formulario (`PortadaEditor` sube comprimido y con cola offline, referencia `articulos/<id>/portada/...`).
  - La portada se muestra en la página del artículo (banner), el listado de la categoría, los resultados del buscador (campo `portadaRef` en el índice MiniSearch, `src/features/busqueda/useIndiceBusqueda.ts`), las rutas "Para empezar" de Inicio y las recomendaciones de artículos similares, todas vía `src/components/MiniaturaPortada.tsx` (nuevo). La descripción se muestra bajo el título y entra al índice de búsqueda (`textoDeProcedimiento`).
  - El historial resume los cambios de descripción y portada (`src/features/historial/resumenProcedimiento.ts`). De paso se cerró un hueco: "Descargar todo para offline" no incluía las imágenes intercaladas en bloques; ahora incluye bloques y portadas (`src/lib/adjuntosOffline.ts`).
- Fase C (puntos 11 y 12, tipos de tarea y Decisión):
  - `BloquePaso` ganó `tipoTarea` ('accion' por defecto y para todo lo guardado antes, 'verificacion' o 'decision') y el vínculo `decisionArticuloId`/`decisionArticuloTitulo` (solo decisiones, mismo patrón de copia de referencia). Normalización tolerante y limpieza al guardar, con pruebas.
  - Editor: selector compacto de tipo por fila (☐ Acción, ☑ Verificación, ❓ Decisión) con placeholder de ejemplo por tipo, y `DecisionVinculoSelector` para elegir qué abrir "si responde No".
  - Ejecución: las verificaciones se ven con casilla azul; una decisión es una pregunta de Sí/No dentro del checklist ("Sí" marca y continúa; "No" despliega en línea el vinculado y al completarlo la decisión queda marcada, el progreso del vinculado se reinicia y el técnico regresa al punto exacto). `DecisionEnTarea` en `src/features/soluciones/ProcedimientoVista.tsx` sirve a ambas vistas mediante el render prop `ejecutarInline` de `BloqueVista` (la lista anida `ProcedimientoVista`, el asistente anida `AsistenteVista`), sin ciclos de import y con la regla de un solo nivel de anidamiento. Como el Diagnóstico Inteligente ejecuta procedimientos con el asistente, las decisiones funcionan igual desde un diagnóstico (punto 12).
- Pruebas: 309 en verde (8 nuevas sobre descripción/portada y tipos de tarea; fixtures actualizados). Lint, typecheck y build en verde en cada fase.
- Verificado en navegador real (sesión simulada + datos sembrados, luego limpiados con `cambiosPendientes` en 0): portada y descripción en artículo, listado, buscador (encontró por el texto de la descripción) y "Para empezar"; editor con todas las etiquetas, barra Agregar, selector de tipos y vínculo de decisión; botón "Guardar procedimiento"/"Guardar artículo" según corresponda; ciclo completo de la Decisión en la vista de lista Y en el modo asistente (No → vinculado en línea → completarlo marca la decisión, reinicia el progreso del vinculado y el paso se completa solo al quedar todas las tareas). La captura de pantalla falló por el problema conocido del entorno; la verificación fue por DOM y Dexie.
- Documentación: ARQUITECTURA.md secciones 5 y 6 actualizadas.

### 46. Fase F de la propuesta de UX: Modo Diagnóstico Inteligente
- Finalizada (código): 2026-07-08. Pendiente de un paso del usuario en Supabase (aplicar schema.sql).
- Descripción: última fase grande de [PROPUESTA_UX_DIAGNOSTICO.md](PROPUESTA_UX_DIAGNOSTICO.md) (cubre F1 y F2 de la propuesta). Convierte IT Brain en un asistente de soporte guiado: el técnico parte del problema ("La impresora no imprime"), responde preguntas simples y llega a la solución, ejecutando los procedimientos existentes como bloques reutilizables (nunca duplicados). Registra cada ejecución para las estadísticas futuras. Apoyado en el modo asistente de la fase D.
- Solución (modelo y sincronización):
  - `src/lib/db.ts`: tipos `OpcionDiagnostico` (etiqueta + continuar en otro nodo, ejecutar un artículo por copia de referencia id/título, o terminar con mensaje final), `NodoDiagnostico` (pregunta, descripción, opciones; Sí/No es una lista de 2 y los tipos futuros de respuesta no rompen el modelo), `Diagnostico` (categoriaId, titulo, descripcion, nodos JSON; el primer nodo es el inicial), `PasoCamino`, `EstadoDiagnostico` (unión discriminada pregunta/articulo/final), `ProgresoDiagnostico` (sesión local, no sincronizada) y `EjecucionDiagnostico` (registro inmutable sincronizado). `TipoEntidadHistorial` gana 'diagnostico'. Dexie versión 8: tablas `diagnosticos`, `ejecuciones_diagnostico` (nombre con guion bajo a propósito: el motor de sync usa el mismo nombre local y remoto) y `progresoDiagnostico`.
  - `src/lib/tablas.ts`: mapeos de ambas tablas; `ejecuciones_diagnostico` es soloInsercion con cursor `recibido_en` (mismo patrón que historial) y queda excluida de `TablaEditable`. Van al final de `TABLAS_SINCRONIZADAS` para que su esquema sin aplicar no frene la descarga del resto.
  - `src/lib/sync.ts`: `descargarBovedaMeta()` se movió ANTES del bucle de tablas: con las tablas nuevas al final de la lista, un fallo por esquema sin aplicar ya no dejaba sin descargar el verificador de la bóveda.
  - `src/lib/repositorio.ts`: `TIPO_POR_TABLA` mapea diagnosticos → 'diagnostico' (historial automático y eliminación sensible con contraseña maestra gratis, vía `guardarRegistro`/`eliminarRegistro`); `formatearValor` resume el campo `nodos` como "N preguntas" para un historial legible; `registrarEjecucionDiagnostico` (nuevo) agrega la ejecución localmente y la encola, sin tocar el historial.
  - `supabase/schema.sql`: tablas `diagnosticos` (con trigger de updated_at y RLS de acceso general) y `ejecuciones_diagnostico` (RLS de solo lectura + inserción, sin políticas de UPDATE/DELETE; sin FK a diagnosticos a propósito, mismo criterio que historial.entidad_id); índices; el check de `historial.entidad_tipo` ahora admite 'diagnostico'.
- Solución (lógica pura, probada):
  - `src/lib/diagnostico.ts`: `crearNodo` (arranca con Sí/No prefilladas), `normalizarNodos` (tolerante a datos corruptos, descarta títulos de referencia sin id y mensajes finales de ramas que continúan), `prepararNodosParaGuardar`, `validarNodos` (preguntas sin texto o sin respuestas, respuestas sin salida, destinos rotos, auto referencias, ciclos por DFS y preguntas inalcanzables desde la primera), `profundidadRestante` (camino más largo, corta ciclos), `porcentajeDiagnostico` (barra de progreso: respondidas contra lo que podría quedar, 100 solo en el final) y `textoDeNodos` (índice de búsqueda).
  - `src/lib/progresoDiagnostico.ts`: transiciones de la sesión (iniciar, `responderOpcion`, `terminarEjecucionArticulo`, `volverAtras`, eliminar, `duracionSegundos`). Al entrar a ejecutar un artículo su avance local se REINICIA (un avance viejo haría que el asistente lo diera por completado y se lo saltara) y al terminarlo se reinicia de nuevo (queda listo para el próximo uso, como las soluciones de error).
- Solución (interfaz):
  - `src/features/diagnostico/DiagnosticosPage.tsx` (`/diagnostico`): problemas agrupados por categoría, filtro local, botón "+ Diagnóstico" y badge "En curso" para sesiones a medias.
  - `src/features/diagnostico/DiagnosticoForm.tsx` (`/diagnostico/nuevo`, `/:id/editar`): editor sin grafos, lista plana de preguntas numeradas (la 1 es el inicio) con respuestas que eligen destino y procedimiento por selects; al eliminar una pregunta, las respuestas que apuntaban a ella quedan sin destino (la validación pide uno nuevo); errores de validación en lenguaje claro que bloquean el guardado; eliminar con `DialogoEliminar` sensible; historial al pie.
  - `src/features/diagnostico/DiagnosticoRunPage.tsx` (`/diagnostico/:id`, fuera del Layout): intro con "Comenzar"; una pregunta a la vez con barra de progreso, "Volver" y "Cancelar" (con confirmación inline, registra 'abandonado'); `ArticuloEnDiagnostico` ejecuta el procedimiento vinculado con `AsistenteVista` y detecta su completado reactivamente (pasos + verificación final) con un guard de un solo disparo, regresando solo al punto exacto; pantalla de resultado con mensaje, camino recorrido, procedimientos ejecutados y "¿Quedó resuelto?" Sí/No que cierra el registro; casos borde cubiertos (diagnóstico editado a mitad de sesión, artículo vinculado eliminado, diagnóstico sin preguntas).
  - Búsqueda global: los diagnósticos entran al índice (título, preguntas, respuestas) con grupo propio "Diagnósticos"; Inicio gana el acceso destacado "Diagnóstico Inteligente". Rutas y chunks propios en `src/App.tsx`.
- Corrección encontrada al probar: la pantalla de inicio del diagnóstico no aparecía nunca porque `useLiveQuery` devuelve `undefined` tanto para "cargando" como para "no hay fila"; se resolvió con el patrón `?? null` que ya usa el resto de la app.
- Pruebas: 301 en verde (30 nuevas: `diagnostico.test.ts` con 16 sobre normalización, validación de árboles con ciclos y nodos inalcanzables, profundidad y porcentaje; `progresoDiagnostico.test.ts` con 10 sobre el flujo completo de la sesión incluidos los reinicios del avance del artículo; 2 de mapeo en `sync.test.ts`). Typecheck, lint y build en verde.
- Verificado en navegador real (sesión simulada + categoría, artículo con procedimiento y el diagnóstico de la impresora de los ejemplos del usuario sembrados): búsqueda global encuentra el diagnóstico con su grupo propio; flujo corto (No → mensaje final → "Sí, resuelto") registra la ejecución con camino, usuario y duración y limpia la sesión; flujo largo (Sí → No ejecuta "Conectar impresora a computadora" dentro del asistente → completar su tarea regresa SOLO a la pregunta 3 con el artículo anotado como ejecutado) funciona de punta a punta; "Volver" deshace la última respuesta incluso desde una ejecución; el resultado muestra camino y procedimientos ejecutados; "Cancelar" con confirmación registra 'abandonado'; quedaron los tres desenlaces (si/no/abandonado) en `ejecuciones_diagnostico`; el editor carga el árbol y la validación bloquea el guardado con el mensaje claro al vaciar un mensaje final. Datos de prueba, sesión y cola limpiados (`cambiosPendientes` en 0).
- Pendiente (usuario): aplicar el `supabase/schema.sql` actualizado en Supabase (agrega `diagnosticos`, `ejecuciones_diagnostico` y amplía el check de `historial.entidad_tipo`). Hasta entonces los diagnósticos funcionan en cada dispositivo pero no se comparten (la sincronización de esas tablas falla sin frenar el resto).
- Posterior (F3, decidir más adelante): tablero de estadísticas sobre las ejecuciones y orden de soluciones por tasa de éxito real.

### 45. Fase E de la propuesta de UX: sinónimos en la búsqueda y sugerencias anti duplicados
- Finalizada: 2026-07-08.
- Descripción: quinta fase de [PROPUESTA_UX_DIAGNOSTICO.md](PROPUESTA_UX_DIAGNOSTICO.md), puntos 12 y 13 del usuario. La búsqueda ya toleraba errores de escritura y prefijos (MiniSearch); lo que faltaba eran los sinónimos ("backup" no encontraba "Crear copia de seguridad") y avisar proactivamente antes de duplicar contenido.
- Solución:
  - `src/features/busqueda/sinonimos.ts` (nuevo): diccionario curado de grupos de sinónimos del dominio del equipo (backup/respaldo/copia de seguridad, internet/red/wifi/ip, impresora/impresión/imprimir, contraseña/clave/password, computador/pc/equipo, cámara/cctv, pos/datáfono/punto de venta, etc.) y `expandirConsulta`, que agrega a la consulta las palabras de los sinónimos de cada término escrito (insensible a mayúsculas y acentos vía NFD + `\p{M}`; las entradas de varias palabras solo aportan expansión, sin palabras vacías como "de"). MiniSearch combina con OR, así que la expansión solo AGREGA resultados.
  - `src/features/busqueda/useIndiceBusqueda.ts`: `buscar` expande la consulta con sinónimos; `buscarArticulosSimilares` (nuevo) devuelve artículos de título parecido exigiendo que la coincidencia esté en el campo `titulo` (vía el `match` de MiniSearch), no solo en el contenido: es lo que define que dos artículos traten de lo mismo.
  - `src/features/soluciones/ArticuloForm.tsx`: al escribir el título de un artículo NUEVO (con rebote de 300 ms), si existen artículos de título parecido se muestra un aviso "Ya existen artículos parecidos" con enlaces "Abrir". Solo al crear, nunca al editar.
  - `src/features/soluciones/PasosEditor.tsx`: al escribir el título de un paso, si existe un procedimiento vinculable de título parecido aparece la sugerencia "Ya existe el procedimiento ..." con botón "Vincular" (usa el mismo handler que el selector manual) y "✕" para descartarla por paso. Índice MiniSearch propio construido solo sobre los títulos de los vinculables.
- Pruebas: 271 en verde (11 nuevas: `sinonimos.test.ts` con 6 casos de expansión, y en `busqueda.test.ts` 2 de sinónimos de punta a punta más 3 de `buscarArticulosSimilares`, incluyendo que ignora coincidencias que solo están en el contenido y que excluye el propio artículo). Typecheck, lint y build en verde.
- Verificado en navegador real (sesión simulada + 2 artículos sembrados): buscar "backup" encuentra "Crear copia de seguridad del SQL Server" y buscar "internet" encuentra "Diagnóstico de red" (los dos ejemplos literales del documento del usuario); en el formulario de artículo nuevo, escribir "Copia de seguridad" muestra el aviso de parecidos; en un paso nuevo, escribir "Crear copia de seguridad" sugiere el procedimiento existente y el botón "Vincular" lo vincula (la sugerencia desaparece). Datos de prueba y sesión limpiados (`cambiosPendientes` en 0).

### 44. Fase D de la propuesta de UX: modo asistente de ejecución
- Finalizada: 2026-07-08.
- Descripción: cuarta fase de [PROPUESTA_UX_DIAGNOSTICO.md](PROPUESTA_UX_DIAGNOSTICO.md), puntos 14 y 18 del usuario. En vez del "mapa" completo del procedimiento (todos los pasos como lista expandible, `ProcedimientoVista.tsx`), esta pantalla nueva muestra un paso a la vez: título, objetivo, checklist (tareas, avisos, imágenes), barra de progreso y botón "Siguiente", para que el técnico no se distraiga con el resto del procedimiento mientras lo ejecuta en campo. Es además un prerrequisito deliberado del futuro Modo Diagnóstico Inteligente (fase F), que ejecutará procedimientos "en modo asistente".
- Solución:
  - `src/features/soluciones/useProcedimientoEjecucion.ts` (nuevo): toda la lógica de ejecución que antes vivía dentro de `ProcedimientoVista.tsx` (marcar paso/tarea, avanzar al siguiente pendiente, subprocedimiento y solución vinculados, verificación final) se extrajo a un hook compartido, parametrizado con un callback `onAvanzar(destino: number | null)` en vez de manejar directamente el estado de la interfaz: quien lo usa decide qué significa "avanzar" (la lista expande el siguiente paso y hace scroll; el asistente cambia de pantalla). Así ambos modos comparten exactamente la misma lógica de completado, sin duplicarla, y no hay riesgo de que diverjan con el tiempo.
  - `src/features/soluciones/ProcedimientoVista.tsx`: refactorizado para consumir el hook (sin cambios de comportamiento); se exportaron `BloqueVista` y `AdjuntosPaso` para que el asistente reutilice el mismo render de tareas/avisos/imágenes y galería, en vez de duplicarlo.
  - `src/features/soluciones/AsistenteVista.tsx` (nuevo): vista recursiva del modo asistente. Resuelve la posición inicial con una lectura directa (no en vivo) del avance guardado al montar, para reanudar en el primer paso pendiente en vez de saltar al azar por una lectura a medio cargar; de ahí en adelante el avance lo decide únicamente `onAvanzar`. Un subprocedimiento o solución vinculados se ejecutan anidando OTRO `AsistenteVista` (recursivo, mismo patrón de un solo nivel de anidamiento que `ProcedimientoVista`: más profundo solo se enlaza) dentro de un recuadro en la misma pantalla, en vez de abrir una pantalla aparte; al completarse, el padre deja de renderizarlo solo (React) y el flujo principal sigue. Pantallas propias para "Verificación final" (cuando los pasos ya están completos pero falta el checklist final) y "Procedimiento completado" (con botón para reiniciar).
  - `src/features/soluciones/AsistentePage.tsx` (nuevo): pantalla de ruta, fuera del `Layout` (sin barra inferior, igual que el escáner) para minimizar distracciones; header solo con "Salir" (vuelve a la ficha sin perder avance, que vive en Dexie) y el título del artículo.
  - `src/App.tsx`: ruta nueva `soluciones/:categoriaId/:articuloId/ejecutar`, cargada en su propio chunk.
  - `src/features/soluciones/ArticuloPage.tsx`: botón primario "▶ Modo asistente", visible solo en artículos con procedimiento.
- Pruebas: 260 en verde (sin nuevas: es interacción de flujo en React, el proyecto no prueba componentes; la lógica reutilizada ya estaba cubierta donde tenía pruebas). Typecheck (`tsc -b`), lint (oxlint) y build (`vite build`) en verde; el refactor de `ProcedimientoVista.tsx` incluso mejoró el code-splitting (bajó de ~177 kB a ~158 kB al separarse en un chunk compartido con el asistente).
- Verificado en navegador real (sesión simulada + procedimiento principal con un subprocedimiento y una solución vinculados, sembrados en Dexie): (1) paso 1 sin vínculos: marcar su única tarea avanza solo al paso 2; (2) paso 2 con subprocedimiento: se ve la tarea propia del paso Y el asistente anidado del subprocedimiento en la misma pantalla; al completar ambas, el subprocedimiento avisa al padre, que se completa y avanza solo al paso 3; (3) paso 3 con solución: responder "No, continuar" avanza directo a la pantalla de "Verificación final"; en una segunda pasada, responder "Sí, ver la solución" anida el asistente de la solución, y al completarla se resuelve el error (su progreso se reinicia, verificado en Dexie), el paso se completa y el flujo sigue; (4) marcada la verificación final, aparece la pantalla "Procedimiento completado"; (5) reanudación: con el paso 1 ya completado en Dexie, recargar el asistente arranca directo en el paso 2 (no en el 1); (6) "Salir" enlaza de vuelta a la ficha del artículo. Datos de prueba y sesión simulada limpiados al terminar (`cambiosPendientes` en 0).
- Nota de método (documentada en memoria): verificar esto con eventos sintéticos reveló dos trampas del entorno de prueba (no del código): `preview_click` puede fallar en silencio si el viewport queda con `innerHeight: 0` tras varias navegaciones (usar `element.click()` directo por DOM o `preview_resize`), y el estado de eventos "continuos" de React puede tardar un macrotask extra en reflejarse (separar la acción de la verificación en llamadas distintas). Ver `gotchas-verificacion-preview-tools.md`.
- Nota de alcance: sin cambios de esquema en Supabase (el progreso ya vivía en `progresoPasos`, sin cambios de forma). No se agregó una opción para "saltar" el checklist en modo asistente (a propósito, para no aflojar la validación de contenido); un paso sin tareas propias habilita el botón "Siguiente" de inmediato.

### 43. Fase C1 de la propuesta de UX: visor de imágenes con zoom y pantalla completa
- Finalizada: 2026-07-08.
- Descripción: tercera fase de [PROPUESTA_UX_DIAGNOSTICO.md](PROPUESTA_UX_DIAGNOSTICO.md), punto 6 del usuario (parte de zoom y pantalla completa; la anotación con flechas/círculos/texto queda como C2, pendiente de decidir si se hace). Antes, tocar una imagen la abría en una pestaña nueva del navegador (`target="_blank"`); ahora se abre en un visor propio dentro de la misma pantalla.
- Solución:
  - `src/components/VisorImagen.tsx` (nuevo): overlay a pantalla completa sobre fondo oscuro. Pellizco con dos dedos para acercar/alejar (clamp entre 1x y 4x), doble toque para alternar entre 1x y 2.5x, arrastre con un dedo para desplazarse cuando está ampliada. Se cierra con Escape, tocando el fondo (fuera de la imagen) o el botón ✕. Implementado con Pointer Events puros (sin librería nueva): un `Map` de punteros activos por id para distinguir un dedo de dos, distancia entre los dos puntos para el factor de zoom del pellizco, y una ventana de 300 ms entre `pointerdown` para detectar el doble toque.
  - `src/features/soluciones/ProcedimientoVista.tsx` (`AdjuntoPaso`) y `src/components/Adjuntos.tsx` (`AdjuntoItem`): la imagen ahora es un `<button>` que abre `VisorImagen` en vez de un `<a target="_blank">`; los archivos que no son imagen (PDF, manuales) siguen abriendo en pestaña nueva. Como `BloqueVista` (imágenes intercaladas de un paso, fase B) reutiliza `AdjuntoPaso`, el visor quedó conectado ahí también sin tocar ese componente.
- Corrección encontrada al probar con eventos sintéticos: `setPointerCapture` puede lanzar una excepción (`NotFoundError`) si el puntero ya no está activo; sin manejarla, esa excepción abortaba todo el resto del handler (zoom y arrastre) en silencio. Se envolvió en `try/catch` para que el gesto siga funcionando aunque la captura falle.
- Pruebas: 260 en verde (sin nuevas: es interacción de gestos en el navegador, el proyecto no prueba componentes React). Lint (oxlint) y build (`tsc -b` + `vite build`) en verde.
- Verificado en navegador real: montado el componente de forma aislada (con una imagen de prueba) para probar los gestos con eventos `PointerEvent` sintéticos, confirmando por estilo computado (`transform`) cada uno por separado: doble toque amplía a 2.5x y alterna de vuelta a 1x; pellizco con dos punteros que duplican su separación duplica la escala; arrastre con un dedo desplaza la imagen ampliada; toque en el fondo y tecla Escape cierran el visor. Nota de método: los eventos sintéticos disparados en llamadas de herramienta separadas quedan minutos aparte en reloj real (el navegador limita `setTimeout` en segundo plano a ~1s), así que las dos partes de un gesto deben dispararse en la misma evaluación; además el estado de un evento "continuo" (`pointermove`) tarda un macrotask extra en reflejarse en el DOM por la prioridad de React, así que la lectura debe hacerse en una llamada posterior, no en la misma. Además, verificado el flujo real de la app: artículo con un adjunto de imagen sembrado y cacheado offline (`guardarEnCacheOffline`), abrir el paso, tocar la imagen abre el visor con la URL real del adjunto, cerrar con Escape funciona. Datos de prueba y sesión simulada limpiados al terminar (`cambiosPendientes` en 0).
- Nota de alcance: sin cambios de modelo ni de Supabase. La anotación de imágenes (C2: flechas, círculos, resaltados, texto) queda pendiente como tarea aparte si el usuario decide hacerla tras ver el uso real de este visor.

### 42. Fase B de la propuesta de UX: bloques tipados dentro del paso
- Finalizada: 2026-07-08.
- Descripción: segunda fase de [PROPUESTA_UX_DIAGNOSTICO.md](PROPUESTA_UX_DIAGNOSTICO.md). Cambio de modelo central: el cuerpo de un paso deja de ser `instrucciones: string[]` (texto plano) y pasa a `bloques: BloquePaso[]`, una lista ordenada y heterogénea de tareas con casilla, avisos con tono e imágenes intercaladas. Resuelve de una vez tres pedidos del usuario (punto 4: tipos de contenido con icono y color; punto 5: la advertencia justo ANTES de la acción peligrosa, no al final; punto 7: imágenes entre tareas concretas) y absorbe el cambio de modelo que necesitaba la tarea 40 (que quedó reducida a agregar campos opcionales a `BloquePaso`). Todo vive en el JSON de `articulos.procedimiento`: sin cambios en `supabase/schema.sql`.
- Solución:
  - `src/lib/db.ts`: tipos nuevos `TonoAviso` (`info | precaucion | importante | consejo | dato`), `TipoBloque` (`tarea | aviso | imagen`) y `BloquePaso` (`{ id, tipo, texto, tono, adjunto }`). `PasoProcedimiento.instrucciones` se reemplaza por `bloques: BloquePaso[]`; la galería `adjuntos` del paso se conserva aparte (para manuales/PDF y adjuntos del paso completo). El progreso local (`ProgresoPasos.instruccionesHechas`) ahora guarda ids de bloques `tarea` (antes claves `pasoId:indice`); las filas viejas dejan de coincidir y el avance a medias se reinicia una sola vez (dato local y efímero, mismo criterio ya documentado).
  - `src/lib/procedimiento.ts`: `crearBloqueTarea/Aviso/Imagen`, `tareasDe(bloques)`; `normalizarBloques` migra las viejas `instrucciones` a bloques `tarea` con id nuevo (mismo patrón que `imagen`→`adjuntos`) y valida los bloques nuevos por tipo (descarta tareas/avisos vacíos e imágenes sin adjunto; tono inválido cae a `info`); `normalizarUnAdjunto` extraído y reutilizado para la galería y el adjunto de los bloques imagen; `textoDeProcedimiento` indexa los textos de los bloques; `prepararProcedimientoParaGuardar` limpia los bloques (`limpiarBloques`).
  - `src/lib/progresoPasos.ts`: `establecerPasoHecho`, `alternarInstruccionHecha` y `contarInstruccionesHechas` pasan a trabajar con ids de tarea en vez de índices; se eliminaron `claveInstruccion`/`clavesDeInstrucciones`. Los tres escritores del progreso preservan ahora `verificacionHecha` (corrige un hueco de la fase A por el que marcar/desmarcar un paso podía borrar la verificación final).
  - `src/features/soluciones/tonos.ts` (nuevo): metadatos visuales de cada tono (etiqueta, icono, clases de color), compartidos por la vista y el editor.
  - `src/features/soluciones/ProcedimientoVista.tsx`: `BloqueVista` renderiza cada bloque según su tipo (tarea con casilla, aviso con su color e icono, imagen con pie); los handlers de completado usan `tareasDe`.
  - `src/features/soluciones/PasosEditor.tsx`: `ContenidoEditor` reemplaza la textarea de instrucciones por una lista de filas de bloques (`FilaBloque`) con reordenar/eliminar y botones "+ Tarea", "+ Aviso", "+ Imagen". El caso común sigue rápido: Enter en una tarea crea otra debajo y la enfoca, y pegar varias líneas las reparte en tareas seguidas. La subida de archivos se refactorizó (`subirArchivos`) y se reutiliza para la galería y para las imágenes intercaladas.
  - `src/features/historial/resumenProcedimiento.ts`: el resumen en lenguaje natural diffea tareas y avisos por texto (reutilizando `diffLista`) y cuenta las imágenes intercaladas junto a las de la galería.
- Pruebas: 260 en verde. Reescritos `progresoPasos.test.ts` (ids de tarea; tests nuevos de verificación final) y adaptados `procedimiento.test.ts` (migración de instrucciones→bloques, validación de bloques, `tareasDe`) y `resumenProcedimiento.test.ts` (helper `tareas`/`aviso`, diff de avisos e imágenes inline). Lint (oxlint) y build (`tsc -b` + `vite build`) en verde.
- Verificado en navegador real (sesión simulada + dos artículos sembrados en Dexie): (1) migración: un artículo con `instrucciones` viejas se lee y muestra como tareas; (2) render: un artículo con bloques mixtos muestra tareas y avisos en orden, con la precaución (ámbar) justo antes de la tarea de borrado y el aviso importante (rojo) después, colores confirmados por `getComputedStyle`; (3) completado: marcar las 2 tareas (de a una) completa el paso, los avisos no cuentan; (4) editor: carga los bloques con sus tonos, Enter agrega una tarea y enfoca, pegar 3 líneas crea 3 tareas; (5) guardado end-to-end: los bloques persisten en Dexie con tipo/texto/tono/orden correctos. Datos de prueba, historial y cola limpiados al terminar (`cambiosPendientes` en 0).
- Nota de alcance: no requiere pasos del usuario en Supabase (el modelo vive en el JSON de `procedimiento`). Como con cambios de modelo anteriores, conviene que el equipo actualice la app antes de editar procedimientos (una app vieja no entiende el formato `bloques`); el aviso de versión nueva ya existe.

### 41. Fase A de la propuesta de UX: metadatos del procedimiento
- Finalizada: 2026-07-08.
- Descripción: primera fase de [PROPUESTA_UX_DIAGNOSTICO.md](PROPUESTA_UX_DIAGNOSTICO.md) (aprobada el mismo día). Agrega los metadatos opcionales del procedimiento pedidos por el usuario: objetivo por paso, objetivo general, verificación final, tiempo estimado, dificultad, requisitos vuelven a editarse, chip de avance en la lista de la categoría y "última modificación" en la cabecera del artículo. Todo vive en el JSON de `articulos.procedimiento`: sin cambios en `supabase/schema.sql`.
- Solución:
  - `src/lib/db.ts`: `PasoProcedimiento.objetivo: string` (1 línea, opcional); `Procedimiento` gana `objetivoGeneral: string`, `verificacionFinal: string[]`, `tiempoEstimadoMin: number | null`, `dificultad: NivelDificultad | null` (tipo nuevo `'principiante' | 'intermedio' | 'avanzado'`); `ProgresoPasos.verificacionHecha?: number[]` (progreso local, no sincronizado, mismo patrón que `instruccionesHechas`).
  - `src/lib/procedimiento.ts`: `crearPaso` y `normalizarPaso` incluyen `objetivo`; `normalizarProcedimiento` valida y completa los campos nuevos (`numeroPositivo` y `dificultadValida` descartan valores fuera de rango o inválidos); `textoDeProcedimiento` indexa objetivo general, objetivo por paso y verificación final; `prepararProcedimientoParaGuardar` cambió de posicional a un objeto `DatosProcedimientoParaGuardar` (más campos ya no cabían legibles como parámetros sueltos).
  - `src/lib/progresoPasos.ts`: `alternarVerificacionFinal` y `verificacionFinalCompleta` (por índice, igual que las instrucciones: la verificación no tiene id propio).
  - `src/features/soluciones/ProcedimientoVista.tsx`: objetivo general y chips de tiempo/dificultad antes de "Antes de empezar"; objetivo del paso al expandirlo; bloque "Verificación final" (checklist propio) que aparece cuando todos los pasos están hechos pero antes de dar el procedimiento por completado: el banner verde ahora exige también esa verificación, no solo los pasos.
  - `src/features/soluciones/PasosEditor.tsx`: campo de una línea para el objetivo del paso, bajo el título.
  - `src/features/soluciones/ArticuloForm.tsx`: objetivo general, tiempo estimado, dificultad y requisitos (reactivados, el 2026-07-03 solo se había retirado el editor) antes del editor de pasos; verificación final después.
  - `src/features/soluciones/ArticuloPage.tsx`: línea "Última modificación: fecha, por usuario" bajo el tipo del artículo (resuelve el nombre con `db.perfiles.get(updatedBy)`).
  - `src/features/soluciones/CategoriaPage.tsx`: chip "X/Y" en cada artículo con procedimiento y avance previo (`AvanceArticulo`), para retomar de un vistazo sin abrir el artículo.
  - `src/features/historial/resumenProcedimiento.ts`: el resumen en lenguaje natural ahora también describe cambios en objetivo (general y por paso), tiempo estimado, dificultad y verificación final, con el mismo patrón de diff que ya existía para requisitos e instrucciones.
- Pruebas: 252 en verde. Se actualizaron `procedimiento.test.ts` (helper `procedimientoCompleto`, helper `preparar` para el nuevo objeto de `prepararProcedimientoParaGuardar`, casos de `tiempoEstimadoMin`/`dificultad` inválidos) y `resumenProcedimiento.test.ts` (helpers `paso`/`proc` con los campos nuevos, casos de objetivo por paso, objetivo general, tiempo, dificultad y verificación final). Lint (oxlint) y build (`tsc -b` + `vite build`) en verde.
- Verificado en navegador real (sesión simulada + categoría y artículo con procedimiento completo sembrados en Dexie): objetivo general, chips de tiempo/dificultad y requisitos se ven en la ficha; el objetivo de cada paso aparece al expandirlo; al completar los dos pasos aparece el checklist de "Verificación final" (el banner de completado NO aparece hasta marcar ambas casillas, y sí aparece al marcarlas); el chip "2/2" en verde se ve en la lista de la categoría; el formulario de edición carga y permite modificar todos los campos nuevos. Datos de prueba y sesión simulada limpiados al terminar (`cambiosPendientes` en 0).

### 38. Enriquecer "Problemas frecuentes" (base de incidencias)
- Finalizada (código): 2026-07-07. Pendiente de un paso del usuario en Supabase (aplicar schema.sql).
- Descripción: registrada tras el análisis de nuevas funcionalidades del 2026-07-07 (idea #1). El tipo de artículo `problema_frecuente` ya existía (con su procedimiento como solución), pero síntomas, causas y equipos afectados iban sueltos en el Markdown, sin estructura ni vínculo real al inventario. Sienta la base para la tarea 39 (dependencias por equipo).
- Solución:
  - `src/lib/db.ts`: nueva interfaz `DispositivoAfectado { id, nombre }` (mismo patrón de referencia que `credencialId`/`credencialTitulo` en los pasos) y tres campos nuevos en `Articulo`: `sintomas: string[]`, `causas: string[]`, `dispositivosAfectados: DispositivoAfectado[]`. Solo tienen sentido con tipo `problema_frecuente`, pero cualquier artículo puede tenerlos vacíos sin romper nada.
  - `src/lib/tablas.ts` y `supabase/schema.sql`: mapeo remoto (`sintomas`/`causas` como `text[]`, `dispositivos_afectados` como `jsonb`) y migración para las bases ya desplegadas.
  - `src/features/soluciones/ArticuloForm.tsx`: bloque nuevo, visible solo con tipo "Problemas frecuentes", con dos textareas (síntomas y causas, una línea por elemento) y `DispositivosAfectadosEditor` (componente nuevo en el mismo archivo): chips de los dispositivos ya vinculados con botón "Quitar" y un select para agregar más, excluyendo los ya elegidos y los dispositivos eliminados.
  - `src/features/soluciones/ArticuloPage.tsx`: `IncidenciaResumen` (componente nuevo) muestra "Síntomas", "Posibles causas" como listas y "Dispositivos afectados" como chips enlazados a `/dispositivos/:id`; no se muestra nada si los tres están vacíos. La solución sigue siendo `ProcedimientoVista`, sin duplicar contenido.
  - `src/features/busqueda/useIndiceBusqueda.ts`: síntomas, causas y los nombres de los dispositivos afectados entran al texto indexado del artículo (buscar "tóner" encuentra la incidencia aunque la palabra solo esté en "Posibles causas").
  - **Corrección de un bug real encontrado en el camino**: `valorComparable` en `src/lib/repositorio.ts` (usada para decidir si un campo cambió al construir el historial) comparaba arrays con `Array.prototype.join`, que convierte cualquier objeto en el texto fijo `"[object Object]"` sin importar su contenido. Para un campo de objetos como `dispositivosAfectados`, cambiar de un dispositivo vinculado a otro sin cambiar la cantidad de vínculos no generaba ninguna entrada de historial (dos listas de un elemento cada una siempre "parecían iguales"). Corregido para que los arrays de objetos se comparen por su `JSON.stringify` elemento a elemento. Se agregó además `formatearValor` especial para este campo: el historial guarda los nombres legibles de los dispositivos, no el JSON con sus id.
  - `src/features/historial/textoHistorial.ts`: etiquetas "Síntomas", "Posibles causas" y "Dispositivos afectados" para el historial de cambios.
- Pruebas: 247 en verde (2 nuevas en `repositorio.test.ts`: detecta el cambio de `dispositivosAfectados` aunque la cantidad de vínculos no cambie, regresión directa del bug encontrado; y que no registra nada si la lista no cambió), lint y build en verde. Se actualizaron las fábricas de artículos de prueba (`recientes.test.ts`, `sync.test.ts`) para incluir los campos nuevos.
- Verificado en navegador real (sesión simulada + categoría y dispositivo sembrados en Dexie): al elegir tipo "Problemas frecuentes" en el formulario aparecen los tres campos nuevos; se completaron síntomas, causas y se vinculó el dispositivo sembrado (el selector desapareció al no quedar más disponibles); al guardar, la ficha del artículo muestra "Síntomas", "Posibles causas" y "Dispositivos afectados" con el chip enlazado a la ficha real del dispositivo (clic verificado, navega a `/dispositivos/:id`); la búsqueda global por "tóner" (una causa) encontró el artículo. Datos de prueba y sesión simulada limpiados al terminar (`cambiosPendientes` en 0).
- Pendiente (usuario): aplicar el `supabase/schema.sql` actualizado en Supabase (agrega `sintomas`, `causas` y `dispositivos_afectados` a `articulos`).

### 37. Ruta de inicio (modo aprendizaje para nuevos integrantes)
- Finalizada (código): 2026-07-07. Pendiente de un paso del usuario en Supabase (aplicar schema.sql) y de que el equipo cure el contenido real.
- Descripción: registrada tras el análisis de nuevas funcionalidades del 2026-07-07 (idea #4). Puerta de entrada para quien recién llega al equipo. Deliberadamente no se creó una sección ni una categoría nueva ni contenido inventado (el asistente no conoce los procedimientos reales del equipo): el trabajo de código es el mecanismo para que el equipo destaque, con el editor que ya usa a diario, la guía que decida escribir.
- Solución:
  - `src/lib/db.ts`: nuevo campo `Articulo.esRutaInicio: boolean`. Cualquier artículo (típicamente uno con procedimiento y subprocedimientos vinculados a lo ya documentado, por ejemplo "Primer día en TI") puede marcarse así; puede haber varios marcados a la vez.
  - `src/lib/tablas.ts`: mapeo `esRutaInicio` → `es_ruta_inicio`.
  - `supabase/schema.sql`: columna `es_ruta_inicio boolean not null default false` en `articulos` (en la creación de la tabla y con `alter table ... add column if not exists` para las bases ya desplegadas).
  - `src/features/soluciones/ArticuloForm.tsx`: casilla "Destacar en Inicio como ruta de aprendizaje" bajo el campo Tipo, con su explicación breve.
  - `src/features/inicio/InicioPage.tsx`: sección "Para empezar" (antes de "Recientes") que lista, con un ícono propio (`IconoRuta`), todos los artículos con `esRutaInicio`; no aparece si no hay ninguno marcado.
  - `src/features/historial/textoHistorial.ts`: etiqueta "Ruta de inicio" para el campo en el historial de cambios del artículo.
- Pruebas: 245 en verde (sin nuevas: es un campo de datos simple sobre lógica ya probada; el proyecto no tiene pruebas de componentes React, solo de lógica pura), lint y build en verde. Se actualizaron dos fábricas de artículos de prueba (`recientes.test.ts`, `sync.test.ts`) que construían un `Articulo` completo, para incluir el campo nuevo.
- Verificado en navegador real (sesión simulada + categoría y dos artículos sembrados en Dexie, uno con `esRutaInicio: true` y otro sin marcar): (1) Inicio muestra "Para empezar" solo con el artículo marcado, el clic navega a su ficha; (2) al editar el artículo sin marcar y tildar la casilla (clic simulado con secuencia completa de eventos de puntero, igual que en la tarea 36) y guardar, el campo queda en `true` en Dexie; (3) al volver a Inicio, ambos artículos aparecen en "Para empezar". Datos de prueba y sesión simulada limpiados al terminar (`cambiosPendientes` en 0).
- Pendiente (usuario): (1) aplicar el `supabase/schema.sql` actualizado en Supabase (agrega `es_ruta_inicio`); (2) curar el contenido real: crear (o elegir) el artículo "Primer día en TI" con sus subprocedimientos vinculados a lo que el equipo ya documentó, y marcar la casilla nueva en su editor.

### 36. Bitácora de intervenciones por dispositivo
- Finalizada (código): 2026-07-07. Pendiente de un paso del usuario en Supabase (aplicar schema.sql).
- Descripción: registrada tras el análisis de nuevas funcionalidades del 2026-07-07 (ideas #2 y #5). El historial automático de una ficha solo captura cambios de campos; no había forma de anotar a mano lo que se le hizo a un equipo (cambio de disco, reinstalación de Windows, configuración de un usuario nuevo).
- Solución:
  - `src/lib/repositorio.ts`: nueva función `registrarIntervencion(dispositivoId, descripcion, motivo?)`, que crea directamente una entrada de historial con `campo: 'intervencion'` (usa `crearEntrada`, no pasa por `guardarRegistro` porque no hay una ficha que editar) y la encola para subir igual que el resto del historial. Devuelve el id de la entrada para poder adjuntarle una foto.
  - `src/lib/db.ts`: `Adjunto.entidadTipo` amplía a `'articulo' | 'dispositivo' | 'historial'`. `'historial'` es la foto opcional de una intervención: `entidadId` apunta al id de esa entrada de historial, no a un artículo ni a un dispositivo. Como una foto colgada de una entrada de historial no debe generar a su vez una entrada de historial ("historial sobre el historial"), `construirHistorial` y `entradasEliminacion` en `repositorio.ts` devuelven `[]` cuando el adjunto tiene `entidadTipo === 'historial'`; `destinoHistorial` ya no maneja `adjuntos` (se resuelve aparte en ambas llamadas) porque su tipo de retorno no admite `'historial'`.
  - `supabase/schema.sql`: la restricción `check` de `adjuntos.entidad_tipo` admite ahora `'historial'`, con migración (`alter table ... drop/add constraint`) para las bases ya desplegadas.
  - `src/features/dispositivos/RegistrarIntervencion.tsx` (nuevo): botón "+ Registrar intervención" en la ficha del dispositivo que abre un formulario (descripción obligatoria, motivo opcional); al guardar muestra "Intervención registrada" y el bloque `Adjuntos` reutilizado (`entidadTipo="historial"`) para adjuntar una foto a esa intervención puntual. Integrado en `src/features/dispositivos/DispositivoPage.tsx`, entre el bloque de adjuntos del dispositivo y el historial.
  - `src/features/historial/textoHistorial.ts`: `descripcionEntrada` devuelve directamente `valorNuevo` para `campo === 'intervencion'` (sin envolverlo en "se definió como...").
  - `src/features/historial/Historial.tsx`: cada entrada con `campo === 'intervencion'` muestra debajo su propio bloque `Adjuntos` (mismo componente, `entidadTipo="historial"`, `entidadId` = id de la entrada), permitiendo ver o agregar la foto después.
- Pruebas: 245 en verde (2 nuevas: `registrarIntervencion` en `repositorio.test.ts`, descripción de `'intervencion'` en `textoHistorial.test.ts`), lint y build en verde.
- Verificado en navegador real (sesión simulada + dispositivo y categoría sembrados en Dexie, ver memoria "Verificación en navegador con sesión simulada"): el botón abre el formulario, el envío real (clic con secuencia completa de eventos de puntero, ya que el clic sintético del entorno de pruebas no disparaba el submit) crea la entrada, muestra "Intervención registrada" con su bloque de adjuntos ("Sin adjuntos todavía"), y al abrir "Ver historial" la entrada aparece con la descripción, el usuario, la fecha, el motivo ("Motivo: ...") y su propio bloque de adjuntos anidado. Datos de prueba y sesión simulada limpiados al terminar (`cambiosPendientes` en 0).
- Pendiente (usuario): aplicar el `supabase/schema.sql` actualizado en el SQL Editor de Supabase (amplía la restricción `check` de `adjuntos.entidad_tipo` para admitir `'historial'`). Sin este paso, subir una foto adjunta a una intervención fallará en el servidor (la app la deja igualmente disponible en el teléfono que la subió, por la cola de subida). Al aplicarlo, verificar desde aquí que la restricción quedó actualizada.

### 35. Evitar que el dispositivo guarde y autocomplete los secretos (campos enmascarados sin type=password)
- Finalizada: 2026-07-07
- Descripción: pedido por el usuario el 2026-07-07 (tercera iteración del tema de seguridad). En móvil seguía guardándose usuario y contraseña, y al reingresar se autocompletaban para entrar. También pasaba dentro de Notas: al crear o editar una credencial, el sistema ofrecía guardar ese secreto de la bóveda en el llavero del teléfono, filtrándolo fuera del cifrado y haciéndolo reaparecer como "dato guardado".
- Causa: todos los campos de secreto usaban `<input type="password">`. Los gestores del sistema operativo (llavero de iOS, Google/Android) detectan cualquier `type="password"` y ofrecen guardarlo al enviar, ignorando `autoComplete="off"`. El caso más grave: el formulario de credenciales (campo "Usuario" + `type="password"`) parecía un login y el sistema lo guardaba, filtrando el secreto de la bóveda.
- Solución:
  - `src/components/CampoContrasena.tsx` (nuevo): campo para escribir secretos que NO es `type="password"`. Usa `type="text"` enmascarado por CSS (`-webkit-text-security: disc`) con detección de soporte (`CSS.supports`); si el navegador no lo soporta, cae a `type="password"`. Incluye `autoComplete/autoCorrect/autoCapitalize="off"`, `spellCheck=false` y pistas `data-1p-ignore`/`data-lpignore`/`data-bwignore`. Prop `revelado` para el botón "Mostrar".
  - `src/index.css`: clase `.enmascarado` con `-webkit-text-security: disc`.
  - Reemplazados los 8 campos de secreto de la app: login (`LoginPage`), formulario de credenciales (`CredencialForm`), contraseña maestra (`BovedaGuard` ×2, `CredencialEnPaso`, `DialogoEliminar`), cambio de contraseña de la cuenta (`CuentaPage` ×3) y bloqueo de la app (`BloqueoAppGuard`, `SeguridadPage`). La vista de un secreto ya guardado (`CampoSecreto`) ya era texto en un `<dd>`, sin input: no se tocó.
- Alcance honesto: previene NUEVOS guardados en los navegadores móviles del equipo (iOS Safari, Android Chrome soportan `-webkit-text-security`). Los secretos que el teléfono ya guardó antes siguen en el sistema hasta que el usuario los borre a mano en los ajustes del teléfono. La bóveda sigue cifrada y con contraseña maestra; el bloqueo de app (tarea 33) gatea el acceso.
- Pruebas: 242 en verde (sin nuevas: es un cambio de componente de interfaz), lint y build en verde.
- Verificado en navegador real: (1) login: el campo de contraseña es `type="text"` con `-webkit-text-security: disc` y `data-1p-ignore`, NO `type="password"`; escribir captura el valor y queda enmascarado; (2) formulario de credenciales (con sesión, perfil con permiso y verificador sembrados, bóveda desbloqueada): NINGÚN input es `type="password"`; el campo Contraseña queda enmascarado; el botón "Mostrar" revela el texto (`-webkit-text-security: none`) y lo vuelve a ocultar, sin cambiar a `type="password"`. Todo limpiado al terminar (`cambiosPendientes` en 0). Sin errores de consola. Documentado en ARQUITECTURA.md sección 14.

### 34. El paso como contenedor: no avanzar hasta completar su subprocedimiento y su solución
- Finalizada: 2026-07-07
- Descripción: pedido por el usuario el 2026-07-07. Bug: en un paso con instrucciones propias Y un subprocedimiento vinculado, al marcar la última instrucción la app saltaba directo al siguiente paso del procedimiento principal, ignorando que el subprocedimiento vinculado aún debía ejecutarse. Un paso debe tratarse como un contenedor completo (instrucciones, subtareas, subprocedimiento, solución de error) y no avanzar hasta terminar todo su contenido.
- Causa: `alternarInstruccionHecha` (`src/lib/progresoPasos.ts`) marcaba el paso como hecho en cuanto se completaban sus instrucciones, y `ProcedimientoVista` avanzaba, sin revisar el subprocedimiento ni la pregunta de error.
- Solución:
  - `src/lib/progresoPasos.ts`: `alternarInstruccionHecha` ya NO agrega el paso a `pasosHechos` al completar las instrucciones (solo lo retira al desmarcar); sigue devolviendo true como señal. La decisión de completar el paso pasa a la vista.
  - `src/lib/procedimiento.ts`: dos ayudantes puros nuevos. `pasoTrabajoPrevioCompleto(totalInstrucciones, instruccionesHechas, subProcedimientoSatisfecho)` (instrucciones + subprocedimiento) y `pasoSeCompletaSolo(trabajoPrevioCompleto, tieneSolucionVinculada)` (solo avanza si el trabajo previo está listo y no hay solución; si la hay, la completa la pregunta de error).
  - `src/features/soluciones/ProcedimientoVista.tsx`: `intentarCompletarPaso` marca el paso hecho y avanza solo cuando el trabajo previo está completo y no hay solución. Lo llaman tanto el completado de instrucciones (`alternarInstruccion`) como el `onCompletado` del subprocedimiento vinculado (antes llamaba directo a `completarPasoYAvanzar`, que saltaba). Usa lecturas frescas de la base para evitar la carrera con el refresco de las live queries. Se agregaron consultas en vivo (`bulkGet`) de los subprocedimientos vinculados del nivel para saber en vivo cuáles están completos (`subSatisfechoReactivo`, para mostrar; `subSatisfechoFresco`, para decidir). La pregunta "¿Ocurrió algún error?" ahora se muestra solo cuando el trabajo previo del paso está completo; y aparece un aviso "Termina el procedimiento vinculado para completar este paso" mientras quede pendiente.
- Pruebas: 6 nuevas puras en `procedimiento.test.ts` (`pasoTrabajoPrevioCompleto`, `pasoSeCompletaSolo`) y 1 actualizada en `progresoPasos.test.ts` (completar las instrucciones ya no marca el paso). 242 en total, lint y build en verde.
- Verificado en navegador real con sesión y datos simulados (artículo "Alta de usuario nuevo" con un paso "Configurar computador nuevo usuario" de 2 instrucciones + subprocedimiento "Configurar correo" de 2 pasos; todo limpiado al terminar, `cambiosPendientes` en 0): (1) marcar las 2 instrucciones NO avanza al paso 2; el paso queda en progreso, con el subprocedimiento inline (0/2) y el aviso; `pasosHechos` vacío; (2) completar el subprocedimiento completa el paso 1 (`pasosHechos` = ['p1']) y expande el paso 2; el aviso desaparece; (3) el caso simple (paso solo con instrucciones) sigue completándose y el banner "Procedimiento completado" aparece al terminar. Sin errores de consola. Documentado en ARQUITECTURA.md sección 5.

### 33. Bloqueo de la aplicación en el dispositivo (patrón o contraseña) y endurecimiento del login
- Finalizada: 2026-07-07
- Descripción: pedido por el usuario el 2026-07-07. Riesgo detectado: la sesión persiste en el navegador (localStorage de Supabase Auth) sin caducar y el login usaba `autoComplete="username"/"current-password"`, así que el navegador ofrecía guardar la cuenta y luego autocompletarla para entrar de un toque. Quien tomara el teléfono con la sesión abierta ya estaba dentro de la app. La bóveda ya estaba bien protegida (contraseña maestra, cifrado, autobloqueo), pero el resto de la app quedaba accesible.
- Decisiones del usuario (preguntas antes de implementar): (1) alcance = bloquear TODA la app; (2) método = patrón o contraseña a elección del usuario, SIN biometría (dato personal sensible que no todos quieren entregar); mantener la contraseña maestra para lo crítico (bóveda y borrados). Modelo de tres capas: sesión de inicio, bloqueo de app (diario), contraseña maestra (crítico).
- Solución:
  - `src/lib/db.ts`: tipo `MetodoBloqueoApp` ('patron' | 'contrasena'), `ID_BLOQUEO_APP`, interfaz `ConfigBloqueoApp` y tabla local `seguridadApp` (versión 7 del esquema). Local a cada dispositivo, no se sincroniza; guarda solo el verificador cifrado, nunca el secreto.
  - `src/features/seguridad/patron.ts`: lógica pura del patrón 3x3 (numeración por fila, `nodoIntermedio` estilo móvil que inserta el punto saltado, `agregarNodo`, `patronValido`, `serializarPatron`, `contarNodosSerializados`). El patrón se maneja siempre serializado ("0-4-8") y ese texto es el secreto.
  - `src/features/seguridad/bloqueoApp.ts`: sesión del bloqueo en memoria (mismo patrón observable que `sesionBoveda.ts`). Verificador con `crypto.ts` (PBKDF2 600k + AES-256-GCM sobre un texto fijo). `configurarBloqueoApp`, `desbloquearApp`, `cambiarBloqueoApp`, `quitarBloqueoApp`, `restablecerBloqueoApp` (sin secreto, solo junto con cierre de sesión), `bloquearApp`, `validarSecreto`. Autobloqueo por inactividad (1/5/15/30 min) y al volver de segundo plano tras ese tiempo (guardas `typeof document`/`localStorage` para el entorno de pruebas node). Freno a la fuerza bruta: espera de 30 s tras 5 fallos, persistida en `bloqueadoHasta`.
  - `src/features/seguridad/PatronInput.tsx`: cuadrícula 3x3 táctil (SVG + eventos de puntero), reutilizada para crear, confirmar y desbloquear; `setPointerCapture` protegido con try/catch.
  - `src/features/seguridad/BloqueoAppGuard.tsx`: envuelve toda la zona autenticada; si el dispositivo tiene bloqueo y no se ha desbloqueado en esta apertura, muestra la pantalla de bloqueo (patrón o contraseña) con salida de emergencia "Cerrar sesión y quitar el bloqueo".
  - `src/features/seguridad/SeguridadPage.tsx` (`/cuenta/seguridad`): activar (elegir método + confirmar), cambiar (pide el actual), quitar (pide el actual), autobloqueo y "Bloquear ahora". `useBloqueoApp.ts`: estado observable con `useSyncExternalStore`.
  - `src/App.tsx`: `BloqueoAppGuard` (lazy, con Suspense) envolviendo escáner, etiquetas y el grupo `Layout`; ruta `cuenta/seguridad`. `src/app/Layout.tsx`: botón de candado en la cabecera (visible solo si hay bloqueo configurado). `src/features/autenticacion/CuentaPage.tsx`: tarjeta que enlaza a Seguridad.
  - Endurecimiento del login `src/features/autenticacion/LoginPage.tsx`: `autoComplete="off"` en el formulario y los campos (antes 'username'/'current-password'); mismo criterio en el cambio de contraseña de `CuentaPage.tsx`.
- Pruebas: `patron.test.ts` (14) y `bloqueoApp.test.ts` (11) nuevas, 236 en total; lint y build en verde. `SeguridadPage` y `BloqueoAppGuard` quedan en trozos propios (no engordan el arranque).
- Verificado en navegador real con sesión simulada (todo limpiado al terminar; `cambiosPendientes` en 0, tablas en 0, token quitado): (1) alta por contraseña con confirmación; la fila guardada contiene solo el verificador cifrado, sin el secreto (`contieneSecreto` = false); (2) "Bloquear ahora" muestra la pantalla de bloqueo cubriendo toda la app; contraseña incorrecta → "Contraseña incorrecta." y sigue bloqueada; correcta → entra; (3) recarga (arranque en frío) con sesión válida en ruta profunda → vuelve a bloquear; (4) salida de emergencia → borra la config y cierra la sesión, lleva a /login; (5) alta por patrón: la pantalla dibuja la cuadrícula 3x3 y el trazo 0-1-2-5 desbloquea; (6) login con `autoComplete="off"` confirmado en el DOM; sin errores de consola. Documentado en ARQUITECTURA.md sección 14.

### 32. Interfaz moderna en navegación y borrado protegido con contraseña maestra
- Finalizada: 2026-07-07
- Descripción: pedido por el usuario el 2026-07-07 en dos frentes. (1) Modernizar el botón de "regresar" (era un enlace de texto gris pequeño `← destino`, repetido en muchas páginas) y los diálogos de eliminación (eran `window.confirm` del navegador). (2) Agregar una capa de seguridad: al eliminar información sensible se pide la contraseña maestra antes de borrar.
- Decisiones del usuario (recogidas con preguntas antes de implementar): piden contraseña maestra al eliminar procedimientos (artículos), credenciales y dispositivos; NO al eliminar adjuntos ni pasos sueltos del editor (menores y reversibles). Un usuario SIN permiso `puede_ver_boveda` no puede verificar la contraseña (RLS no le entrega el verificador): en ese caso se BLOQUEA la eliminación sensible (solo el personal con acceso a la bóveda borra información importante). Si el equipo aún no definió la contraseña maestra, la eliminación sensible cae a confirmación normal.
- Solución:
  - `src/components/Modal.tsx`: primitiva de ventana modal (fondo oscurecido, cierre con Escape o tocando fuera, bloqueo del scroll del fondo, hoja inferior en móvil y centrada en escritorio, accesible con role/aria).
  - `src/components/DialogoEliminar.tsx`: diálogo de confirmación moderno. Con `sensible` decide su modo: 'cargando' → 'contrasena' (pide y verifica la maestra), 'simple' (confirmación normal), o 'bloqueado' (sin permiso de bóveda). Verifica con `verificarContrasenaMaestra` (contraseña incorrecta → error y no borra; correcta → ejecuta; sin verificar / sin conexión → mensaje). Lee el permiso del usuario de `useAuth().perfil`.
  - `src/components/BotonVolver.tsx`: botón de regreso con icono de flecha y área táctil cómoda, consistente en toda la app.
  - `verificarContrasenaMaestra` (`ResultadoVerificacion`: 'correcta' | 'incorrecta' | 'sin-verificar') en `src/features/boveda/sesionBoveda.ts`: comprueba la contraseña contra el verificador SIN abrir la sesión de la bóveda ni tocar el estado; trae el verificador del servidor si no está local; funciona sin conexión si ya lo está.
  - Uso del diálogo: `ArticuloPage.tsx`, `DispositivoPage.tsx`, `CredencialPage.tsx` (con `sensible`); `Adjuntos.tsx` y `PasosEditor.tsx` (confirmación normal). `BotonVolver` adoptado en ArticuloPage, ArticuloForm, CategoriaPage, DispositivoPage, DispositivoForm, CredencialPage, CredencialForm, CuentaPage, EtiquetasPage y TopologiaPage. EscanerPage se dejó con su botón propio (overlay de cámara, contexto distinto). El componente `Adjuntos.tsx` se conserva.
- Pruebas: 4 nuevas en `sesionBoveda.test.ts` para `verificarContrasenaMaestra` (correcta/incorrecta/vacía contra el verificador local, que NO abre la sesión, que trae y guarda el verificador del servidor, y 'sin-verificar' sin nada local ni servidor). 211 pruebas en total, lint y build en verde.
- Verificado en navegador real con sesión y perfil simulados y un verificador sembrado para la contraseña "maestra" (todo eliminado al terminar, incluidos el historial y la cola que generó el borrado real; `cambiosPendientes` en 0): (1) botón de regreso moderno (pill redondeada con icono); (2) eliminar procedimiento con permiso de bóveda: el diálogo pide la contraseña; incorrecta → "Contraseña incorrecta." y el artículo NO se borra; correcta → se borra y navega a la categoría; (3) sin permiso de bóveda al eliminar un dispositivo: el diálogo muestra "Solo el personal con acceso a la bóveda puede eliminar esta información.", sin campo de contraseña y solo con "Cerrar"; (4) eliminar un paso en el editor: diálogo moderno sin contraseña, y al confirmar el paso desaparece. Sin errores de consola. Documentado en ARQUITECTURA.md sección 8.

### 31. Adjuntos por paso del procedimiento (en vez de al artículo completo)
- Finalizada: 2026-07-07
- Descripción: pedido por el usuario el 2026-07-07. Los archivos e imágenes de un procedimiento deben acompañar el paso donde se usan (foto del puerto en el Paso 1, captura del instalador en el Paso 2), no vivir mezclados en un apartado "Adjuntos" del artículo completo del que no se sabe a qué paso pertenecen. Cada paso permite varios adjuntos, con dos formas de agregarlos: "Cámara" (tomar foto en el sitio, útil desde el celular en un mantenimiento) y "Archivos" (imágenes, capturas, manuales, PDF ya guardados).
- Decisión de modelo: se reemplazó el campo `imagen: string | null` del paso (una sola captura) por `adjuntos: PasoAdjunto[]` (`{ referencia, nombre, tipo }`), inline en el JSON del procedimiento, siguiendo el patrón de los demás datos del paso (no una tabla nueva). Migración automática al normalizar (`normalizarProcedimiento`): un `imagen` viejo se convierte en un único adjunto, derivando nombre y tipo de la referencia; la lista nueva tiene prioridad sobre el campo viejo. La cola de subida offline (`archivosPendientes`) y el cache offline (`adjuntosOffline`) funcionan igual, por referencia de Storage.
- Ubicación: `src/lib/db.ts` (tipo `PasoAdjunto` nuevo, campo `adjuntos` en `PasoProcedimiento`), `src/lib/procedimiento.ts` (`crearPaso`, `normalizarPaso` con `normalizarAdjuntos`/`nombreDeReferencia`/`tipoDeReferencia`, `prepararProcedimientoParaGuardar`), `src/features/soluciones/PasosEditor.tsx` (`subirAdjuntos` multiarchivo + `quitarAdjunto`, componentes `AdjuntosPasoEditor`/`AdjuntoPasoMiniatura`), `src/features/soluciones/ProcedimientoVista.tsx` (componentes `AdjuntosPaso`/`AdjuntoPaso`: imágenes a ancho completo o en dos columnas, archivos no-imagen como enlace que abre en pestaña), `src/features/soluciones/ArticuloPage.tsx` (el apartado `Adjuntos` del artículo solo se muestra si NO hay procedimiento), `src/lib/adjuntosOffline.ts` (`referenciasParaOffline` recorre `paso.adjuntos`), `src/features/historial/resumenProcedimiento.ts` (`agregarLineasAdjuntos`: diff por referencia, "imagen/archivo" según tipo, con plural). El componente `src/components/Adjuntos.tsx` se conserva sin cambios (lo usan las fichas de dispositivos y los artículos sin procedimiento).
- Decisión de alcance: para los artículos sin procedimiento (manuales en Markdown, que no tienen pasos donde anclar el archivo) se conserva el apartado "Adjuntos" del artículo; solo desaparece cuando el artículo tiene procedimiento.
- Pruebas: 4 nuevas en `procedimiento.test.ts` (adjuntos válidos/descartados, completar nombre y tipo desde la referencia, migración del `imagen` viejo, prioridad de la lista nueva) y reescrita la de imágenes en `resumenProcedimiento.test.ts` (alta, reemplazo como alta+baja, archivo no-imagen, plural). 207 pruebas en total, lint y build en verde.
- Verificado en navegador real con sesión simulada y datos sembrados en Dexie (blobs cacheados offline para que las imágenes rendericen sin red; todo eliminado al terminar, `cambiosPendientes` y `archivosPendientes` en 0): (1) vista del procedimiento: el Paso 1 muestra la imagen a ancho completo y el PDF como enlace "📄 manual.pdf" que abre en pestaña, y NO aparece el apartado "Adjuntos" del artículo; (2) migración: un artículo cuyo paso guardaba el campo viejo `imagen` se lee y se muestra como adjunto de imagen; (3) artículo sin procedimiento: conserva el apartado "Adjuntos" del artículo con los botones Cámara/Archivos; (4) editor: cada paso muestra "Adjuntos del paso" con Cámara + Archivos, el Paso 1 con la miniatura de la imagen y la tarjeta del PDF, el Paso 2 con "Sin adjuntos todavía". Sin errores de consola. La captura de pantalla falló por un timeout puntual del entorno de previsualización (no del código); se verificó por snapshot de accesibilidad y consultas al DOM. No se probó la subida real de un archivo porque exige Storage de Supabase, pero reutiliza la misma lógica ya probada del componente de adjuntos del artículo (`comprimirImagen` + `subirOEncolarArchivo`). Documentado en ARQUITECTURA.md sección 5.

### 30. Historial legible de "Campos adicionales" en dispositivos
- Finalizada: 2026-07-07
- Descripción: el campo `detalles` de los dispositivos (Record<string,string>, mostrado como "Campos adicionales") se registraba en el historial como JSON crudo, el mismo problema que tenía `procedimiento` antes de la tarea 29.
- Solución: mismo patrón, mismo lugar. Módulo puro nuevo `src/features/historial/resumenDetalles.ts` que compara clave por clave el objeto de antes y de después y describe cada diferencia en lenguaje natural: `Se agregó el campo "X": "valor".`, `Se quitó el campo "X" ("valor").`, `Se cambió "X": "antes" → "después".`; si no hay diferencias detectables (por ejemplo JSON inválido en el historial) cae en un resumen genérico. En `src/features/historial/Historial.tsx` se extrajo la parte compartida con la tarea 29 (lista de cambios + `<details>` "Detalle técnico" plegado con el JSON embellecido de antes/después) al componente `ResumenCambios`, reutilizado ahora por `CambioProcedimiento` y el nuevo `CambioDetalles` (activado con `entrada.campo === 'detalles'`). `textoHistorial.ts` suma el mismo respaldo defensivo que tiene `procedimiento`, para nunca volcar JSON crudo si esa vista no se aplicara. 7 pruebas nuevas (`resumenDetalles.test.ts`: campo agregado, quitado, cambiado, campos sin cambios ignorados, varios cambios juntos, JSON inválido, valor anterior vacío tratado como objeto vacío; 203 pruebas en total). Verificado en navegador real con sesión simulada y datos sembrados en Dexie (dispositivo con una entrada de historial `detalles` que cambia un campo, quita otro y agrega uno nuevo; todo eliminado al terminar, `cambiosPendientes` en 0): el historial mostró "Cambios en los campos adicionales" con las tres líneas correctas (`Se cambió "Switch": "D31" → "D32".`, `Se quitó el campo "Puerto" ("1").`, `Se agregó el campo "VLAN": "10".`) sin JSON a la vista, y el "Detalle técnico" reveló el JSON de ambos lados al abrirlo (verificado por contenido; la captura de pantalla falló por un timeout puntual del entorno de previsualización, no del código). Documentado en ARQUITECTURA.md sección 9. Build, lint y 203 pruebas en verde.

### 29. Historial legible de los cambios de procedimiento (resumen en lenguaje natural)
- Finalizada: 2026-07-07
- Problema (reportado por el usuario el 2026-07-07): al editar el procedimiento de un artículo, el historial mostraba el JSON completo del procedimiento (antes y después) porque `procedimiento` es un objeto y `repositorio.ts` lo serializaba con `JSON.stringify` en `valorAnterior`/`valorNuevo`, y el visor lo pintaba crudo con `"antes" → "después"`. Extenso e ilegible: imposible ver de un vistazo qué cambió.
- Solución (en la capa de visualización, sin tocar la escritura ni la sincronización, para que funcione también con el historial ya guardado, conserve el JSON para depuración y no exija migración): módulo puro nuevo `src/features/historial/resumenProcedimiento.ts` que compara el JSON de antes y el de después (normalizados con `normalizarProcedimiento`) y genera un resumen en lenguaje natural: pasos agregados (por título) / eliminados (por posición anterior) / reordenados; por paso, cambios de título, instrucciones (agregadas/eliminadas/editadas, con edición = alta y baja emparejadas para no duplicar, y plural correcto), imagen (agregada/actualizada/eliminada), credencial (agregada/reemplazada/eliminada), subprocedimiento y solución de error vinculados; y requisitos previos. Agregar o eliminar todo el procedimiento se describe con su tamaño ("2 pasos, 3 instrucciones"). Devuelve `{ contexto, cambios[] }` con el tamaño del estado anterior como contexto ("Antes: 5 pasos, 10 instrucciones"). El componente `src/features/historial/Historial.tsx` renderiza, para las entradas con `campo === 'procedimiento'`, el encabezado "Cambios en el procedimiento", el contexto, la lista de cambios y un `<details>` "Detalle técnico" (plegado) con el JSON de antes/después embellecido, disponible para desarrolladores sin ensuciar la vista normal. `textoHistorial.ts` suma la etiqueta `procedimiento` y un respaldo defensivo ("Se actualizó el procedimiento") por si esa vista no se aplica, para no volcar nunca el JSON crudo. 17 pruebas nuevas (`resumenProcedimiento.test.ts`) cubriendo cada tipo de cambio, la pluralización, la edición vs alta/baja, el reordenamiento, varios cambios juntos y el respaldo genérico ante JSON inválido (196 pruebas en total). Verificado en navegador real con sesión simulada y datos sembrados en Dexie (artículo con dos entradas de historial de tipo `procedimiento`: alta del procedimiento y una edición que agrega un paso, una instrucción, una imagen y una credencial; todo eliminado al terminar, `cambiosPendientes` en 0): el historial muestra los resúmenes correctos ("Se agregó un nuevo paso ...", "Se agregó una nueva instrucción al Paso 1: ...", "Se agregó una imagen/credencial al Paso 2: Databases", "Antes: 2 pasos, 3 instrucciones") sin JSON a la vista, y "Detalle técnico" revela el JSON al abrirlo (captura). Documentado en ARQUITECTURA.md sección 9. Build, lint y 196 pruebas en verde.

### 28. Contraseña maestra anclada al servidor (verificador en boveda_meta)
- Finalizada (código): 2026-07-06. Pendiente de un paso del usuario en Supabase (aplicar schema.sql).
- Problema (reportado por el usuario el 2026-07-06): al borrar la caché o los datos del navegador, la app volvía a pedir "crear" la contraseña maestra, porque el estado de "primera vez" se decidía solo con la base local (0 credenciales locales == bóveda vacía). Cualquiera con acceso al dispositivo podía definir una contraseña nueva y, aunque el cifrado impedía leer los datos viejos, quedaba una vulnerabilidad de diseño: se podía suplantar el estado "vacío" y fragmentar la bóveda con dos contraseñas.
- Solución: la contraseña maestra queda anclada a la cuenta/servidor mediante un verificador. Tabla nueva `public.boveda_meta` (una sola fila, id fijo 'principal') que guarda `verificador`: un bloque AES-256-GCM de un texto fijo (`TEXTO_VERIFICADOR`) cifrado con la clave derivada de la contraseña maestra. La contraseña NUNCA viaja ni se guarda; el verificador solo permite comprobarla en cualquier dispositivo. RLS: SELECT e INSERT (una única vez, la PK fija impide una segunda fila) con `puede_ver_boveda`; a propósito SIN políticas de UPDATE ni DELETE, así reemplazarlo o borrarlo exige el panel de Supabase (esa es la validación de identidad para restablecer). Nueva lógica de desbloqueo (`src/features/boveda/sesionBoveda.ts`): (1) con verificador (local o descargado del servidor) solo se VERIFICA, jamás se crea; (2) sin verificador pero con credenciales cifradas (equipos que vienen de la versión anterior) se valida contra ellas como siempre y se sube el verificador en el mismo desbloqueo (migración automática; si falla por estar sin conexión, se reintenta en el próximo); (3) crear una contraseña maestra nueva SOLO se permite cuando el servidor confirma explícitamente que no existe ni verificador ni credenciales; (4) sin nada local y sin respuesta del servidor (sin conexión, sin permiso o esquema sin aplicar) el desbloqueo se NIEGA en vez de asumir "primera vez". Función `estadoInicialBoveda()` decide qué formulario mostrar ('verificar' | 'crear' | 'sin-confirmar'); `BovedaGuard.tsx` ya no cuenta credenciales locales, reacciona en vivo si la sincronización trae el verificador, y en 'sin-confirmar' muestra "No se pudo comprobar... Reintentar" sin ofrecer crear. Acceso al servidor aislado en `src/features/boveda/verificadorRemoto.ts` (consultar y subir, con manejo de conflicto 23505 por si dos técnicos la crean a la vez). Sincronización: `descargarBovedaMeta()` en `src/lib/sync.ts`, best effort (no rompe si la tabla no existe aún o el usuario no tiene permiso; nunca borra la copia local). Dexie v6 con store `bovedaMeta` e interfaz `BovedaMeta` + `ID_VERIFICADOR` en `src/lib/db.ts`. Restablecimiento documentado en `supabase/INSTRUCCIONES.md` sección 5 (borrar boveda_meta y credenciales desde el panel; los datos cifrados con la contraseña perdida son irrecuperables por diseño, ni con los respaldos). ARQUITECTURA.md sección 8 actualizada. 13 pruebas nuevas (179 en total) que simulan el servidor (mock de verificadorRemoto): verificación local, cache borrada con verificador en servidor (rechaza al atacante, acepta la correcta), primera vez solo con confirmación del servidor, fallo de registro que no desbloquea, carrera entre dos técnicos, migración desde credenciales sin verificador, sin conexión que sigue desbloqueando, vaciar credenciales que ya no permite recrear, y los 6 casos de estadoInicialBoveda. Verificado en navegador real con sesión simulada (datos borrados al terminar, cambiosPendientes en 0): con la caché "borrada" (sin credenciales ni verificador local) y el servidor sin responder (sesión falsa), la pantalla muestra "No se pudo comprobar la configuración... Reintentar" y NO ofrece crear contraseña (captura); con el verificador ya presente, aparece el formulario de verificación, rechaza la contraseña de un atacante ("Contraseña incorrecta") y desbloquea con la correcta. Sin errores de consola. Build, lint y 179 pruebas en verde. Compatibilidad hacia atrás: desplegar antes de aplicar el esquema es seguro (los equipos con credenciales cacheadas siguen desbloqueando; el caso de bóveda vacía + caché borrada queda en 'sin-confirmar', que es justamente el comportamiento seguro).
- Pendiente (usuario): aplicar el `supabase/schema.sql` actualizado en el SQL Editor de Supabase (crea `boveda_meta` y sus políticas). Al hacerlo, verificar desde aquí por REST que la tabla responde.

### 27. Aviso de actualización cuando llega una versión nueva
- Finalizada: 2026-07-06
- Resultado: corrección de un problema real detectado en producción, pedido por el usuario el 2026-07-06 porque los cambios no se veían reflejados en la URL de Vercel ni en PC ni en celular. Diagnóstico (verificado descargando los archivos servidos por Vercel): el despliegue estaba correcto (HTML, chunks `ArticuloPage`/`ArticuloForm` y `sw.js` ya servían el código nuevo), pero el `registerSW.js` autoinyectado por vite-plugin-pwa era la versión mínima (`navigator.serviceWorker.register('/sw.js')` a secas): no comprobaba actualizaciones ni recargaba al activarse una versión nueva. Combinado con la carga diferida por ruta (los chunks se piden bajo demanda y los sirve el precache del service worker), el worker viejo seguía entregando los chunks cacheados y los clientes se quedaban en la versión vieja indefinidamente. Solución (opción "aviso para actualizar" elegida por el usuario): `registerType` cambiado de `autoUpdate` a `prompt` en `vite.config.ts`, tipos de vite-plugin-pwa agregados en `src/vite-env.d.ts` (`vite-plugin-pwa/react` y `/client`), componente nuevo `src/components/ActualizacionDisponible.tsx` (hook `useRegisterSW`) que muestra una pastilla flotante discreta "Versión nueva disponible" + botón Actualizar cuando hay una versión en espera; al confirmar, `updateServiceWorker(true)` activa el nuevo worker y recarga con el código nuevo, sin interrumpir un procedimiento a medias. Comprueba si hay versión nueva al abrir la app y cada hora mientras siga abierta. Montado en `src/App.tsx` (siempre presente, devuelve null sin novedad; flota sobre la barra inferior con `bottom-20 z-50`). El manejador de `vite:preloadError` (tarea 25) sigue como red de seguridad. Documentado en ARQUITECTURA.md sección 7. Verificado de punta a punta en el build de producción (`vite preview`): con el service worker v1 controlando, se reconstruyó una v2 (marca temporal en el DOM para cambiar el hash, revertida después), `registration.update()` dejó el nuevo worker "instalado y en espera" (comportamiento de `prompt`), apareció el aviso en la interfaz (captura), y tocar Actualizar recargó la app dejando el marcador `v2` en el DOM y el aviso desaparecido (worker viejo ya sin `waiting`). Service worker y caches del preview limpiados al terminar. Build, lint y 166 pruebas en verde. Nota de transición: el primer despliegue de este cambio cae encima del service worker viejo (autoUpdate sin recarga), así que los clientes actuales necesitan una limpieza manual una única vez (F12 > Application > Service Workers > Unregister + Clear site data, o reinstalar la PWA); desde entonces el aviso funciona en cada despliegue.

### 26. Apartado "Datos" por paso: contraído y revelado al tocarlo
- Finalizada: 2026-07-06
- Resultado: mejora de presentación pedida por el usuario el 2026-07-06. Nota de diseño: las tres funciones de fondo que pidió (datos sensibles por paso ligados a la bóveda sin duplicar información, mismo comportamiento en procedimientos anidados y manejo de errores con solución vinculada que regresa al punto exacto) ya existían de las tareas 18, 20 y 22; esta tarea cubre el delta detectado. El vínculo de credencial en la vista del paso pasa a llamarse "Datos" y va contraído por defecto: una sola fila "Datos: <título>" con botón Ver/Ocultar (`aria-expanded`), y los secretos no entran al DOM hasta que el técnico lo toca, aunque la bóveda ya esté desbloqueada; al contraerlo salen del DOM. Al tocarlo: sin permiso `puedeVerBoveda` se ve un mensaje genérico; con la bóveda bloqueada aparece el formulario de contraseña maestra en el propio paso (desbloqueo local PBKDF2/AES, con autobloqueo compartido con la sección Notas; contraseña incorrecta muestra error y nada se revela); desbloqueada se muestran usuario, contraseña (enmascarada con su propio "Mostrar"), IP, URL y extras, más el enlace "Abrir en Notas". El editor de pasos renombró el vínculo: "+ Vincular datos de la bóveda (opcional)" y chip "Datos vinculados: <título>". Cambios en `src/features/boveda/CredencialEnPaso.tsx` (componente completo: encabezado convertido en botón conmutador y contenido condicionado a `abierto`), `src/features/soluciones/PasosEditor.tsx` (CredencialSelector), `src/lib/db.ts` (comentario de `credencialId`) y ARQUITECTURA.md secciones 5 y 8. El comportamiento en subprocedimientos y soluciones anidados es idéntico sin código extra porque reutilizan el mismo componente. Verificado en navegador real con sesión simulada y datos sembrados en Dexie (credencial cifrada con contraseña maestra de prueba; todo eliminado al terminar, `cambiosPendientes` en 0): apartado contraído sin secretos en el DOM, toque que abre el formulario, contraseña incorrecta rechazada con los datos ocultos, correcta que revela los campos con captura de pantalla, Ocultar que los saca del DOM, mismo flujo dentro de un procedimiento padre que reutiliza el artículo, y etiquetas nuevas en el editor. Build, lint y 166 pruebas en verde.

### 25. Corrección de pantalla en blanco en la carga (recuperación ante fallo de chunk)
- Finalizada: 2026-07-06
- Resultado: la app se quedaba en blanco de forma intermitente al abrirla y solo se recuperaba actualizando a mano. Causa: la PWA usa `registerType: 'autoUpdate'` (vite-plugin-pwa) con carga diferida por ruta (`React.lazy` en `src/App.tsx`); al publicar una versión nueva, el service worker se actualizaba en segundo plano, borraba del caché los trozos (chunks) viejos y tomaba el control, pero la página que seguía corriendo con el `index.html` viejo pedía un chunk con nombre viejo que ya no existía ni en caché ni en el servidor. El `import()` fallaba y, al NO haber ningún error boundary, React desmontaba toda la app: pantalla en blanco. Corrección (patrón estándar Vite/PWA): (1) `src/lib/recargaChunk.ts` con `esErrorDeChunk` (reconoce los mensajes de import dinámico fallido de cada navegador, sin confundir errores de red normales de la app) y `recargarUnaVezPorChunk` (recarga una sola vez, con guard en sessionStorage de 10 s para no entrar en bucle si el fallo persiste, por ejemplo sin conexión); (2) listener de `vite:preloadError` en `src/main.tsx` que recarga automáticamente para tomar el `index.html` nuevo, sin que el usuario actualice a mano; (3) `src/components/ErrorBoundary.tsx` envolviendo `<App/>` en main.tsx: recupera solo el fallo de chunk (muestra "Actualizando...") y, ante cualquier otro error o si ya se recargó hace poco, muestra una pantalla con botón "Recargar" en vez de la blanca; (4) `iniciarSync()` blindado con try/catch para que un fallo suyo tampoco deje la pantalla en blanco. 2 pruebas nuevas (166 en total) para la detección de mensajes. Verificado en navegador: el login (cargado bajo demanda) renderiza a través del boundary sin regresión; disparar `vite:preloadError` recarga la página sola (marcador de ventana borrado tras la recarga, guard en sessionStorage puesto) y la app vuelve a renderizar; un segundo fallo dentro de la ventana de 10 s NO recarga (anti-bucle, cae al reintento manual). Sin errores de consola. Build, lint, typecheck y 166 pruebas en verde. Nota: el primer despliegue de esta corrección todavía puede exigir una actualización manual a los clientes que tengan abierta la versión vieja (aún sin el handler); a partir de ahí, futuros despliegues se recuperan solos.

### 24. Sección Red: inventario de infraestructura, conexiones y topología
- Finalizada: 2026-07-06
- Resultado: sección nueva "Red" en la barra inferior con inventario de infraestructura (racks, puntos de red, switches, access points, cámaras), conexiones entre dispositivos y topología en árbol, según lo pedido por el usuario. Las fichas siguen viviendo en la tabla `dispositivos` (la marca `es_red` por categoría decide la sección; heredan sin costo búsqueda global, historial, fotos, QR e importación CSV) y se agregó la tabla `conexiones` (enlaces puerto a puerto con medio UTP/fibra/inalámbrico, e instalación en rack), con historial registrado en las fichas de ambos extremos. Detalle completo del código, pruebas y verificación en navegador en la sesión que lo implementó (2026-07-06); documentado en ARQUITECTURA.md secciones 4, 5, 11 y 12. Esquema aplicado en Supabase por el usuario y verificado por REST (2026-07-06): `categorias.es_red` existe y la tabla `conexiones` responde (antes daban error 42703 y PGRST205 respectivamente).

### 23. Sección "Notas": bóveda discreta y mínima exposición en la interfaz
- Finalizada: 2026-07-06
- Resultado: reorganización de la navegación pedida por el usuario el 2026-07-06 para que las funciones de seguridad no se anuncien en la interfaz (principio de mínima exposición). La antigua "Bóveda" pasa a llamarse "Notas" con un icono neutro (bloc de notas, no candado), su pestaña de la barra inferior solo aparece a los usuarios con permiso `puedeVerBoveda` (el resto ni ve la pestaña ni sabe que existe) y la ruta interna cambia de `/boveda` a `/notas` (la vieja `/boveda/*` redirige con `<Navigate>`, por si quedó algún enlace guardado). La barra (`src/components/BottomNav.tsx`) ahora lee el perfil en vivo desde Dexie y alterna entre 4 y 5 columnas según el permiso. Se neutralizaron todos los textos que revelaban el propósito: pantalla de desbloqueo y de acceso restringido ("Sección protegida del equipo" / "Tu usuario no tiene acceso a esta sección", `src/features/boveda/BovedaGuard.tsx`), grupo de resultados de búsqueda ("Notas" en vez de "Bóveda", `src/features/busqueda/ResultadosBusqueda.tsx`), credencial vinculada en los pasos de procedimiento ("Credencial" en vez de "Credencial de la bóveda", `src/features/boveda/CredencialEnPaso.tsx`) y el selector del editor (`src/features/soluciones/PasosEditor.tsx`). Rutas y enlaces `/notas` actualizados en `src/App.tsx`, `BovedaPage.tsx`, `CredencialPage.tsx`, `CredencialForm.tsx` y `useIndiceBusqueda.ts`. La carpeta interna sigue llamándose `boveda/` (solo cambia lo visible). IMPORTANTE: es un cambio de presentación; la protección real no se toca (RLS por `puede_ver_boveda`, contraseña maestra PBKDF2+AES-256-GCM y autobloqueo siguen igual). Verificado en navegador con sesión simulada (datos borrados al terminar, sin encolar nada a producción): con permiso, la barra muestra 5 pestañas y `/notas` abre la pantalla de desbloqueo neutra; sin permiso, la pestaña desaparece (vuelve a 4) y el acceso directo a `/notas` muestra el mensaje genérico de restringido; `/boveda` redirige a `/notas`. Sin errores de consola. Documentado en ARQUITECTURA.md (secciones 4 y 8). Lint, typecheck y 164 pruebas en verde.

### 22. Simplificación del editor de procedimientos y solución de errores
- Finalizada: 2026-07-03
- Resultado: simplificación pedida por el usuario el 2026-07-03. (a) El formulario de artículo (`src/features/soluciones/ArticuloForm.tsx`) conserva solo Título y Tipo: se eliminó el campo Etiquetas porque su única función era aportar sinónimos a la búsqueda, que ya indexa título, contenido y todos los pasos; los valores existentes se conservan en la base sin mostrarse (pasan de largo al guardar), dejaron de mostrarse como chips en `ArticuloPage.tsx` y de indexarse en `src/features/busqueda/useIndiceBusqueda.ts`. (b) El editor de pasos (`src/features/soluciones/PasosEditor.tsx`) quedó sin el encabezado "Procedimiento paso a paso", sin la "Guía para redactar buenos pasos", sin "Antes de empezar" (los requisitos guardados se conservan y se siguen mostrando en la vista, solo dejó de editarse) y sin los campos por paso Detalle, Nota, Advertencia y Consejo; cada paso ofrece ahora solo título, instrucciones con casilla, captura y los tres vínculos (credencial, tarea reutilizable y solución). Los campos retirados se quitaron de `PasoProcedimiento` (`src/lib/db.ts`): la normalización los descarta al leer datos viejos y el guardado los limpia del JSON (verificado con un artículo sembrado con datos viejos: no se muestran, no rompen nada y el JSON queda limpio tras guardar). (c) La "Decisión (ramificación)" se reemplazó por la solución vinculada (campos `solucionArticuloId` y `solucionArticuloTitulo`, mismo patrón de referencia que el subprocedimiento): en la vista (`src/features/soluciones/ProcedimientoVista.tsx`, componente `SolucionEnPaso`), un paso pendiente con solución pregunta "¿Ocurrió algún error durante este paso?"; "No, continuar" completa el paso y el avance sigue solo; "Sí, ver la solución" despliega la solución ahí mismo (ámbar, con sus propios pasos, casillas y barra) y, al completarla, el paso donde ocurrió el error se completa, el flujo principal continúa desde ese punto y el progreso local de la solución se reinicia para el próximo error (aquí o en otro procedimiento que la reutilice); si la solución quedó a medias (salir y volver), se muestra abierta de nuevo sola; con el vínculo roto hay aviso ámbar, y dentro de un subprocedimiento expandido la solución se enlaza sin expandirse (misma regla de un solo nivel que corta ciclos). Con la decisión desaparecida, el avance automático ya no tiene excepciones. El selector del editor lista los mismos artículos con procedimiento que el de tareas. Pruebas actualizadas y nuevas en `src/lib/procedimiento.test.ts` (151 en total). Verificado en navegador real con sesión simulada y datos sembrados directo en Dexie (sin tocar el servidor; artículos, historial, recientes y cola de cambios eliminados al terminar), con capturas: pregunta de error visible en el paso expandido, solución desplegada con avance automático interno, regreso automático al flujo principal (paso completado en verde y siguiente expandido), "No, continuar" equivalente, progreso de la solución reiniciado, campos viejos invisibles y editor simplificado con el chip "Solución si el paso falla". Sin errores de consola. Build, lint y 151 pruebas en verde.

### 21. Rediseño de procedimientos: instrucciones con casilla, avance automático y estados
- Finalizada: 2026-07-03
- Resultado: rediseño de la experiencia paso a paso pedido por el usuario el 2026-07-03. Cada paso puede desglosarse en instrucciones con casilla de verificación (campo `instrucciones` en `PasoProcedimiento`, `src/lib/db.ts`; sin migración de Dexie ni de Supabase: viaja dentro del JSON `procedimiento` y el avance local `instruccionesHechas` con clave `<pasoId>:<indice>` es un campo opcional no indexado de `progresoPasos`). Al marcar la última instrucción, el paso se completa solo, se contrae y se expande automáticamente el siguiente pendiente (hacia adelante, o el primero saltado si no queda ninguno adelante) con scroll suave; lo mismo ocurre al marcar el círculo del paso a mano y al completarse un subprocedimiento vinculado, que además completa la tarea del paso padre (nueva prop `onCompletado` de `ProcedimientoVista`, propagada por `SubProcedimientoEnPaso`). Excepción deliberada: un paso con decisión de ramificación no avanza solo, porque la decisión elige el destino. Estados visibles por paso: pendiente (círculo gris), en progreso (círculo ámbar con contador "hechas/total" de instrucciones; el contador del subprocedimiento también pasa a ámbar cuando va a medias) y completado (círculo y borde verdes). Al terminar todo: banner verde "Procedimiento completado" con botón Reiniciar y barra de progreso en verde. Lógica pura y probada sin navegador: `siguientePasoPendiente` en `src/lib/procedimiento.ts`; API nueva de avance en `src/lib/progresoPasos.ts` (`establecerPasoHecho` arrastra las instrucciones del paso sin tocar las de otros, `alternarInstruccionHecha` devuelve si el paso quedó completo y mantiene la consistencia en ambos sentidos: desmarcar una instrucción vuelve pendiente un paso completado). Vista en `src/features/soluciones/ProcedimientoVista.tsx` (checklist con casillas accesibles `role="checkbox"`, texto tachado al marcar); editor en `src/features/soluciones/PasosEditor.tsx` (textarea "Instrucciones con casilla", una por línea, y línea nueva en la guía de redacción). Las instrucciones se normalizan, se limpian al guardar (un paso que solo tiene instrucciones no se descarta) y se indexan en la búsqueda (`textoDeProcedimiento`). 14 pruebas nuevas (151 en total). Verificado en navegador real con sesión simulada y datos sembrados (eliminados al terminar), con capturas de pantalla: pasos contraídos al abrir, checklist del paso expandido, estado ámbar 2/3 con tachado, avance automático al marcar la última instrucción (paso 1 verde contraído y paso 2 expandido solo), subprocedimiento completado que completó el paso padre (contador 2/2 verde) y abrió el paso 3, marcado manual del círculo que también avanza, banner verde final con "3 de 3 pasos completados" y el campo nuevo del editor precargado con las instrucciones existentes. Sin errores de consola. Build, lint y 151 pruebas en verde.

### 20. Procedimientos jerárquicos con tareas reutilizables
- Finalizada: 2026-07-03
- Resultado: un paso de un procedimiento puede vincular otro artículo con procedimiento, pedido por el usuario el 2026-07-03. Un procedimiento principal ("Configuración de PC para nuevo usuario") se compone así de tareas reutilizables ("Configurar impresora", "Configurar correo") que viven en su propio artículo: al expandir la tarea, su paso a paso completo se despliega ahí mismo (requisitos, capturas, credenciales de la bóveda, decisiones y barra de progreso incluidos), y actualizar el subprocedimiento actualiza automáticamente todos los que lo usan. Progreso en ambos niveles: cada tarea y cada subtarea tienen su casilla; el progreso del subprocedimiento usa el id del artículo vinculado, así es el mismo se abra desde el procedimiento padre o desde el artículo original, y se ve como contador "hechos/total" en la fila colapsada de la tarea (verde al completar). La expansión anidada es de un solo nivel: más profundo se muestra como enlace, lo que corta ciclos (A vincula a B y B a A) sin lógica adicional. Sin cambios de esquema: campos `subArticuloId` y `subArticuloTitulo` (copia de referencia) en el JSON `procedimiento` (`src/lib/db.ts`); normalización, limpieza e indexación en búsqueda (el título del subprocedimiento sí se indexa, no es información protegida) en `src/lib/procedimiento.ts`; selector "+ Vincular otro procedimiento como tarea" en `src/features/soluciones/PasosEditor.tsx` (lista solo artículos con procedimiento, excluye el artículo en edición para impedir la autorreferencia; al vincular sobre un paso sin título, toma el del artículo vinculado; nueva línea en la guía de redacción); componentes `SubProcedimientoEnPaso` y `ContadorSubProgreso` en `src/features/soluciones/ProcedimientoVista.tsx` (que ahora acepta `nivel` y se renderiza a sí misma para el subprocedimiento; título de respaldo y aviso ámbar si el artículo vinculado no está disponible). 3 pruebas nuevas (137 en total). Verificado en navegador real con sesión simulada y datos sembrados (eliminados al terminar): lista de tareas con contador 0/3, expansión anidada completa con "Antes de empezar" y barra propia, ciclo intencional impresora→PC mostrado como enlace sin expansión infinita, marcar 2 subtareas actualizó en vivo el contador del padre a 2/3 y el mismo progreso apareció al abrir el artículo de la impresora directamente, vínculo roto con aviso ámbar y título de respaldo, y en el editor: chips de vínculo, autoexclusión del propio artículo en el selector y título autocompletado al vincular en un paso vacío. La captura de pantalla volvió a fallar por la limitación conocida del entorno (no del código); lo visual se verificó inspeccionando DOM y estilos calculados. Documentado en ARQUITECTURA.md sección 5. Build, lint y 137 pruebas en verde.

### 19. Cambio de contraseña de inicio de sesión desde la app
- Finalizada: 2026-07-03
- Resultado: página nueva "Mi cuenta" (`src/features/autenticacion/CuentaPage.tsx`, ruta perezosa `/cuenta` en `src/App.tsx`, chunk propio de 2.7 kB) a la que se llega tocando el nombre del técnico en la cabecera (`src/app/Layout.tsx`, ahora es un enlace con subrayado punteado; muestra "Mi cuenta" si el perfil aún no sincronizó). Permite que cada técnico cambie su propia contraseña de inicio de sesión, ya que la inicial la asigna el administrador al crear la cuenta en Supabase. El formulario pide contraseña actual, nueva y confirmación; antes de cambiar, la contraseña actual se verifica contra Supabase con `signInWithPassword` (evita que alguien que tome un teléfono con la sesión abierta la cambie sin conocerla) y luego se llama `supabase.auth.updateUser`. Función `cambiarContrasena` agregada al contexto de autenticación (`AuthProvider.tsx`, `authContext.ts`). Lógica pura extraída a `src/features/autenticacion/erroresAuth.ts`: `validarCambioContrasena` (actual obligatoria, mínimo 8 caracteres, confirmación igual, nueva distinta de la actual) y `traducirErrorAuth` (antes función privada de AuthProvider; ahora compartida y ampliada con los errores del cambio de contraseña: mínimo de caracteres del servidor conservando el número, contraseña repetida, límite de intentos). Requiere internet; sin conexión muestra el error traducido de red. Documentado en INSTALACION.md (sección "Cambiar tu contraseña"), supabase/INSTRUCCIONES.md (nota en la creación de usuarios) y ARQUITECTURA.md (sección 8). 8 pruebas nuevas (134 en total). Verificado en navegador real con sesión simulada (datos eliminados al terminar): la cabecera muestra el nombre como enlace y navega a /cuenta, las tres validaciones locales muestran su mensaje, y con el formulario válido pero contraseña actual equivocada la app hizo la petición real a Supabase (POST /auth/v1/token devolvió 400) y mostró "La contraseña actual no es correcta."; el camino de éxito no se probó contra el servidor porque exigiría cambiar la contraseña de una cuenta real (queda cubierto por las pruebas unitarias y verificable por el usuario en su primer uso). Build, lint y 134 pruebas en verde.

### 18. Integración de la bóveda con los procedimientos de Soluciones
- Finalizada: 2026-07-03
- Resultado: cada paso de un procedimiento puede vincular una credencial de la bóveda, según lo pedido por el usuario el 2026-07-03: en lugar de escribir usuario y contraseña en el texto del paso, el paso guarda solo el id de la credencial y una copia de su título como referencia; los secretos se consultan cifrados en la bóveda al leer el procedimiento, así nunca se duplican y un cambio de contraseña se hace una sola vez en la bóveda. Sin cambios de esquema en Supabase ni en Dexie: el vínculo viaja dentro del JSON `procedimiento` de `articulos` (campos `credencialId` y `credencialTitulo` en `PasoProcedimiento`, `src/lib/db.ts`). Normalización y limpieza en `src/lib/procedimiento.ts` (un paso que solo tiene credencial vinculada no se descarta; el título de referencia se limpia si queda sin id; el título NO se indexa en la búsqueda, coherente con que la bóveda solo se busca desbloqueada). Editor (`src/features/soluciones/PasosEditor.tsx`): selector "+ Vincular credencial de la bóveda" por paso con las credenciales locales (solo las tienen los usuarios con permiso, por RLS), chip "Credencial vinculada" con "Quitar", y nueva línea en la guía de redacción. Vista (`src/features/soluciones/ProcedimientoVista.tsx` + componente nuevo `src/features/boveda/CredencialEnPaso.tsx`): bloque violeta en el paso expandido con cuatro estados: (1) sin permiso de bóveda o credencial no sincronizada, solo el título de referencia; (2) credencial eliminada de la bóveda, aviso ámbar; (3) bóveda bloqueada, desbloqueo en línea sin salir del procedimiento (misma sesión global, mismo autobloqueo por inactividad); (4) desbloqueada, usuario/contraseña (oculta, con Mostrar)/IP/URL/extras/notas con botones de copiar y enlace "Abrir" a la ficha de la bóveda. Se extrajo `CampoSecreto` + botón de copiar de `CredencialPage.tsx` al componente compartido `src/features/boveda/CampoSecreto.tsx` (ahora con el respaldo de portapapeles de `src/lib/portapapeles.ts`). 5 pruebas nuevas (126 en total). Verificado en navegador real con sesión simulada y datos sembrados (bloque AES-256-GCM real generado con WebCrypto, PBKDF2 600k): contraseña equivocada rechazada, desbloqueo en línea correcto, secretos visibles con Mostrar/Copiar, credencial no sincronizada muestra solo el título de respaldo, recargar la app vuelve a bloquear (claves solo en memoria), vincular y quitar en el editor funcionan, captura de pantalla tomada y sin errores de consola; datos de prueba eliminados al terminar. Documentado en ARQUITECTURA.md (secciones 5 y 8). Build, lint y 126 pruebas en verde.

### 17. Cola de subida para adjuntos offline
- Finalizada: 2026-07-03
- Resultado: se cerró la última excepción del modo offline (adjuntar fotos o manuales exigía internet en ese momento). Módulo nuevo `src/lib/archivosPendientes.ts`: `subirOEncolarArchivo` intenta la subida directa a Storage y, si no hay conexión (o el fallo es de red), guarda el archivo completo en la tabla local `archivosPendientes` (versión 4 de Dexie en `src/lib/db.ts`, Blob incluido) y lo deja disponible de inmediato en el caché offline (`guardarEnCacheOffline` en `src/lib/adjuntosOffline.ts`), así la ficha lo muestra al instante con URL blob sin tocar la red; un rechazo real del servidor estando en línea se propaga a la interfaz como error de subida, igual que antes, para no envenenar la cola. El motor de sincronización (`src/lib/sync.ts`) procesa la cola de archivos ANTES que la cola de cambios (para que la fila del adjunto nunca llegue al servidor sin su archivo), con la misma semántica que las filas: corte de red interrumpe y reintenta después, rechazo del servidor se anota (error, intentos) y no frena a los demás; la subida usa upsert para que un reintento tras un éxito a medias no falle por duplicado. El indicador de la cabecera (`IndicadorSync`) cuenta los archivos pendientes junto con los cambios. Integrado en la galería de adjuntos (`src/components/Adjuntos.tsx`, con aviso ámbar "quedó guardado en este dispositivo y se subirá solo"; eliminar un adjunto también saca su archivo de la cola y del caché) y en las capturas de pasos (`src/features/soluciones/PasosEditor.tsx`). De paso se corrigió un bug latente de `esErrorDeRed` (movida de sync.ts a archivosPendientes.ts): `!navigator.onLine` trataba `undefined` como offline; ahora exige `=== false`. 8 pruebas nuevas (122 en total) con subidor inyectado: orden de subida, corte de red que interrumpe sin perder nada, rechazo anotado que no frena, reintento exitoso que limpia, y clasificación de errores de red. Verificado en navegador real (build de producción, sesión simulada, offline forzado con `navigator.onLine`): foto adjuntada sin conexión visible al instante desde blob local, aviso ámbar, indicador "Sin conexión · 3 cambio(s) por subir" (2 filas + 1 archivo), la foto sobrevive a recargar la app, al "volver la conexión" la sincronización intentó la subida real contra Supabase y anotó el rechazo del token falso ("Invalid Compact JWS") sin perder el archivo con el indicador en rojo, y eliminar el adjunto vació cola, caché y galería. Datos de prueba eliminados al terminar. Build, lint y 122 pruebas en verde.

### 16. Importación masiva de inventario desde Excel/CSV
- Finalizada: 2026-07-03
- Resultado: pantalla "Importar inventario" (`src/features/dispositivos/importar/ImportarDispositivosPage.tsx`, ruta perezosa `/dispositivos/importar` en `src/App.tsx`, botón en `DispositivosPage.tsx`) para cargar el inventario desde .xlsx, .xls o .csv en tres pasos: elegir archivo, revisar (columnas detectadas, filas listas y filas omitidas con su número y motivo) e importar con barra de progreso. Lógica pura y probada sin navegador: `parsearCsv.ts` (detección de separador coma/punto y coma/tabulador fuera de comillas, comillas escapadas, saltos de línea dentro de celdas, BOM, y decodificación UTF-8 con caída a Windows-1252, la codificación de los CSV de Excel en español) y `mapearFilas.ts` (mapeo de encabezados por sinónimos ignorando acentos, mayúsculas y puntuación; columnas desconocidas a "Campos adicionales"; validación de nombre y de categoría contra las de la app; categoría predeterminada elegible para filas sin ella; omisión de duplicados por serial o placa contra la base y dentro del archivo, lo que hace la importación reintentable sin duplicar; plantilla CSV descargable con BOM y punto y coma). Excel vía SheetJS 0.20.3 instalado desde el CDN oficial de SheetJS con hash en el lockfile (la versión de npm, 0.18.5, tiene CVEs conocidos), en `leerArchivo.ts` con import diferido: es un trozo propio de 493 kB (160 kB gzip) que solo se descarga al abrir un .xlsx, precacheado por el service worker (funciona offline); quien usa CSV nunca lo carga. Cada fila se guarda vía `repositorio.ts` con motivo "Importado desde <archivo>": historial y cola de sincronización offline automáticos. 26 pruebas nuevas (114 en total). Verificado en navegador real (build de producción, sesión simulada, datos eliminados al terminar): CSV real en Windows-1252 con punto y coma (tildes intactas de punta a punta), encabezados con sinónimos ("Tipo", "Fabricante", "No. de serie", "Sede"), omisiones correctas (categoría desconocida, fila sin nombre, serial repetido en el archivo), importación con historial y 6 cambios encolados, reimportación del mismo archivo 100 % omitida por duplicados, y un .xlsx real con hoja vacía inicial (se salta), serial numérico (queda como texto) y fila sin categoría que usó la predeterminada del selector. Durante la verificación se detectó y corrigió un bug real: los encabezados con puntuación ("No. de serie") no se reconocían; se agregó `normalizarEncabezado` que ignora puntuación solo en encabezados (los valores no: "SN-100" y "SN100" siguen siendo seriales distintos). Nota de proceso: reaparecieron las navegaciones fantasma de las herramientas de preview (documentadas en la tarea 7); se confirmó con instrumentación de history que el código de la app no navega solo.

### 14. Ping automático para mantener Supabase activo
- Finalizada: 2026-07-03
- Resultado: workflow de GitHub Actions en `.github/workflows/ping-supabase.yml` que consulta la API REST de Supabase (tabla `categorias`, usando solo la clave publishable, pública por diseño y protegida por RLS) los lunes y jueves a las 13:00 UTC (8:00 en Colombia), con margen amplio sobre el límite de 7 días de inactividad del plan gratuito. Si la respuesta no es 200, el workflow falla y GitHub notifica por correo. Incluye disparo manual desde la pestaña Actions (workflow_dispatch) y autoverificación: también se ejecuta cuando cambia su propio archivo (push con filtro de ruta). Documentado en ARQUITECTURA.md sección 10, incluida la salvedad de que GitHub desactiva los workflows programados de repos públicos tras 60 días sin actividad en el repositorio (avisa por correo y se reactivan con un clic en Actions). Verificado con una ejecución real en GitHub Actions disparada por el push del propio workflow: terminó en verde (conclusion=success, consultado por la API pública de GitHub).
- Finalizada: 2026-07-03
- Resultado: los artículos pueden tener un procedimiento guiado que se muestra como lista vertical de pasos numerados y expandibles, según el diseño aprobado el 2026-07-03. Modelo de datos: campo `procedimiento` (jsonb, opcional) en `articulos`, un solo objeto con requisitos previos y pasos (título, detalle, captura, nota, advertencia, consejo y decisión de ramificación con saltos por número de paso); se llamó `procedimiento` y no `pasos` como decía el plan porque agrupa requisitos y pasos juntos, y los identificadores quedaron en español siguiendo la convención real de todo el código. Ubicaciones: tipos en `src/lib/db.ts` (más la tabla local `progresoPasos` en la versión 3 de Dexie), mapeo en `src/lib/tablas.ts`, columna con `add column if not exists` en `supabase/schema.sql`, normalización defensiva del JSON y helpers puros (`normalizarProcedimiento`, `textoDeProcedimiento`, `prepararProcedimientoParaGuardar`, `crearPaso`) en `src/lib/procedimiento.ts`, avance local por técnico en `src/lib/progresoPasos.ts` (marcar/desmarcar, reiniciar, conteo que ignora pasos eliminados). Vista en `src/features/soluciones/ProcedimientoVista.tsx`: bloque "Antes de empezar", pasos colapsados que se expanden al tocarlos, círculo para marcar hecho, barra y contador de progreso con "Reiniciar", decisiones con botones de salto que expanden y hacen scroll al paso destino, y captura por paso servida desde el cache offline o con URL firmada. Editor en `src/features/soluciones/PasosEditor.tsx`: guía de redacción plegable (una acción por paso, verbo imperativo, máximo ~12 pasos, captura solo si la pantalla confunde, cerrar con verificación), requisitos uno por línea, reordenar/eliminar pasos, subir capturas comprimidas a Storage (carpeta `articulos/<id>/pasos/`) y edición de decisiones. `ArticuloForm.tsx` pregenera el id del artículo para poder subir capturas antes del primer guardado y el contenido Markdown pasa a ser opcional cuando hay pasos ("Notas adicionales"); `ArticuloPage.tsx` renderiza la vista. La búsqueda indexa títulos, detalles, avisos y preguntas de los pasos (`useIndiceBusqueda.ts` + `textoDeProcedimiento`). "Descargar todo para offline" (`src/lib/adjuntosOffline.ts`) ahora incluye las capturas de los pasos, que no son filas de adjuntos. De paso se extrajo el hook `src/components/useUrlAdjunto.ts` (antes duplicado dentro de `Adjuntos.tsx`) reutilizado por la galería, la vista y el editor. 17 pruebas nuevas (88 en total). Verificado en navegador real con sesión simulada y datos de prueba (eliminados al terminar): creación desde el formulario con requisitos, 3 pasos, advertencia, consejo y decisión; la vista muestra "Antes de empezar" y los pasos colapsados; expandir el paso 2 muestra advertencia y pregunta con botones "Sí: ir al paso 3" / "No: ir al paso 3"; el salto expande el paso 3; marcar el paso 1 deja "1 de 3 pasos completados" con "Reiniciar progreso" y el avance sobrevive a recargar la app; buscar "back up" encuentra el artículo aunque el término solo exista en un paso. La captura de pantalla del entorno de pruebas volvió a fallar (limitación conocida del entorno, no del código); la parte visual se verificó inspeccionando clases y estilos calculados en el DOM. Build, lint y 88 pruebas en verde.
- Pendiente (usuario): reejecutar `supabase/schema.sql` completo en el SQL Editor (es idempotente) para que exista la columna `procedimiento` en el servidor; quedó anotado en la tarea 2.

### 12. Escáner QR para el inventario
- Finalizada: 2026-07-02
- Resultado: identificar un dispositivo apuntándole con la cámara, en dos partes. (a) Pantalla de escaneo a pantalla completa en `src/features/escaner/EscanerPage.tsx` (ruta `/escaner`, fuera del Layout para no mostrar la barra inferior): cámara trasera (`facingMode: environment`, `playsInline` para iPhone), detección con la API nativa BarcodeDetector donde exista (Android: QR y códigos de barras code_128, code_39, EAN, UPC, ITF, intersectados con los formatos que el navegador soporte) y respaldo jsQR (solo QR, para iPhone) importado bajo demanda; bucle de detección cada 200 ms sobre un cuadro reducido a 640 px; botón de linterna si la cámara la ofrece (`getCapabilities().torch`); vibración al detectar; al leer un código abre la ficha del dispositivo. Si la cámara falla (sin permiso, sin cámara o navegador sin soporte) muestra el motivo y un campo para escribir la placa o serial a mano, que usa la misma lógica de resolución. (b) Etiquetas QR imprimibles en `src/features/dispositivos/EtiquetasPage.tsx` (ruta `/dispositivos/etiquetas`, también fuera del Layout): filtro por categoría, cada etiqueta lleva el QR con la URL absoluta de la ficha (también la entiende la cámara normal del teléfono), nombre, placa o serial y ubicación; botón Imprimir con estilos de impresión (utilidades `print:` de Tailwind más `@page` en `src/index.css`, etiquetas con `break-inside-avoid` en rejilla de 3 por fila). La decisión de qué hacer con un código leído es lógica pura en `src/features/escaner/resolverCodigo.ts`: acepta URLs de etiqueta de cualquier origen (siguen valiendo si el dominio cambia), placas de inventario y seriales (sin distinguir mayúsculas ni espacios; la placa tiene prioridad sobre el serial por ser la etiqueta propia del equipo), valida que el dispositivo exista y no esté eliminado, y devuelve "varios" si más de uno comparte el código (la pantalla ofrece elegir). 11 pruebas nuevas (71 en total) sobre esa lógica. Rutas y carga diferida en `src/App.tsx`; accesos en `DispositivosPage.tsx` (Escanear código y Etiquetas QR) y en `InicioPage.tsx` (Escanear código de un equipo). Nada pesa en el arranque: el escáner (8 kB), jsQR (130 kB, solo si no hay detector nativo) y las etiquetas con la librería qrcode (27 kB) van en trozos propios precacheados por el service worker (funcionan offline). Verificado en navegador real con sesión simulada y datos de prueba (eliminados al terminar): los QR generados decodifican exactamente a la URL de la ficha (comprobado decodificándolos con jsQR en el propio navegador), la búsqueda manual por placa en minúsculas abre la ficha correcta, el código desconocido muestra el aviso con la opción de registrar el dispositivo, y el fallo de cámara muestra el mensaje y el campo manual. Build, lint y 71 pruebas en verde. Pendiente de los pasos de usuario ya anotados en la tarea 10: probar la cámara real en los teléfonos del equipo (Android con códigos de barras, iPhone solo QR).

### 11. Mejoras rápidas de uso diario
- Finalizada: 2026-07-02
- Resultado: paquete de seis mejoras, todas locales y sin peso en el arranque. (1) Recientes reales en Inicio: tabla `recientes` en la versión 2 de Dexie (`src/lib/db.ts`; migración desde la v1 instalada verificada en navegador real), lógica en `src/lib/recientes.ts` (registro al abrir artículos/dispositivos, máximo 20, omite fichas eliminadas o inexistentes) con 4 pruebas nuevas (60 en total), lista en `InicioPage.tsx`. (2) Indicador de sincronización en la cabecera (`src/components/IndicadorSync.tsx`, integrado en `src/app/Layout.tsx`): verde al día, ámbar con cambios por subir o sincronizando, rojo con errores, gris sin conexión; muestra el número de pendientes y al tocarlo fuerza una sincronización. (3) Valores copiables con un toque en la ficha del dispositivo (`src/components/ValorCopiable.tsx`, con respaldo `execCommand` en `src/lib/portapapeles.ts` para navegadores sin Clipboard API) y enlace "Abrir" en la IP hacia `http://<ip>`; el copiado no pudo verificarse de punta a punta en el navegador de pruebas porque la Clipboard API exige una ventana con foco real (limitación del entorno headless, no del código; el enlace Abrir y el render sí quedaron verificados). (4) Adjuntos con botón "Cámara" (`capture="environment"`) y "+ Archivos" (selección múltiple, subida secuencial con progreso "Subiendo x de y" y errores por archivo sin frenar el resto), atributos verificados en el DOM. (5) "Duplicar" en la ficha del dispositivo (`/dispositivos/nuevo?copiarDe=<id>` en `DispositivoForm.tsx`): copia categoría, marca, modelo, ubicación, estado, observaciones y campos adicionales; deja en blanco serial, placa e IP; nombre queda "... (copia)"; verificado en navegador; si la ficha origen no existe, sigue con el formulario vacío en vez de quedarse cargando. (6) "Compartir" en artículos y dispositivos (`src/components/BotonCompartir.tsx`): Web Share API con respaldo de copiar el enlace. Las filas de acciones de ambas fichas pasaron a su propia línea (con 4 botones ya no cabían junto al título en 375 px). Build, lint y 60 pruebas en verde.

### 8. Visor de historial de cambios
- Finalizada: 2026-07-02
- Resultado: componente reutilizable `Historial` en `src/features/historial/Historial.tsx`, integrado en `ArticuloPage.tsx`, `DispositivoPage.tsx` y `CredencialPage.tsx`. Consulta el índice compuesto `[entidadTipo+entidadId]` ya existente en `src/lib/db.ts`, ordena por fecha descendente y se mantiene reactivo con `dexie-react-hooks`. Sección plegable (colapsada por defecto) con contador de entradas; cada entrada muestra usuario, fecha y hora localizadas, una descripción en español del cambio y el motivo si se indicó. Traducción de nombres de campo e interpretación de cada tipo de entrada (creación, eliminación, adjunto agregado, cambio de campo, definición inicial, campo vaciado) separadas en funciones puras en `src/features/historial/textoHistorial.ts` para poder probarlas sin React ni base local; 9 pruebas nuevas (42 en total, todas en verde), incluida una que confirma que el historial de credenciales nunca expone el bloque cifrado, solo el marcador "(cifrado)" ya usado por `repositorio.ts`. Verificado en navegador real con datos de prueba: historial de un dispositivo con 3 entradas (creación + 2 ediciones con motivo), historial de una credencial mostrando "Datos protegidos: (cifrado) → (cifrado)" sin exponer la contraseña real, y un artículo sin cambios mostrando "Sin cambios registrados"; los datos de prueba se eliminaron al terminar. Build y lint sin errores.

### 9. Bóveda de IP y credenciales
- Finalizada: 2026-07-02
- Resultado: capa de cifrado en `src/lib/crypto.ts` (WebCrypto: AES-256-GCM con clave derivada por PBKDF2-SHA256, 600.000 iteraciones según OWASP; formato de bloque versionado `v1.<iteraciones>.<salt>.<iv>.<datos>` para poder subir los parámetros sin invalidar lo guardado). Sesión de la bóveda en `src/features/boveda/sesionBoveda.ts`: guarda las claves derivadas solo en memoria (la contraseña se descarta tras derivar), verifica la contraseña descifrando un bloque representativo por cada salt (si un bloque se cifró con otra contraseña queda como "no descifrable" pero no impide desbloquear el resto), y autobloqueo por inactividad configurable (1/5/15/30 min, por defecto 5, persistido en localStorage; se reinicia con actividad de puntero o teclado). Acceso restringido en `BovedaGuard.tsx`: exige `perfil.puedeVerBoveda` (leído en vivo de la base local) y bóveda desbloqueada; con la bóveda vacía pide confirmar la contraseña porque esa primera contraseña queda como maestra del equipo. Pantallas: lista con filtro por categoría y botón Bloquear (`BovedaPage.tsx`), ficha con contraseña enmascarada, botones Mostrar/Copiar por campo y aviso si el bloque no se puede descifrar (`CredencialPage.tsx`), formulario con generador de contraseñas y campos adicionales cifrados (`CredencialForm.tsx`). Rutas anidadas en `src/App.tsx`. Búsqueda global: las credenciales entran al índice solo por título y categoría y solo con la bóveda desbloqueada (`useIndiceBusqueda.ts`, grupo "Bóveda" en `ResultadosBusqueda.tsx`). 12 pruebas nuevas (33 en total, todas en verde) cubren ida y vuelta del cifrado, contraseña incorrecta, integridad (bloque alterado), formatos inválidos, salts mixtos y bloqueo de sesión. Verificado en navegador real con una sesión local simulada (el backend aún no tiene usuarios, tarea 2): desbloqueo inicial con confirmación, creación de credencial, bloque cifrado real en IndexedDB sin texto plano, historial con "(cifrado)", Mostrar/Copiar, bloqueo, rechazo de contraseña incorrecta, y aparición/desaparición en la búsqueda global; los datos de prueba se eliminaron al terminar. Pendiente para cuando se complete la tarea 2: probar con usuarios reales la política RLS de la bóveda y la sincronización de credenciales entre teléfonos.

### Diseño de la arquitectura y del plan del proyecto
- Finalizada: 2026-07-02
- Resultado: propuesta técnica completa en ARQUITECTURA.md, tablero de tareas inicial en TAREAS.md, reglas de trabajo en REGLAS.md y contexto del proyecto en CLAUDE.md.

### Validación de la propuesta de arquitectura
- Finalizada: 2026-07-02
- Resultado: usuario aprobó la propuesta sin cambios.

### 7. Módulo Dispositivos
- Finalizada: 2026-07-02
- Resultado: inventario en `DispositivosPage.tsx` con filtros combinables por categoría, estado y ubicación (el listado de estados se arma dinámicamente a partir de los valores en uso). Ficha de dispositivo en `DispositivoPage.tsx` con los campos fijos del modelo, campos dinámicos libres (par clave/valor, sección "Campos adicionales" en vez de una plantilla fija por categoría, ver ARQUITECTURA.md sección 5), enlace a los procedimientos de su categoría en Soluciones, y adjuntos reutilizando `src/components/Adjuntos.tsx`. Formulario único de creación/edición en `DispositivoForm.tsx` con campo de motivo, siguiendo el mismo patrón que `ArticuloForm.tsx`. Toda escritura pasa por `src/lib/repositorio.ts` (historial y cola offline automáticos). Rutas nuevas en `src/App.tsx`. Se actualizó `src/features/busqueda/useIndiceBusqueda.ts` para que los resultados de dispositivos enlacen directo a su ficha (antes iban a la lista general, limitación ya resuelta). Verificado en navegador real: creación con campos dinámicos, edición con cambio de categoría y motivo registrado en el historial, borrado suave, los tres filtros combinados, y búsqueda global enlazando a la ficha correcta. Nota de proceso: durante la verificación, la herramienta `preview_fill` produjo una navegación espuria que parecía un bug de la app; se aisló repitiendo la prueba con manipulación nativa del DOM vía `eval`, confirmando que el formulario funciona correctamente y que fue un artefacto de la herramienta, no del código. Build, lint y las 21 pruebas en verde.

### 6. Búsqueda global
- Finalizada: 2026-07-02
- Resultado: índice MiniSearch en memoria (`src/features/busqueda/useIndiceBusqueda.ts`) construido de forma reactiva a partir de artículos y dispositivos locales (`dexie-react-hooks`), con búsqueda por prefijo y tolerante a errores de escritura (fuzzy 0.2) y el título con más peso que el contenido. Función pura `crearIndiceDesdeDocumentos` separada del hook de React para poder probarla; 7 pruebas nuevas con Vitest cubren coincidencias por contenido, errores de escritura ("epsom" encuentra Epson), prefijos, dispositivos por marca/ubicación, prioridad del título y consulta vacía. Resultados agrupados por tipo (Soluciones/Dispositivos) en `ResultadosBusqueda.tsx`, integrados en la pantalla de Inicio: mientras se escribe se reemplaza la sección "Recientes" por los resultados. Verificado en navegador real con datos de prueba: "epsom" encontró "Epson", "zebr" encontró un artículo donde "Zebra" solo aparece en el contenido, y "bodega" encontró un dispositivo por ubicación. Los resultados de tipo dispositivo enlazan por ahora a la lista `/dispositivos` porque su ficha individual aún no existe (la creará la tarea 7); queda anotado en el tablero para actualizar el enlace cuando esa ficha exista. Build, lint y las 21 pruebas totales en verde.

### 5. Módulo Soluciones
- Finalizada: 2026-07-02
- Resultado: categorías dinámicas desde la base local (`SolucionesPage.tsx`), vista de categoría con artículos agrupados por tipo (`CategoriaPage.tsx`), vista de artículo con contenido Markdown renderizado vía `react-markdown` + `remark-gfm` y estilos de `@tailwindcss/typography` (`ArticuloPage.tsx`), formulario único de creación/edición con campo de motivo (`ArticuloForm.tsx`), componente reutilizable de adjuntos con subida a Supabase Storage y URLs firmadas (`src/components/Adjuntos.tsx`, pensado para reutilizarse en dispositivos). Toda escritura pasa por `src/lib/repositorio.ts` (historial y cola offline automáticos). Rutas anidadas en `src/App.tsx`. Corrección de UX durante la prueba: el enlace "volver" en la ficha de artículo mostraba el nombre del tipo pero navegaba a la categoría completa; ahora muestra el nombre real de la categoría. Verificado en navegador real (con la protección de rutas omitida temporalmente para la prueba y restaurada después, confirmado sin diff): creación, renderizado de Markdown (títulos, listas, negritas, enlaces), edición con y sin cambios reales (confirma que no se genera historial innecesario), historial con motivo, y eliminación con borrado suave. Limitación conocida: la subida de adjuntos requiere conexión a internet en el momento (no se encola offline); documentado en ARQUITECTURA.md. Build, lint y las 14 pruebas existentes siguen en verde.

### 4. Autenticación en la app
- Finalizada: 2026-07-02
- Resultado: contexto de sesión en `src/features/autenticacion/authContext.ts` y `AuthProvider.tsx` (sesión persistente vía Supabase Auth, perfil local, sincronización automática al iniciar sesión). Pantalla de login en `LoginPage.tsx` con validación y mensajes de error traducidos al español; redirige a Inicio si ya hay sesión. Protección de rutas en `RequireAuth.tsx`: las 4 pantallas internas redirigen a `/login` sin sesión (verificado en navegador). Botón "Cerrar sesión" y nombre del perfil en `src/app/Layout.tsx`. No se borra la base local al cerrar sesión para no perder cambios sin sincronizar. Probado en navegador contra el proyecto real de Supabase (login con credenciales inválidas devolvió el error esperado en español). Login con usuario real queda pendiente de que se complete la tarea 2. 14 pruebas existentes siguen pasando; build y lint sin errores ni warnings.

### 3. Capa de datos local y sincronización
- Finalizada: 2026-07-02
- Resultado: base local Dexie completa (`src/lib/db.ts`) con perfiles, borrado suave, cola de cambios y metadatos de sync. Mapeo local/remoto en `src/lib/tablas.ts`. Punto único de escritura en `src/lib/repositorio.ts`: guarda en local, registra historial automático por campo (con motivo y usuario) y encola la subida agrupando ediciones repetidas. Motor de sincronización en `src/lib/sync.ts`: subida de la cola en orden con manejo de errores por cambio, descarga incremental por cursor con margen de 5 minutos, protección de cambios locales pendientes al aplicar filas remotas y estado observable para la interfaz. Se agregó la columna `recibido_en` al historial en `supabase/schema.sql` para que los cambios hechos offline se propaguen bien. 14 pruebas unitarias con Vitest (una detectó y permitió corregir un error real: los cambios de credenciales no quedaban en el historial por comparar valores enmascarados). Verificado en navegador real: creación, edición con historial por campo y cola agrupada.

### 1. Scaffold del proyecto
- Finalizada: 2026-07-02
- Resultado: proyecto creado con Vite + React 19 + TypeScript (modo estricto) + Tailwind CSS 4 + vite-plugin-pwa. Repositorio git inicializado (sin commit inicial). Estructura de carpetas según ARQUITECTURA.md sección 11 (`src/app`, `src/features/{inicio,soluciones,dispositivos,boveda}`, `src/lib`, `src/components`). Navegación inferior con las 4 secciones funcionando (verificado en vista previa móvil: build, lint y navegación entre pestañas sin errores). Esquema local en `src/lib/db.ts` (Dexie) con las entidades de ARQUITECTURA.md sección 5. Cliente de Supabase en `src/lib/supabase.ts` a la espera de credenciales. Icono de la app es un placeholder de texto ("IT"); el diseño final queda pendiente para la tarea 9.
