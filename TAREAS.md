# Tablero de tareas

Reglas del tablero: solo puede haber una tarea "En proceso" a la vez. Las tareas terminadas se mueven a [TAREAS_ARCHIVO.md](TAREAS_ARCHIVO.md). Cada tarea indica su ubicación exacta dentro del proyecto.

## En proceso

Sin tarea en desarrollo. La tarea 88 (Ubicaciones re-autorizada en Nocturne) quedó terminada, verificada en navegador y archivada el 2026-07-18. Por decisión del usuario (2026-07-18) se están re-autorizando en orden las pantallas de la carpeta del handoff (regla 15); ya van Ficha (86), Editor (87) y Ubicaciones (88, sus 4 pantallas). Las siguientes son la tarea 89 (Etiquetas QR) y 90 (Importar Dispositivos); ver "Por hacer". La 89 es la siguiente en desarrollarse.

La tarea 85 (rediseño IT Brain: pantalla Dispositivos re-autorizada en Nocturne) quedó terminada, verificada en navegador y archivada el 2026-07-18. Llegó por el handoff "Rediseño de aplicación empresarial-handoff.zip" (nueva exportación con la entrada R4), README apuntando a `Dispositivos.dc.html`. Se reescribió `src/features/dispositivos/DispositivosPage.tsx` del tema claro heredado (zinc/blue-600 + `AppShell`) a Nocturne con `ShellNocturne`, fiel al mockup, conservando intacta toda la lógica y los datos (consultas Dexie en vivo, exclusión de categorías de red, filtrado por categoría+texto, resumen de estados sobre el total, rutas reales). Nuevo aspecto: cabecera pegajosa (título "Dispositivos" + subtítulo), acciones Escanear/Crear/menú "···" (que despliega Ubicaciones, Etiquetas QR e Importar), buscador único con borde de acento, chips de categoría deslizables con conteo (incluye "Todos"), resumen con puntos de color y filas de 56px con miniatura/icono, "categoría · ubicación" y estado (punto+etiqueta del color del estado) sobre la IP en monoespaciado. Helper local `claseEstado` para el color Nocturne del estado (no se tocó `estadoConEtiqueta`, compartido con Red). Se añadieron 2 iconos (`MapPin`, `UploadSimple`; ahora 76) vía el generador. La ruta ya vivía fuera del `Layout`, solo cambió el shell. 2015 pruebas, oxlint y build en verde. Verificado end-to-end en navegador (sesión simulada, 8 equipos generales de 9 sembrados, colores Nocturne exactos, menú/chips/búsqueda/estado vacío). Con esto de la carpeta del handoff queda pendiente `Ficha de Dispositivo.dc.html` (tarea 86).

La tarea 84 (responsive de Inicio y Soluciones: shell + rejillas + rail de filtros) quedó terminada, verificada en navegador en 4 tamaños y archivada el 2026-07-18. Causa raíz común: `ShellNocturne` solo tenía un salto (columna de 448px hasta 1024px, luego 816px), así que las tablets recibían la interfaz de teléfono. Cambios, sin tocar lógica ni datos (solo clases y envoltorios de layout): (1) `ShellNocturne` ancho progresivo `max-w-md sm:max-w-xl md:max-w-3xl lg:max-w-[1040px] 2xl:max-w-[1240px]`. (2) `InicioPage` con `@container`: Recientes | Para empezar a 2 columnas en ancho (`@2xl:grid-cols-2`, solo si hay rutas), resultados de búsqueda a 2 columnas; atajos siguen 2 columnas. (3) `SolucionesPage`: rejilla de resultados por container query (`@lg:grid-cols-2 @4xl:grid-cols-3`) y en escritorio (`xl`) rail de categorías vertical siempre visible + subfiltro por tipo, ocultando los chips horizontales (`xl:hidden`); el rail no aparece al buscar. Decisiones del usuario (2026-07-18): alcance "shell + rejillas" (el formulario a 2 columnas con vista previa queda fuera) y Soluciones escritorio con "rail + rejilla". Verificado por medición de estilos computados: móvil 375 (1 columna, chips, sin scroll horizontal), tablet 768 (rejilla 2 columnas + chips, sin sidebar/rail), escritorio 1280 (sidebar + rail de 4 categorías + rejilla 2 columnas, contenido 1040px), e Inicio con dashboard 2 columnas en ancho y apilado en móvil. 450 pruebas, oxlint y build en verde, sin errores de consola. Recomendación de a11y pendiente (no implementada): migrar tamaños `text-[15px]` a `rem` para el escalado de texto del sistema. Queda como mejora futura extender el mismo patrón responsive al resto de pantallas Nocturne y el formulario a 2 columnas.

La tarea 83 (rediseño IT Brain: Editor de Diagnóstico re-autorizado en Nocturne) quedó terminada, verificada en navegador y archivada el 2026-07-18. Era la única `.dc.html` de la carpeta del handoff "Rediseño de aplicación empresarial" que faltaba (el editor del árbol, distinto de `Diagnóstico.dc.html` de la tarea 81). Se reescribió `src/features/diagnostico/DiagnosticoForm.tsx` del tema slate heredado a Nocturne fiel al mockup (cabecera pegajosa con Cancelar/Eliminar, chips de categoría desde la BD con `iconoDeCategoria`, tarjetas de pregunta con menú "···" plegable, respuestas con destino/mensaje final/procedimiento vinculado por selector inline, barra inferior fija Probar/Guardar), conservando intactos el modelo `nodos`/`opciones` y toda la lógica (validación de ramas/ciclos/inalcanzables, duplicar/mover, aviso de vínculos rotos, eliminación sensible con contraseña maestra, historial en edición). Se recoloreó `PruebaDiagnostico.tsx` a Nocturne. Las rutas `diagnostico/nuevo` y `:id/editar` salieron del `Layout`. Se añadieron 2 iconos (Signpost, FlagCheckered; ahora 74) vía el generador. Verificado end-to-end: nuevo y edición (carga de datos, chip activo, destino "Sigue en la pregunta N", chip Ejecuta), validación, menú, selector de procedimiento y modo prueba, sin errores de consola. 450 pruebas, oxlint y build en verde. Con esto la carpeta del handoff "Rediseño de aplicación empresarial" queda COMPLETA de verdad. Pendientes de re-autoría Nocturne (otros handoffs/pendientes): Dispositivos, Ficha de Dispositivo, Red, Topología (tema claro heredado), CategoriaPage (tarea 77) y Bóveda.

La tarea 82 (rediseño IT Brain: pantalla Escanear equipo re-autorizada en Nocturne) quedó terminada, verificada en navegador y archivada el 2026-07-18. Se hizo al aplicar la nueva regla 15 (al autorizar un handoff se implementan TODAS las pantallas de la carpeta, no solo la del README): `Escanear Equipo.dc.html` era la única `.dc.html` de la carpeta del handoff "Rediseño de aplicación empresarial" que faltaba. Se reescribió `src/features/escaner/EscanerPage.tsx` del tema slate a Nocturne conservando intacta la lógica de cámara/detector (BarcodeDetector + jsQR, linterna, `resolverCodigo`): fondo con degradado radial, marco de escaneo con esquinas en acento y línea de barrido animada (`@keyframes esc-linea` en `index.css`), pastilla "Linterna", y búsqueda manual SIEMPRE visible en la barra inferior. Cambio fiel al diseño: un único equipo encontrado ahora muestra la tarjeta "Equipo identificado" (Abrir la ficha / Seguir) en vez de saltar directo a la ficha; se conservan las tarjetas "Varios equipos" y "Ningún equipo coincide". "Volver" sube a Inicio (se declaró `/escaner` en `navegacion.ts`, regla 13). Se añadieron 4 iconos (ArrowRight, CameraSlash, Flashlight, FlashlightFill; ahora 72). 450 pruebas (1 nueva), oxlint y build en verde. Con esto, la carpeta del handoff "Rediseño de aplicación empresarial" queda COMPLETA (Inicio, Editor, Diagnóstico, Escanear equipo; Ficha de Procedimiento y Soluciones ya venían de antes). Del orden D-008 quedan pendientes de re-autoría Nocturne pantallas de otros handoffs/pendientes: Dispositivos, Ficha de Dispositivo, Red y Topología (hoy en tema claro heredado), la CategoriaPage (tarea 77) y la Bóveda.

La tarea 81 (rediseño IT Brain: Modo Diagnóstico Inteligente re-autorizado en Nocturne) quedó terminada, verificada en navegador y archivada el 2026-07-18. Llegó por el mismo handoff que Inicio y el Editor ("Rediseño de aplicación empresarial-handoff.zip"), esta vez con el README apuntando a `Diagnóstico.dc.html`: la siguiente pantalla del orden D-008 tras Inicio. Se reescribieron `src/features/diagnostico/DiagnosticosPage.tsx` (lista de problemas agrupada por categoría, buscador, tarjeta "en curso", estados vacíos) y `src/features/diagnostico/DiagnosticoRunPage.tsx` (sesión guiada: pregunta con opciones, ejecución de procedimiento con `AsistenteVista` embebido, resultado con "camino recorrido" y retroalimentación de motivos) del tema oscuro slate heredado a Nocturne, fieles al mockup y cableadas a los datos reales (Dexie: `diagnosticos`, `progresoDiagnostico`, `ejecuciones_diagnostico`). Decisión del usuario (2026-07-18): tocar un problema entra DIRECTO a la primera pregunta (auto-inicio, sin pantalla "Comenzar"); "Salir" sin responder descarta la sesión para no dejar "en curso" fantasma. Se conservaron accesos que el mockup no modela (editar el diagnóstico con icono lápiz en la sesión, "Sugerencias del equipo", banner de filtro por `?categoria=` para el deep-link desde la ficha de un equipo). La lista `/diagnostico` salió del `Layout` oscuro (trae su propio shell Nocturne). Se añadieron 4 iconos Phosphor (ArrowLeft, XCircle, ListPlus, DotsThreeCircle; ahora 68) y la primitiva `BTN_GHOST`. 449 pruebas, oxlint y build en verde. Del orden D-008 quedan pendientes de re-autoría Nocturne: Dispositivos, Ficha de Dispositivo, Red y Topología (hoy en tema claro heredado), más la CategoriaPage (tarea 77) y la Bóveda.

La tarea 80 (rediseño IT Brain: pantalla de Inicio re-autorizada en Nocturne) quedó terminada, verificada en navegador y archivada el 2026-07-18. Llegó por el mismo handoff que el Editor (tarea 71), "Rediseño de aplicación empresarial-handoff.zip", cuyo README apuntaba a `Inicio.dc.html`: era la siguiente pantalla del orden D-008 tras Soluciones. Se reescribió `src/features/inicio/InicioPage.tsx` del tema oscuro slate heredado a Nocturne (cabecera con marca "IT Brain" + saludo por hora, pastilla de sincronización, buscador global; búsqueda agrupada por fuente con término resaltado; en inicio, tarjeta "Continuar donde quedaste", 2 atajos, Recientes y "Para empezar"). Todo cableado a datos reales (buscador, recientes, rutas de inicio, estado de sync en vivo con PanelSync); la tarjeta "Continuar donde quedaste" es NUEVA y se deriva de `progresoPasos` (procedimiento a medias más reciente, barra + "Paso X de Y"). La ruta índice salió del `Layout` oscuro (trae su propio `ShellNocturne`). Se añadieron 5 iconos Phosphor y, al hacerlo, se detectó y corrigió que `scripts/generar-iconos.mjs` estaba desincronizado del archivo (13 iconos añadidos a mano en tareas 63/71 faltaban en el mapa del generador, así que regenerar los borraba): se agregaron los 13 y el generador vuelve a ser fuente de verdad completa (64 iconos). 449 pruebas, oxlint y build en verde. Con esto, del orden D-008 quedan pendientes de re-autoría Nocturne: Dispositivos, Ficha de Dispositivo, Red y Topología (hoy en tema claro heredado), más la CategoriaPage (tarea 77) y la Bóveda.

La tarea 78 (Modo Asistente: arreglar "pantalla en blanco" y rediseñar en Nocturne, Fases 0 y 1) quedó terminada, verificada en navegador y archivada el 2026-07-18. La pantalla en blanco NO era bug de enrutado/datos/implementación (el contenido renderizaba sin errores de consola): era un defecto de contraste, porque `AsistentePage`/`AsistenteVista` vivían fuera del `Layout` y usaban texto claro sobre fondo transparente (blanco). Se rediseñó en Nocturne (shell oscuro, contraste correcto verificado: fondo `#161826`, texto `#e9e9ed`), se reposicionó la entrada ("Asistente" → "Ejecutar") y se agregó valor exclusivo de ejecución: cronómetro contra el tiempo estimado, navegación Atrás/Siguiente explícita (con "Siguiente paso" contextual al revisar un paso ya completo) y resumen final con tiempo. Sin cambios de esquema (el progreso sigue en `progresoPasos`; el motor `useProcedimientoEjecucion` no se tocó). La captura de evidencia por paso (Fase 2) queda como tarea 79. Ver el archivo para el detalle y la verificación end-to-end.

La tarea 76 (navegación "Volver" definitiva: fuente única de la jerarquía) quedó terminada, verificada en navegador y archivada el 2026-07-18. Misma causa raíz que la tarea 75, ahora en el botón de regreso de la ficha de artículo (`ArticuloPage.tsx`) y en otras pantallas del subárbol de Soluciones: apuntaban a `/soluciones/:categoriaId` (la `CategoriaPage` de tema claro heredado) en vez de a la lista Nocturne, aterrizando en "una pantalla antigua". Se creó `src/lib/navegacion.ts` (`padreDe`, fuente única de la jerarquía "Up", 19 pruebas) y se refactorizó `BotonVolver` para derivar destino y etiqueta de ahí (variantes claro/nocturne, overrides para es_red y etiquetas "Salir"/"Cancelar"). Se migraron las ~17 pantallas con "Volver" al componente unificado; ninguna cablea ya su destino. Decisión del usuario (2026-07-18): la categoría es un FILTRO de la lista, no una pantalla, así que Volver desde una solución regresa a `/soluciones?categoria=<id>`. La `CategoriaPage` se conserva (ficha 360° desde buscador y ficha de dispositivo); su migración a Nocturne queda pendiente (tarea 77). Regla 13 de REGLAS.md consolidada y nota nueva en ARQUITECTURA.md.

La tarea 75 (Cancelar del Editor de Artículo aterrizaba en una pantalla intermedia heredada) quedó terminada, verificada en navegador y archivada el 2026-07-18. En Soluciones, al filtrar por categoría, pulsar "+ Crear" y luego "Cancelar", la app iba a `/soluciones/:categoriaId` (la `CategoriaPage`, ficha de categoría del tema claro que la lista Nocturne ya reemplazó) en vez de a la lista principal `/soluciones`, obligando a re-pulsar "Soluciones". Se corrigió el destino de Cancelar en modo creación a `/soluciones?categoria=${categoriaId}` y se sembró el chip activo desde `?categoria=` en `SolucionesPage`, para volver a la lista con el mismo filtro y sin pantallas intermedias. Se revisó el resto de formularios (todos ya correctos) y se registró la estrategia unificada de Cancelar/Volver en REGLAS.md (regla 13).

Sin tarea en desarrollo. Las siguientes pendientes son pasos del usuario o propuestas por decidir (ver "Por hacer").

La tarea 73 (color propio por tipo de documento en Soluciones) quedó terminada, verificada en navegador y archivada el 2026-07-18. El usuario reportó que Instalación, Configuración y Conexión seguían todas moradas (compartían el acento) y no se distinguían. Se les dio a los tres tipos operativos un color propio: instalación rosa/magenta `oklch(0.72 0.14 330)`, configuración cian `oklch(0.78 0.1 195)`, conexión azul `oklch(0.7 0.13 255)` (tokens `--color-noct-tipo-*` en `src/index.css`). Problema (ámbar/precaución), mantenimiento (verde/éxito) y manual (neutro) se conservan. `iconosSoluciones.ts` actualizó `TONO_POR_TIPO` (recuadro de icono en la lista y el buscador) y ganó `colorIconoDeTipo` (color de icono solo) para la rejilla de tipos del editor, que dejó de fijar el acento. Verificado por estilos computados: los 6 tipos con matices distintos en la lista y en el editor.

La tarea 72 (cablear los placeholders del Editor de Artículo) quedó terminada, verificada en navegador y archivada el 2026-07-18. Alcance decidido por el usuario: solo los "Vínculos del paso" (bóveda, procedimiento y solución) y "Artículos relacionados"; las 4 funciones ocultas por el mockup (equipos donde aplica, adjuntos de paso, orden en ruta de inicio, cambio mayor) quedan tal cual, sin cablear (tarea 73 en "Por hacer"). En `PasosEditor.tsx` se agregaron consultas de credenciales y artículos vinculables (mismo criterio que el editor anterior: publicados con procedimiento, excluyendo el propio artículo) y un componente `VinculoDelPaso` reutilizable: sin vínculo, un select punteado con icono superpuesto (mismo aspecto que el mockup); vinculado, una fila sólida con el título de referencia y "Quitar". También se repuso la forma de AGREGAR el vínculo "Si responde No" de una tarea de decisión (antes solo se podía quitar). En `ArticuloForm.tsx` se cableó "Artículos relacionados" con el mismo patrón de chips + select que ya usan Etiquetas y Dispositivos afectados. Verificado en navegador: los 4 selectores aparecen con las opciones correctas, vincular convierte el select en fila sólida, "Quitar" lo revierte, y el guardado persiste todo (credencial, subprocedimiento, solución, vínculo de decisión y relacionados) confirmado leyendo Dexie directamente.

La tarea 71 (rediseño Nocturne del Editor de Artículo) quedó terminada, verificada en navegador y archivada el 2026-07-18. Llegó un handoff nuevo ("Rediseño de aplicación empresarial-handoff.zip") cuyo README apuntaba a `Editor de Artículo.dc.html`. Se reescribieron `src/features/soluciones/ArticuloForm.tsx` y `src/features/soluciones/PasosEditor.tsx` al sistema Nocturne, fieles al mockup: cabecera pegajosa (Cancelar, estado, tipo dinámico, "Se guarda en la categoría X"), rejilla de 6 tipos con icono coloreado por tono, tarjeta de similares, oferta de plantilla, tarjetas de paso (número, menú subir/bajar/eliminar, objetivo, bloques de tarea con icono que cicla acción/verificación/decisión, advertencia con icono que cicla tono, imagen con slot de subida), secciones plegables "Detalles" y "Publicación", y barra inferior fija con completitud + sugerencias + Vista previa/Guardar. Las rutas `nuevo` y `:articuloId/editar` salieron del `Layout` oscuro (traen su propio shell a pantalla completa, como la ficha). Se añadieron 12 iconos Phosphor a `iconos.tsx` (ArrowUp/Down, ArrowElbowDownRight, Camera, DotsThreeOutline, FloppyDisk, LinkSimple, Question, SealCheck, Sparkle, Square, X). La completitud usa la fórmula del mockup (10 señales del formulario, reacciona sin pasos). Por decisión del usuario ("tal cual como se encuentra en la carpeta"), los "Vínculos del paso" (bóveda/procedimiento/solución) y "Artículos relacionados" quedan como placeholders no cableados, y el editor ya no muestra equipos donde aplica (`dispositivosAfectados`), adjuntos por paso, orden en la ruta de inicio ni "cambio mayor". CLAVE: esos campos NO se pierden, se conservan tal cual al guardar (verificado en navegador: editar un artículo con dispositivos/relacionados/orden/adjuntos/vínculos de paso los preserva y sube la versión 1.3 -> 1.4). Los "Vínculos del paso" y "Artículos relacionados" quedaron cableados en la tarea 72.

La tarea 70 (grupo de esquema N3: ubicación como entidad, vínculo credencial↔dispositivo, orden de rutas de inicio, tipo 'relacionado' en conexiones y columna color en categorías) quedó terminada, verificada en navegador y archivada el 2026-07-17. Era el único grupo con esquema que restaba de [PROPUESTA_BASE_CONOCIMIENTO.md](PROPUESTA_BASE_CONOCIMIENTO.md): con él, N0 a N5 quedan cubiertas (solo faltarían adornos de N4/N5 sin esquema). En el camino se encontró y corrigió un bug real: `guardarRegistro` no persistía un cambio que solo tocaba un campo suprimido del historial (`ubicacionId`), por lo que la migración no vinculaba los equipos cuyo texto ya coincidía con el nombre de la ubicación; se corrigió comparando la entidad de verdad en vez de usar "¿generó historial?" como proxy. Falta un paso del usuario: aplicar el `schema.sql` COMPLETO en Supabase (idempotente) y luego correr la migración de ubicaciones desde Dispositivos > menú > Ubicaciones.

La tarea 69 (eliminaciones sensibles solo con contraseña maestra, sin exigir acceso a la bóveda) quedó terminada, verificada en navegador y archivada el 2026-07-17. Falta un paso del usuario: aplicar el `schema.sql` COMPLETO en Supabase (cambia la política RLS `boveda_meta_lectura` para que el verificador se descargue a todo el equipo); hasta entonces, quien no tiene permiso de bóveda elimina con confirmación simple en vez de con contraseña.

La tarea 68 (panel de sincronización: ver y recuperar cambios con error desde la app) quedó terminada, verificada en navegador y archivada el 2026-07-17. Origen: al usuario le quedó un cambio atascado en la PC (punto rojo tras eliminar una solución, por el esquema atrasado del servidor) y el celular seguía mostrando la solución; el punto rojo no explicaba nada y la única vía de diagnóstico era la consola del desarrollador.

Antes, la tarea 67 (sincronización en tiempo real con Supabase Realtime) quedó terminada y verificada en local (typecheck, lint, 402 pruebas, build y app en navegador sin errores). El `schema.sql` completo ya fue aplicado por el usuario y se verificó desde aquí la noche del 2026-07-17 con sondas de solo lectura: las 11 tablas y todas las columnas nuevas responden por REST, la publicación `supabase_realtime` acepta suscripciones por tabla, y el build desplegado en Vercel incluye el canal `cambios-equipo` y el panel de sincronización. Verificación de la propagación real entre dos dispositivos: pendiente del usuario (protocolo en el análisis del 2026-07-17). Archivada el 2026-07-17.

Antes, la tarea 66 (entrada rápida de tareas dentro de los pasos) quedó terminada, verificada en navegador y archivada el 2026-07-17.

Antes, la tarea 65 (nueva categoría "Software") quedó terminada, verificada en navegador y archivada el 2026-07-17.

Antes, la tarea 64 (etiquetas más ágiles: pegar o escribir varias etiquetas separadas por coma o salto de línea) quedó terminada, verificada en navegador real y archivada el 2026-07-17.

Antes, la tarea 63 (rediseño IT Brain: pantalla Soluciones re-autorizada) quedó terminada, verificada en navegador y archivada el 2026-07-17. Llegó un handoff Nocturne nuevo ("Herramienta IT para técnicos-handoff.zip", mismo tipo que el de la tarea 58, ya en tema oscuro) cuyo README apuntaba a `Soluciones.dc.html`: una re-autoría refinada de la pantalla Soluciones que ya existía en Nocturne (tarea 59), así que se ACTUALIZÓ `SolucionesPage.tsx`. Cambios: chips de categoría con conteo en vez de rejilla, subfiltros por tipo dentro de una categoría, icono de tipo coloreado por tono (acento/ámbar/verde/neutro), resultados de búsqueda agrupados por categoría con el término resaltado, y búsqueda que ahora coincide también por etiqueta y por tipo. Se añadió el icono `XCircleFill` y el helper `claseTonoDeTipo`. El botón "Crear" conserva el gating por categoría de la tarea 59 (el prototipo es estático y no resuelve que crear necesita una categoría), con estilo secundario como pide el diseño. Este handoff Nocturne SÍ es la línea vigente (a diferencia del zip de tema claro descartado de la tarea 62).

Antes, la tarea 62 (rediseño IT Brain: sidebar de escritorio en el shell Nocturne) quedó terminada, verificada en navegador y archivada el 2026-07-17. A pedido del usuario se hizo `ShellNocturne` responsive trayendo del handoff `Soluciones.dc.html` el layout de escritorio (sidebar fija de 240px con los 5 módulos y el perfil a partir de >=1024px), pero manteniendo el sistema oscuro Nocturne y el bottom nav intacto en móvil; beneficia a todas las pantallas Nocturne (Soluciones y Ficha de Procedimiento), no solo a Soluciones. El handoff de la tarea 62 seguía siendo el zip de TEMA CLARO ya descartado y la pantalla Soluciones YA estaba implementada en Nocturne (tarea 59): se aclaró con el usuario antes de tocar nada y él eligió justamente "agregar el sidebar de escritorio sobre Nocturne", no reimplementar en claro.

Antes, la tarea 61 (base de conocimiento inteligente, fase N4: ficha de categoría con esqueleto estándar y línea de tiempo unificada) quedó terminada, verificada en navegador y archivada el 2026-07-16. Al investigar se descubrió que los cambios de cableado de un dispositivo YA vivían en el historial (no había nada que fusionar ahí); lo que de verdad faltaba eran las ejecuciones de diagnóstico de un artículo y los accesos de auditoría de una credencial, ahora fusionados en el mismo `Historial.tsx` sin cambiar ningún call site. En el camino se encontró y corrigió un error de tipos real que `npx tsc --noEmit` no detectaba pero `npm run build` (`tsc -b`) sí: lección para adelante, correr siempre el build completo antes de cerrar una tarea, no solo `tsc --noEmit`. La ficha de ubicación de N4 queda fuera hasta la fase N3 (tabla `ubicaciones`, bloqueada por las 4 decisiones abiertas del usuario, sección 12 de [PROPUESTA_BASE_CONOCIMIENTO.md](PROPUESTA_BASE_CONOCIMIENTO.md)). De la propuesta solo queda N5 (aprendizaje ligero: rutas de inicio ordenadas, progreso por categoría, prerequisitos desde el grafo) sin depender del usuario; N3 sigue bloqueada.

Antes, la tarea 60 (base de conocimiento inteligente, fase N2: buscador con categorías/adjuntos/filtro por tipo, `dispositivosAfectados` generalizado a todo tipo de artículo, propiedades sugeridas por categoría y creación contextual de incidencia/credencial desde un equipo) quedó terminada, verificada en navegador y archivada el 2026-07-16. Elegida por el usuario porque la línea Nocturne seguía bloqueada sin handoff nuevo de Bóveda ni Inicio. En el camino se encontró y corrigió un bug real (zona muerta temporal: un `useMemo` usaba `categoriaId` antes de declararlo, rompía toda la pantalla de dispositivo en tiempo de ejecución sin que typecheck ni lint lo detectaran) gracias a la verificación en navegador. N3 (tabla `ubicaciones`, vínculo credencial↔dispositivo por esquema, tipo 'relacionado' en conexiones) depende de las 4 decisiones abiertas del usuario (sección 12 de la propuesta).

Antes, la tarea 59 (rediseño IT Brain: pantalla Soluciones en Nocturne) quedó terminada, verificada en navegador y archivada el 2026-07-16. Su diseño (`Soluciones.dc.html`) venía en el zip de TEMA CLARO descartado "Prompt Maestro para Claude Design-handoff.zip"; por decisión del usuario NO se implementó en claro sino que se TRADUJO a Nocturne (misma estructura y contenido, sistema oscuro), como se registró en el archivo. Antes, la tarea 58 (Ficha de Procedimiento en Nocturne, del handoff "Herramienta IT para técnicos-handoff.zip") también quedó archivada el 2026-07-16.

El sistema Nocturne (oscuro) REEMPLAZA la dirección de tema claro del handoff anterior (decisión D-006 del proyecto de diseño): las pantallas claras de las tareas 55 a 57 (Dispositivos, Red, Topología) quedan pendientes de re-autoría en Nocturne. El orden previsto por el proyecto de diseño (D-008) es Bóveda, Inicio, Soluciones, Dispositivos, Ficha de Dispositivo, Red, Topología; ya están hechas en Nocturne la Ficha de Procedimiento (58) y Soluciones (59). Lo IDEAL para las que faltan es que el usuario entregue un handoff Nocturne re-autorizado (como el de la tarea 58); si solo hay handoff en tema claro (como pasó con Soluciones), el patrón acordado es traducirlo a Nocturne. Siguiente sugerido: Bóveda (no venía en el zip de tema claro) o Inicio.

Fundaciones reutilizables ya construidas: tokens `noct-*` en `src/index.css`, iconos en `src/components/iconos.tsx` (46, regenerables con `scripts/generar-iconos.mjs`), shell en `src/app/ShellNocturne.tsx`, primitivas en `src/components/nocturne.tsx`, e iconos por tipo/categoría de Soluciones en `src/features/soluciones/iconosSoluciones.ts`. Sigue pendiente de decisión del usuario: si el rótulo del shell queda "IT Brain" o vuelve a "Soluciones IT"; y si el botón "Nuevo equipo" de Red debería abrir un flujo dedicado a crear una conexión.

**Hallazgo 2026-07-16 (revisión de zips):** el zip "Prompt Maestro para Claude Design-handoff.zip" (exportado 05:55, ANTES del handoff Nocturne de las 17:53 que dio la tarea 58) es un proyecto de diseño distinto y superado: sus documentos internos (`01_PROYECTO.md`, `05_DECISIONES.md` D-001, `03_EN_PROGRESO.md`) confirman que es la línea de TEMA CLARO, la misma que produjo las tareas 55 a 57, no la línea Nocturne. Trae `Inicio.dc.html`, `Soluciones.dc.html`, `Dispositivos.dc.html`, `Ficha de Dispositivo.dc.html`, `Red.dc.html`, `Topologia.dc.html`, todos en claro; NO trae `Boveda.dc.html` (ese proyecto se quedó en progreso justo en esa pantalla, D8). Por decisión del usuario, NINGUNA de estas pantallas se implementa desde este zip. Falta pedir al usuario el mismo tipo de handoff Nocturne que ya se usó para Ficha de Procedimiento (tarea 58), para Bóveda primero y luego Inicio, Soluciones, Dispositivos, Ficha de Dispositivo, Red y Topología, según el orden D-008. El archivo `Prompt Maestro para Claude Design-handoff.zip` puede borrarse de disco cuando el usuario confirme que ya no lo necesita como referencia (está ignorado en git, no versionado).

La tarea 54 (base de conocimiento inteligente, fases N0 y N1) quedó terminada y archivada, sin cambios de esquema. Siguientes fases de la propuesta [PROPUESTA_BASE_CONOCIMIENTO.md](PROPUESTA_BASE_CONOCIMIENTO.md), sin depender del usuario: N2 (categorías y adjuntos en el buscador, filtros por tipo, creación contextual, propiedades sugeridas por categoría, generalizar dispositivosAfectados) y N4 (fichas 360° de categoría y ubicación, línea de tiempo unificada). N3 (tabla `ubicaciones`, vínculo credencial↔dispositivo, tipo 'relacionado' en conexiones) es el único grupo con esquema; sus decisiones de forma quedaron RESUELTAS el 2026-07-17 (ver el párrafo de la propuesta más abajo), así que N3 está desbloqueado y listo para agendar. Notas de estado previas: la fase 2 de la tarea 39 queda absorbida por N2/N3. El grupo de esquema (tarea 52) quedó terminado; falta que el usuario aplique el `schema.sql` en Supabase. Otras fases de código sin depender del usuario: S2 y D4+F3. Falta decidir R2. Las tareas 28, 36, 37 y 38 tienen su código completo y verificado; solo queda que el usuario aplique el schema.sql.)

## Por hacer

### 89. Rediseño IT Brain: Etiquetas QR re-autorizada en Nocturne
- Descripción: implementar `Etiquetas QR.dc.html` de la carpeta del handoff. Migrar `src/features/dispositivos/EtiquetasPage.tsx` del tema slate heredado a Nocturne, fiel al mockup, conservando la generación e impresión de etiquetas QR. Tercera de las 4 pantallas restantes de la carpeta (regla 15).
- Prioridad: Alta (handoff activo, en orden).
- Ubicación: `src/features/dispositivos/EtiquetasPage.tsx`. Hoy dentro del `Layout` oscuro. Referencia: `Etiquetas QR.dc.html`.

### 90. Rediseño IT Brain: Importar Dispositivos re-autorizada en Nocturne
- Descripción: implementar `Importar Dispositivos.dc.html` de la carpeta del handoff. Migrar `src/features/dispositivos/importar/ImportarDispositivosPage.tsx` del tema slate heredado a Nocturne, fiel al mockup, conservando la lectura de archivo, el mapeo de columnas y el parseo CSV. Cierra la carpeta del handoff (regla 15).
- Prioridad: Alta (handoff activo, en orden).
- Ubicación: `src/features/dispositivos/importar/ImportarDispositivosPage.tsx` (+ helpers `leerArchivo.ts`, `mapearFilas.ts`, `parsearCsv.ts`, sin tocar su lógica). Hoy dentro del `Layout` oscuro. Referencia: `Importar Dispositivos.dc.html`.

### 79. Modo Asistente Fase 2: captura de evidencia por paso
- Descripción: en el modo ejecución (`AsistenteVista`), permitir adjuntar una foto de "prueba de trabajo" al completar un paso (el técnico documenta lo que hizo en el sitio). Reutiliza la tubería de subida/compresión y cola offline existente (`comprimirImagen`, `subirOEncolarArchivo`, `Adjuntos`). Decisión pendiente del usuario: dónde vive la evidencia. Opciones sin/con esquema: (a) como adjunto del paso en el propio artículo (no ideal: la evidencia es por ejecución, no del contenido); (b) como intervención en el `historial` del dispositivo asociado (sin esquema, reutiliza `registrarIntervencion` + `adjuntos` entidad 'historial'); (c) un registro de ejecución compartido con evidencia (requeriría esquema, agrupable con otros cambios). Recomendado: empezar por (b) si el procedimiento tiene un dispositivo afectado; si no, diferir.
- Prioridad: Media
- Ubicación: `src/features/soluciones/AsistenteVista.tsx` (+ posible `AsistentePage.tsx`); reutiliza `src/components/Adjuntos.tsx`, `src/lib/comprimirImagen.ts`, `src/lib/archivosPendientes.ts`, `src/lib/repositorio.ts` (`registrarIntervencion`).

### 77. Migrar la ficha de categoría (CategoriaPage) al sistema Nocturne
- Descripción: `src/features/soluciones/CategoriaPage.tsx` (`/soluciones/:categoriaId`) sigue en el tema claro heredado (`bg-sky-500`, `slate`). Desde la tarea 76 ya NO es el destino de "Volver" de las soluciones (eso ahora va a la lista con chip), pero sigue siendo la ficha 360° de la categoría, accesible desde el buscador ([useIndiceBusqueda.ts](src/features/busqueda/useIndiceBusqueda.ts) `tipo:'categoria'`) y desde la ficha de un dispositivo ([DispositivoPage.tsx:196](src/features/dispositivos/DispositivoPage.tsx)). Al abrirla se ve "vieja" respecto al resto de la app. Falta re-autorizarla en Nocturne (misma pauta que las demás pantallas: shell propio, tokens `noct-*`). Idealmente con un handoff Nocturne del usuario; si no llega, se traduce el diseño actual a Nocturne (regla 12).
- Prioridad: Media
- Ubicación: `src/features/soluciones/CategoriaPage.tsx`.

### 74. Decidir si se resurgen las 4 funciones ocultas por el rediseño del Editor de Artículo
- Descripción: el rediseño Nocturne (tarea 71) no muestra equipos donde aplica (`dispositivosAfectados`), adjuntos por paso (galería de PDF/Word), orden en la ruta de inicio (`ordenRutaInicio`) ni "cambio mayor" (salto de versión mayor al publicar). Sus datos se conservan al guardar pero no hay interfaz para editarlos desde el editor nuevo. El usuario decidió (2026-07-18) dejarlas así por ahora; esta tarea queda para cuando decida si las repone y en qué parte del editor Nocturne encajarían.
- Prioridad: Baja
- Ubicación: `src/features/soluciones/ArticuloForm.tsx` (equipos donde aplica, orden de ruta, cambio mayor), `src/features/soluciones/PasosEditor.tsx` (adjuntos por paso).

### 15. Respaldo automático de datos
- Descripción: workflow de GitHub Actions que cada domingo exporta las tablas de Supabase (el plan gratuito no tiene copias de seguridad), cifra el resultado con AES-256 y lo guarda como artefacto del workflow por 90 días. Lee los datos con un usuario dedicado de respaldo (respetando RLS, sin usar jamás la clave service_role, prohibida por REGLAS.md) cuyas credenciales viven en los secretos de GitHub Actions. El cifrado es obligatorio porque el repositorio es público y sus artefactos son descargables por cualquier usuario de GitHub.
- Prioridad: Media
- Ubicación: `scripts/respaldo-supabase.sh`, `.github/workflows/respaldo-supabase.yml`, guía en `supabase/RESPALDO.md`, nota en `ARQUITECTURA.md` sección 10.
- Avance: código completo y subido a GitHub (2026-07-03). Script `scripts/respaldo-supabase.sh` (inicio de sesión por la API de auth, exportación paginada de las tablas ordenadas por id, manifiesto con conteos, tar.gz cifrado con AES-256-CBC + PBKDF2 600k iteraciones; aviso si credenciales sale vacía por falta de puede_ver_boveda). Workflow con cron dominical, disparo manual y artefacto con retención de 90 días. Verificado localmente: sintaxis, fallo limpio sin variables, fallo limpio con credenciales falsas (HTTP 400 real contra Supabase), fusión de páginas, manifiesto, y ciclo completo de cifrado/descifrado con rechazo de clave equivocada (jq descargado temporalmente al scratchpad, no quedó en el proyecto).
- Avance (2026-07-10): la lista de tablas se actualizó a las 11 actuales (`conexiones`, `diagnosticos`, `ejecuciones_diagnostico` y `accesos_boveda` habían quedado fuera desde que se agregaron esas tablas); sintaxis verificada de nuevo.
- Bloqueada por (usuario): crear el usuario de respaldo en Supabase, darle puede_ver_boveda, cargar los 3 secretos en GitHub y disparar la primera ejecución manual, siguiendo `supabase/RESPALDO.md`. Al hacerlo, verificar desde aquí la ejecución real por la API de GitHub y archivar la tarea.

Propuestas presentadas al usuario el 2026-07-03, pendientes de que elija cuáles convertir en tareas: recordatorios de mantenimiento preventivo por dispositivo y reporte mensual de actividad desde el historial. (La tercera propuesta, cola de subida para adjuntos offline, ya se hizo: tarea 17 en el archivo.)

Propuesta presentada al usuario el 2026-07-17, PENDIENTE de que elija mejoras: endurecimiento del motor de sincronización colaborativa, tras el análisis que confirmó las causas del "no se ven los cambios sin reinstalar" (esquema de producción atrasado hasta el 2026-07-17, publicación Realtime ausente hasta esa fecha, y celulares con build viejo por el service worker de la era autoUpdate que requieren una limpieza manual única). Mejoras propuestas, en orden sugerido: (1) aislar errores de descarga por tabla en `src/lib/sync.ts` (hoy la primera tabla que falla aborta las siguientes, líneas ~207-209); (2) exponer en la interfaz el estado "sin sesión" (hoy `ejecutarSync` retorna en silencio, líneas ~189-193); (3) detección de conflictos: guardar el `updated_at` base en cada entrada del outbox (`src/lib/repositorio.ts`, `encolarCambioDeEntidad`) y, al subir sobre una fila más nueva del servidor, marcar conflicto en el panel de sincronización en vez de pisar en silencio; (4) protocolo de verificación de propagación entre PC y celular (paso del usuario, guiado).

Propuesta presentada al usuario el 2026-07-15, PENDIENTE de que elija fases y resuelva 4 decisiones abiertas: rediseño arquitectónico "base de conocimiento inteligente" (principio: cada dato existe una sola vez, todo lo demás lo referencia), analizada contra el código real en [PROPUESTA_BASE_CONOCIMIENTO.md](PROPUESTA_BASE_CONOCIMIENTO.md). Diagnóstico: el principio ya se cumple en un ~80 %; quedan 4 grietas estructurales: (1) ubicación como texto libre sin entidad, (2) copias de referencia sin regla de frescura (por ejemplo `lib/conexiones.ts:22-25` muestra nombres congelados), (3) falta el inverso universal "¿qué referencia a esto?" (se propone un grafo de referencias DERIVADO en `src/lib/grafo.ts`, nunca almacenado), (4) enlaces faltantes credencial↔dispositivo y artículo↔dispositivo (absorbe la tarea 39 fase 2). Fases N0 a N5; solo N3 (tabla `ubicaciones` + vínculos de credenciales + tipo 'relacionado' en conexiones) toca el esquema, agrupado en una sola intervención. **Decisiones RESUELTAS por el usuario el 2026-07-17** (fijan la forma de N3): (1) ubicaciones CON jerarquía, `padre_id` opcional; (2) vínculo credencial↔dispositivo SIN cifrar (lista `{id, nombre}` en claro dentro de `credenciales`, ya protegida por la RLS de bóveda; el contenido secreto sigue cifrado); (3) orden de las rutas de inicio en una COLUMNA de `articulos` (no en el JSON `procedimiento`, para que también ordene manuales sin pasos); (4) SÍ incluir el tipo `'relacionado'` en `conexiones` (relacionar equipos no-red) dentro de N3. La decisión 4 original de la propuesta (prioridad N0+N1 vs S2/D4) quedó resuelta de hecho: N0/N1/N2/N4 ya se construyeron (tareas 54/60/61). Con esto N3 queda DESBLOQUEADO; solo falta agendarlo e implementarlo como un único grupo de esquema. El color de categoría (Fase 0 de [PROPUESTA_REVISION_ARQUITECTURA.md](PROPUESTA_REVISION_ARQUITECTURA.md)) arranca derivado del `orden` sin esquema; su columna `color` (overrides) se suma al mismo lote de N3.

Propuesta presentada al usuario el 2026-07-09, PENDIENTE de que elija fases y resuelva 6 decisiones abiertas: mejoras por módulos (Soluciones, Diagnóstico, Dispositivos, Red y Bóveda; 71 puntos en 5 documentos del usuario), analizadas contra el código real en [PROPUESTA_MODULOS.md](PROPUESTA_MODULOS.md). Unos 25 puntos ya estaban resueltos por las tareas 38 a 47. El orden recomendado (sección 7 del documento) empieza por S1 (constructor de Soluciones), sigue con D1, Dis1+B1, R1, D2, y agrupa TODOS los cambios de esquema de Supabase en una sola actualización (fase 7) para una única intervención del usuario. Decisiones abiertas (sección 8): reactivar etiquetas (contradice el 2026-07-03), renombrar "Notas" a "Bóveda" (contradice la discreción), nombre "IT Brain", estados del artículo, nodos compartidos entre diagnósticos (se recomienda NO), y si el mapa interactivo de red (R2) se aprueba de una vez o tras probar R1.

Propuesta presentada al usuario el 2026-07-08, aprobada para ejecutarse por fases: mejoras de UX de procedimientos (18 puntos del usuario) y Modo Diagnóstico Inteligente, analizadas contra el código real en [PROPUESTA_UX_DIAGNOSTICO.md](PROPUESTA_UX_DIAGNOSTICO.md). TODAS las fases principales están terminadas y archivadas: A (tarea 41), B (tarea 42), C1 (tarea 43, visor de imágenes con zoom), D (tarea 44, modo asistente), E (tarea 45, sinónimos y anti duplicados) y F (tarea 46, Modo Diagnóstico Inteligente; pendiente solo de que el usuario aplique el schema.sql actualizado en Supabase, ver la tarea en el archivo). Quedan como opcionales a decidir: C2 (anotación de imágenes: flechas, círculos, texto) y F3 (tablero de estadísticas sobre las ejecuciones de diagnóstico). La tarea 40 (vínculos por instrucción, fase 1: credencial de la bóveda) se hizo y quedó archivada el 2026-07-09; otros vínculos por instrucción (IP/URL, nota, subprocedimiento o solución) quedan pendientes de que el usuario los pida.

### 2. Backend en Supabase (pasos del usuario, guiados)
- Descripción: aplicar el esquema y dar de alta al equipo en el proyecto de Supabase.
- Prioridad: Alta
- Ubicación: `supabase/schema.sql`, `supabase/INSTRUCCIONES.md`
- Avance: esquema completo escrito (tablas, historial inmutable con `recibido_en`, triggers de updated_at, RLS con permiso especial para la bóveda, bucket de adjuntos y categorías iniciales). Credenciales configuradas en `.env` local.
- Avance: paso 1 HECHO. Verificado por REST (2026-07-03) que `articulos.procedimiento` ya existe (antes daba error 42703); el esquema actualizado quedó aplicado en Supabase.
- Avance: paso 2 EN PARTE. 1 de los 5 usuarios creado (2026-07-03); el usuario creará los otros 4 más adelante.
- Pendiente (usuario, guiado en sesión): (2) crear los 4 usuarios restantes con Auto Confirm; (3) autorizar la bóveda con `puede_ver_boveda`; (4) desactivar el registro público. Al terminar cada paso se verifica desde aquí lo que sea posible.

### 10. Pulido móvil y puesta en marcha (solo quedan pasos del usuario)
- Descripción: optimización de rendimiento (dividir el bundle, que hoy supera los 700 kB por supabase-js + react-markdown + minisearch), compresión de fotos al subirlas, botón "Descargar todo para offline", icono definitivo de la app, pruebas en los teléfonos reales del equipo y guía de instalación de la PWA.
- Prioridad: Media
- Ubicación: general
- Avance: división del bundle HECHA. Ya no hay un único archivo de 766 kB; se parte en 30+ trozos con carga diferida por ruta (`React.lazy` en `src/App.tsx`, límite de Suspense en `src/app/Layout.tsx`, fallback compartido en `src/components/Cargando.tsx`) y separación de vendors estables (`supabase`, `react-vendor`, `dexie`) vía `build.rollupOptions.output.advancedChunks` en `vite.config.ts`. El chunk de entrada bajó de ~110 kB a ~15 kB; react-markdown (155 kB) solo carga al abrir un artículo y minisearch solo en Inicio; la pantalla de login no arrastra ninguno. Verificado en build de producción real (`vite preview`, config nueva en `.claude/launch.json`): el service worker precachea los 25 trozos JS (cache-first, funcionan offline) y tiene NavigationRoute a index.html (recarga de URL profunda offline sirve el shell SPA). 42 pruebas, lint y build en verde.
- Avance: compresión de fotos HECHA. Lógica pura y testable en `src/lib/comprimirImagen.ts` (`debeComprimir`, `calcularDimensiones`) separada de la parte que usa APIs del navegador (`comprimirImagen`, con `createImageBitmap` + `canvas`). Integrada en `src/components/Adjuntos.tsx`: antes de subir, la foto se redimensiona a un lado máximo de 1600 px y se recodifica como JPEG (calidad 0.82); se omiten PDF, SVG (vectorial), GIF (podría ser animado) y archivos ya livianos (≤300 KB); si algo falla o el resultado no queda más liviano, se sube el archivo original sin tocar (nunca bloquea la subida). 10 pruebas nuevas (52 en total) para la lógica de decisión y de escalado. Verificado en el navegador real con una foto sintética de 4032×3024 (7.5 MB): quedó en 1600×1200 y 47.9 KB; y con los tres casos de exclusión (imagen pequeña, PDF, SVG), que devuelven el archivo original intacto. Build, lint y bundle-splitting sin cambios.
- Avance: botón "Descargar todo para offline" HECHO. `src/lib/adjuntosOffline.ts`: cache dedicado (Cache Storage, `adjuntos-offline-v1`) con clave sintética estable basada en la referencia de Storage (nunca la URL firmada, que expira a la hora); `estaDisponibleOffline`, `obtenerUrlOffline` (URL `blob:` local), `cachearSiHaceFalta` (guarda un adjunto la primera vez que se ve, cerrando el hueco de ARQUITECTURA.md sección 10 que prometía esto pero no estaba implementado) y `descargarTodoOffline` (recorre todos los adjuntos, descarga los que falten, sigue adelante si alguno falla) con estado observable (`ProgresoDescarga`) para la interfaz. Botón nuevo en `src/components/DescargarOffline.tsx`, integrado en `src/features/inicio/InicioPage.tsx`. `AdjuntoItem` en `src/components/Adjuntos.tsx` ahora muestra primero desde el cache offline si existe, sin tocar la red. 4 pruebas nuevas (56 en total) para la clave sintética y el estado inicial. Verificado en navegador real: ida y vuelta completa del cache (descargar, detectar disponible, servir como blob, no volver a descargar), y el flujo de error real contra Supabase (URL de un archivo inexistente devolvió 400, se contó como "fallido" sin romper el resto y la interfaz lo mostró reactivamente). Build, lint y 56 pruebas en verde.
- Avance: guía de instalación HECHA. [INSTALACION.md](INSTALACION.md) nuevo, en la raíz del proyecto: pasos de instalación para Android (Chrome), iPhone/iPad (Safari, con la advertencia de que solo funciona desde Safari) y PC (Chrome/Edge); nota sobre el borrado de datos de iOS tras semanas sin abrir la app (ya documentado en ARQUITECTURA.md, aquí en lenguaje para el usuario final); recordatorio de usar "Descargar todo para offline" antes de salir a un mantenimiento; cómo se actualiza la app; problemas comunes. Enlazada desde `README.md` y `CLAUDE.md`.
- Avance: ícono definitivo HECHO. `public/icon.svg` rediseñado: escudo en degradado azul (`#38bdf8` a `#0284c7`) con un checkmark "recortado" en el mismo tono del fondo oscuro (`#0f172a`), sin texto (se lee bien incluso muy chico), sobre el cuadrado redondeado de siempre. Todo el arte cabe dentro de la "zona segura" de los íconos maskable (círculo del 80% central del lienzo), algo que el placeholder anterior (letras "IT" a todo el ancho) no respetaba y que Android podía recortar mal al aplicar su máscara. Verificado por muestreo de píxeles sobre un canvas real (esquinas transparentes, escudo sin tocar los bordes, checkmark visible cerca del centro) porque la herramienta de captura de pantalla falló por un problema del entorno, no del código. Se agregó además `public/apple-touch-icon.png` (180×180, sin transparencia ni esquinas redondeadas propias, seguiendo la convención de Apple de que iOS aplica su propio recorte) generado con `sharp` instalado temporalmente (`npm install --no-save`, desinstalado después; no quedó como dependencia). `index.html` y `vite.config.ts` actualizados para usarlo. Sigue siendo una sola pieza de arte SVG (liviano, no afecta la fluidez ni el peso de la app: el PNG nuevo pesa ~3.8 KB). Build, lint, bundle-splitting y 56 pruebas sin cambios.
- Avance: despliegue en Vercel OPERATIVO EN PARTE. El usuario montó el proyecto en Vercel (https://soluciones-it-psi.vercel.app, desplegado desde GitHub). Se detectó y corrigió: (1) GitHub estaba 6 commits atrás del repo local, por eso producción tenía un build viejo; ya se subió todo. (2) Las rutas internas daban 404 al abrirlas directo; se agregó `vercel.json` con la reescritura SPA a `index.html`. (3) Verificado por HTTP: el esquema de Supabase SÍ está aplicado (la tabla `categorias` responde y RLS oculta filas a usuarios sin sesión). (4) El usuario agregó `VITE_SUPABASE_URL` y `VITE_SUPABASE_ANON_KEY` en Settings > Environment Variables, pestaña "Project" (confirmado en pantalla que quedaron guardadas), pero el botón "Redeploy" del panel de Vercel dio "Unexpected error (500)" de forma repetida y no llegó a crear un despliegue nuevo (verificado: la lista de Deployments no muestra un intento adicional, y el build en producción seguía sin la URL de Supabase). Se disparó un despliegue nuevo por la vía de Git (commit a `main`) como alternativa al botón del panel, que sí dispara el build automático de Vercel con las variables ya guardadas.
- Avance: despliegue por Git VERIFICADO. El build de producción ya incluye la URL de Supabase; probado en navegador real contra el dominio: redirige a /login, sin aviso de "no conectado", y un intento de login llegó hasta Supabase y devolvió el error esperado en español. Producción conectada de punta a punta.
- Bloqueada por (usuario): (1) confirmar que los 5 usuarios del equipo estén creados en Supabase Authentication; (2) pruebas en los teléfonos reales del equipo con la guía de INSTALACION.md, que ya incluye la dirección real.

### 39. Dependencias por equipo (información conectada)
- Descripción: al abrir un dispositivo puntual, ver conectado todo lo suyo: procedimientos, credenciales autorizadas, ubicación, dispositivos conectados, historial y problemas frecuentes de ese equipo (no solo de su categoría). Para red ya existe gran parte (conexiones + topología); lo nuevo es vincular un equipo concreto a procedimientos, credenciales y problemas frecuentes específicos. La más ambiciosa; conviene hacerla por fases y apoyada en las tareas 36 y 38. Cubre la idea #7 del análisis del 2026-07-07.
- Prioridad: Baja
- Avance (2026-07-10, fase 1, SIN esquema, elegida por el usuario): la ficha del dispositivo (`src/features/dispositivos/DispositivoPage.tsx`) se reorganizó en vista 360° por secciones (Información, Procedimientos y diagnóstico, más los bloques autotitulados de Conexiones, Adjuntos e Historial) y se agregó "Problemas frecuentes de este equipo" (`ProblemasDelEquipo.tsx` + función pura `problemasDeDispositivo` en `problemasDeDispositivo.ts`, con 5 pruebas): el inverso del vínculo `dispositivosAfectados` (tarea 38), que hoy solo se veía del artículo hacia el equipo. Filtra a incidencias publicadas (excluye borrador/obsoleto/eliminado). Verificado en navegador real (sesión simulada, 4 artículos sembrados: 2 incidencias publicadas del equipo aparecen y enlazan bien, el borrador y el de otro equipo se excluyen; datos limpiados). 354 pruebas, typecheck, lint y build en verde.
- Fase 2 ENTREGADA por otras tareas: la parte sin esquema (generalizar `dispositivosAfectados` a todo tipo de artículo + "Procedimientos de este equipo") se hizo en N2 (tarea 60); la parte con esquema (credenciales de la bóveda vinculadas por id a un equipo, con "Credenciales de este equipo" en la ficha protegido por permiso de bóveda; y el tipo 'relacionado' en `conexiones` para equipos no-red) se hizo en el grupo N3 (tarea 70, archivada 2026-07-17). Con esto la tarea 39 queda cubierta; se conserva aquí solo como referencia del origen.
