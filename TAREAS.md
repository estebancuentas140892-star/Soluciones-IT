# Tablero de tareas

Reglas del tablero: solo puede haber una tarea "En proceso" a la vez. Las tareas terminadas se mueven a [TAREAS_ARCHIVO.md](TAREAS_ARCHIVO.md). Cada tarea indica su ubicación exacta dentro del proyecto.

## En proceso

### Tarea 167 - Mejora integral de la documentación y de la arquitectura funcional

- **Descripción:** Convertir la documentación en la única fuente de verdad. Crear `ARQUITECTURA_FUNCIONAL.md` (reglas de negocio numeradas RN-xxx, modelo de permisos + matriz, ciclos de vida y máquinas de estado por entidad, eventos del sistema, dependencias entre entidades, arquitectura offline, manejo de conflictos, auditoría, rendimiento, accesibilidad, arquitectura de navegación, modelo entidad-relación, convenciones y roadmap), `COMPONENTES_UI.md` (catálogo de componentes reutilizables) y `BUSCADOR.md` (catálogo de búsqueda). Refactorizar `DOCUMENTACION_FUNCIONAL.md` para que quede solo con lo visible al usuario. Crear `DECISIONES.md` y `CHANGELOG.md` (hoy inexistentes). Auditoría final de duplicación, contradicciones y enlaces.
- **Motivo:** La documentación mezcla lo interno con lo visible, está dispersa y no cubre de forma sistemática reglas de negocio, permisos, estados, eventos ni dependencias. Faltan `DECISIONES.md` y `CHANGELOG.md` pese a estar en la política de mantenimiento (REGLAS.md regla 19).
- **Impacto:** Mejora mantenibilidad, escalabilidad e incorporación de nuevos desarrolladores; documentación como única fuente de verdad. Sin cambios de comportamiento del sistema (solo documentación; si se detecta un error evidente de código, se documenta y se propone antes de tocar nada).
- **Prioridad:** Alta.
- **Estado:** En progreso.
- **Área afectada:** Documentación de la raíz del repo. Análisis de verificación sobre todo `src/` y `supabase/schema.sql` (se prioriza el código sobre la documentación).
- **Dependencias:** Ninguna de código. Dos decisiones de arquitectura documental pendientes de confirmación del usuario (casa canónica de CHANGELOG/DECISIONES y alcance del traslado desde `DOCUMENTACION_FUNCIONAL.md`).
- **Modelo/esfuerzo:** Opus 4.8 / Max + Ultracode (47/55).
- **Método:** Fase 1 (en curso) analiza el código con 6 agentes en paralelo (datos, offline, búsqueda, permisos, componentes, navegación/eventos); Fase 2 sintetiza y redacta; Fase 3 audita duplicación, contradicciones y enlaces.

---

La tarea 179 (el botón "Actualizar" del aviso de versión nueva no hacía nada, reportado por el usuario en su teléfono) quedó terminada y archivada el 2026-07-27. Causa raíz: `messageSkipWaiting()` de workbox-window no hace nada si no hay worker en espera, y en ese caso el botón quedaba inerte de forma permanente con el aviso puesto. La recarga ya no se delega en la librería. **Pendiente de que el usuario lo confirme en su teléfono**, porque el tramo exacto que fallaba no es reproducible en escritorio. Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 171 (rediseño de la lista de Soluciones, pantalla P1 del handoff "Auditoría de Soluciones TI") quedó terminada y archivada el 2026-07-27. Se implementaron los 11 problemas y las 13 decisiones que la auditoría documentó para `/soluciones`, más las 5 primeras piezas compartidas de la sección: `PastillaEstado`, `IndicadorAvance`, `PastillaFrescura`, `HojaFiltro` y `FilaArticulo`, con 4 módulos de lógica pura nuevos y probados (`tiempoRelativo`, `sinTerminar`, `coincidencia`, `sugerenciaBusqueda`). Typecheck, lint y build limpios; 96 pruebas en la sección y los helpers nuevos, 44 de ellas nuevas. Quedan los mismos **4 fallos preexistentes y ajenos** de `archivosPendientes.test.ts` (RLS de Storage contra el `.env` real de esta sesión), ya conocidos desde la tarea 166. **Sin verificar en navegador** (la app exige login de técnico real, no disponible en esta sesión); se confirmó arranque sin errores de consola y redirección limpia a login. Tres decisiones nuevas quedaron registradas en [DECISIONES.md](DECISIONES.md): AD-019 (reglas visuales R1 a R7 de la sección), AD-020 (`FilaArticulo` matiza, sin contradecir, la decisión de la tarea 145) y AD-021 (el escritorio se conserva). **Las 4 pantallas restantes del handoff (P2 a P5) quedan en "Por hacer" como tareas 172 a 175**, a petición del usuario, que quiso revisar el criterio aplicado en P1 antes de replicarlo. Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 159 (auditoría de rediseño del flujo del técnico, caso de uso "conectar impresora a PC Windows") entregó su **auditoría completa** el 2026-07-23 en [AUDITORIA_FLUJO_INSTALACION.md](AUDITORIA_FLUJO_INSTALACION.md). **Veredicto:** la app ya cumple ~85% del principio "cada dato una sola vez" (referencia viva, grafo derivado, vínculos protegidos que referencian secretos sin copiarlos, subprocedimientos por composición, Red sin duplicar el inventario, creación contextual precargada). Los huecos reales quedan como tareas 160 a 166 en "Por hacer", priorizados. **La implementación está pendiente de que el usuario apruebe las fases** (regla del encargo: auditar antes de tocar código; y algunos puntos, como el orden de pestañas y "aplica a" por marca/modelo, se pidió analizarlos antes de implementarlos). Modelo/esfuerzo: Opus 4.8 / Max (47/55). Se registró la nueva política obligatoria de mantenimiento de tareas y documentación en REGLAS.md (regla 19).

Antes, la tarea 158 (documentación funcional exhaustiva de toda la aplicación) quedó terminada el 2026-07-23: se creó `DOCUMENTACION_FUNCIONAL.md` en la raíz, un inventario completo y estructurado de toda la app (visión general, stack/shells/temas/navegación, mapa completo de rutas, modelo de datos, las 5 secciones del menú y las secundarias con su objetivo/botones/acciones/filtros/listas/indicadores, catálogo de formularios campo por campo con tipo de control/obligatoriedad/defecto/placeholder/validaciones, catálogo de modales y hojas, catálogo de botones y primitivas, menús, acciones transversales, grafo de relaciones entre secciones, seis flujos funcionales completos, árbol jerárquico de navegación, auditoría de cobertura y una sección de hallazgos y oportunidades de mejora). Se construyó leyendo directamente el código de todas las pantallas, editores, modales y primitivas de `src/`. **Modelo/esfuerzo:** Opus 4.8 / Extra (35/55 en la matriz), en línea sin subagentes. Enlazado desde CLAUDE.md como documento clave (pendiente si el usuario quiere el enlace). Sin cambios de código de producción. Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 157 (hallazgo D2 de la auditoría de flujos: el buscador del formulario de conexión reutiliza `incluyeTexto()`) quedó terminada y archivada el 2026-07-23: `candidatosConexion` en `src/lib/conexiones.ts` dejó de repetir el patrón de filtro por subcadena y reusa `incluyeTexto()` de `src/lib/texto.ts` (regla ya extraída en la tarea 145 para Dispositivos y Red). Typecheck, lint y 1602 pruebas en verde. **Con esto D2 queda resuelto para los tres buscadores que comparten semántica; Soluciones sigue aparte por diseño** (normalización de acentos, resaltado del término, campos extra), decisión ya registrada en la tarea 145. Sin verificar en navegador (falta una cuenta de técnico real para iniciar sesión). Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 156 (hallazgo D1 de la auditoría de flujos: `<FormularioConexion>` compartido entre ficha y topología) quedó terminada y archivada el 2026-07-23: `src/features/red/FormularioConexion.tsx` unifica el estado y la lógica de guardado que antes estaban duplicados en `ConexionesFicha.tsx` y `TopologiaEquipoPage.tsx` (una prop `variante` solo controla el chrome visual de cada pantalla); el default de medio quedó unificado en UTP en las dos. Typecheck, lint y 1602 pruebas en verde; el bundle de ambas pantallas bajó de peso. **Con esto se cierra el grupo completo de la "Fase incorporación y cableado"** (O1-O3 de la tarea 135, más N2 a N5 y D1 de hoy). Sin verificar en navegador (falta una cuenta de técnico real para iniciar sesión). Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 155 (hallazgo N3 de la auditoría de flujos: el punto de red hereda la ubicación de su rack/switch) quedó terminada y archivada el 2026-07-23: `ubicacionHeredable` en `src/lib/conexiones.ts` ofrece copiar la ubicación del otro extremo cuando el equipo actual no tiene ninguna; `ConexionesFicha.tsx` y `TopologiaEquipoPage.tsx` muestran el aviso con botón "Copiar ubicación". Typecheck, lint y 1602 pruebas en verde. **Con esto los cuatro hallazgos de red del día (N2, N3, N4, N5) quedan resueltos.** Sin verificar en navegador (falta una cuenta de técnico real para iniciar sesión). Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 154 (hallazgo N4 de la auditoría de flujos: sugerencia de puerto consecutivo libre al enlazar) quedó terminada y archivada el 2026-07-23: `proximoPuertoLibre` en `src/lib/conexiones.ts` calcula el menor puerto numérico libre de un switch a partir de sus enlaces ya registrados; `ConexionesFicha.tsx` y `TopologiaEquipoPage.tsx` lo usan como valor inicial editable del campo "Puerto en este equipo". Typecheck, lint y 1599 pruebas en verde. Sin verificar en navegador (falta una cuenta de técnico real para iniciar sesión). Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 153 (hallazgo N5 de la auditoría de flujos: la búsqueda del otro extremo prioriza ubicación y categoría de red) quedó terminada y archivada el 2026-07-23: `candidatosConexion` en `src/lib/conexiones.ts` prioriza equipos de la misma ubicación o de categoría `es_red` y pre-sugiere sin necesidad de teclear; `ConexionesFicha.tsx` y `TopologiaEquipoPage.tsx` (que duplicaban el cálculo, hallazgo D1) ahora comparten la misma función. Typecheck, lint y 1594 pruebas en verde. Sin verificar en navegador (falta una cuenta de técnico real para iniciar sesión). Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 152 (hallazgo N2 de la auditoría de flujos: "Crear" de Red prioriza las categorías de red) quedó terminada y archivada el 2026-07-23: `ordenarPriorizandoRed` en `src/lib/categorias.ts` antepone las categorías `es_red` en el selector cuando se llega desde `/dispositivos/nuevo?red=1` (enlace nuevo en `RedPage.tsx`). Typecheck, lint y pruebas en verde. Sin verificar en navegador (falta una cuenta de técnico real para iniciar sesión). Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 151 (hallazgo S6 de la auditoría de flujos: sugerencia de equipo por coincidencia de IP/URL al crear un secreto) quedó terminada y archivada el 2026-07-23: `src/features/boveda/sugerenciaEquipoPorIp.ts` detecta cuándo la URL/IP escrita coincide con un equipo del inventario, y `CredencialForm.tsx` ofrece vincularlo sin salir del formulario. Typecheck, lint y pruebas en verde. **Con esto el flujo 4 completo (S1 a S6, secretos y bóveda) queda sin hallazgos pendientes.** Sin verificar en navegador (falta una cuenta de técnico real para iniciar sesión). Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 150 (hallazgo S5 de la auditoría de flujos: aviso de solapamiento entre credencial y campo protegido) quedó terminada y archivada el 2026-07-23: `src/features/boveda/solapamientoSecreto.ts` detecta cuándo un equipo vinculado a una credencial 'cuenta' ya guarda una contraseña protegida en su ficha, y `CredencialForm.tsx` avisa antes de duplicarla. Typecheck, lint y pruebas en verde. **Sin verificar en navegador**: esta sesión ya tiene Supabase configurado (`.env` con las credenciales del proyecto), pero falta una cuenta de técnico real para iniciar sesión. Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 149 (hallazgo S4 de la auditoría de flujos: el título de credencial ya no queda desfasado si el equipo se renombra) quedó terminada y archivada el 2026-07-23: `src/features/boveda/tituloAcceso.ts` detecta cuándo el título sigue el patrón "Acceso {nombre}" sin personalizar y el equipo vinculado se renombró, y `CredencialForm.tsx` ofrece un botón para actualizarlo (mismo patrón UX que el aviso de rotación S1). Typecheck, lint y 1449 pruebas en verde. **Sin verificar en navegador** (mismo límite de esta sesión: sin Supabase configurado no hay login). Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 148 (hallazgo S3 de la auditoría de flujos: motivo del cambio y "Generar contraseña" para campos protegidos) quedó terminada y archivada el 2026-07-23: `generarContrasena` se extrajo a `src/lib/generarContrasena.ts` (compartida entre Bóveda y campos protegidos), `SeguridadDelEquipo.tsx` suma el botón "Generar" y el campo "Motivo del cambio (opcional)". Typecheck, lint y 1444 pruebas en verde. **Sin verificar en navegador** (mismo límite de esta sesión: sin Supabase configurado no hay login). Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 147 (hallazgo S2 de la auditoría de flujos: vencimiento y recordatorio de rotación para campos protegidos del equipo) quedó terminada y archivada el 2026-07-23: `CampoProtegido.venceEn` (mismo patrón que `Credencial.venceEn`), badge de vencimiento en la ficha del equipo, y nueva categoría en el bloque "Pendientes" de Inicio. Typecheck, lint y 1444 pruebas en verde. **Sin verificar en navegador** (mismo límite de esta sesión: sin Supabase configurado no hay login). Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 146 (tablero de estadísticas de diagnóstico y "Problemas frecuentes" en Inicio, cierra D4+F3 de PROPUESTA_MODULOS.md) quedó terminada y archivada el 2026-07-23: `src/features/diagnostico/estadisticas.ts` agrega `ejecuciones_diagnostico` (tasa de éxito, duración mediana, problemas y procedimientos más usados, motivos de fallo), pantalla nueva `/diagnostico/estadisticas` y bloque derivado en Inicio con fallback a "recientes" sin volumen. 1442 pruebas en verde, typecheck, lint y build limpios. **Verificación en navegador solo parcial** (sin Supabase configurado en esta sesión, no hay login; se confirmó arranque sin errores y la ruta nueva redirige a login limpio). Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 145 (remates de la Fase 1 de la revisión arquitectónica: `esDeRed()` en `src/lib/categorias.ts`, `incluyeTexto()` en `src/lib/texto.ts` y `<FilaDispositivo>` compartida) quedó terminada y archivada el 2026-07-23: 1410 pruebas en verde, typecheck, lint y build limpios, sin cambio de comportamiento visible. Incluye dos decisiones registradas: NO unificar los tres buscadores locales con el índice global (rompería los borradores visibles en Soluciones) y NO crear `<FilaArticulo>`. **SIN verificar en navegador**, mismo motivo que la 144. Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 144 (anti duplicados de IP en `DispositivoForm.tsx`, mismo patrón que el aviso de serial ya existente) quedó terminada y archivada el 2026-07-23: código, typecheck, lint y 1394 pruebas en verde. **SIN verificar en navegador**: este entorno de sesión no tiene Supabase configurado (falta `.env`), así que no fue posible iniciar sesión y probar el flujo real. Queda como mejora opcional que el usuario lo confirme en su teléfono. Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 143 (endurecimiento de `camposOpcionales` y columna `dispositivos.responsable` que faltaba en el esquema) quedó terminada y archivada el 2026-07-22. **Requiere un paso del usuario**: volver a ejecutar `supabase/schema.sql` completo en Supabase para crear `dispositivos.responsable`. Hasta que se aplique, TODA escritura de dispositivos sigue siendo rechazada por el servidor.

Antes, la tarea 142 (K6 de la auditoría de flujos, crear procedimiento contextual desde un equipo) quedó terminada, verificada en navegador real y archivada el 2026-07-22. Con esto queda resuelto el grupo K completo (K1 a K6) de la auditoría de flujos.

Antes, la tarea 141 (K3, K4 y K5 de la auditoría de flujos) quedó terminada, verificada en navegador real y archivada el 2026-07-22. Con esto quedaron resueltos los tres hallazgos MEDIA de prioridad más alta; ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 140 (bucle sugerencia -> borrador de artículo, hallazgo K2) quedó terminada, verificada en navegador real y archivada el 2026-07-22. **Queda un paso del usuario**: aplicar en Supabase el bloque nuevo de `supabase/schema.sql` (columna `articulos.origen_sugerencia_id`). La app funciona igual antes y después de aplicarlo (la columna está declarada en `camposOpcionales`), pero hasta que se aplique, el vínculo sugerencia -> artículo NO viaja entre dispositivos: cada técnico verá "Ya redactada" solo para los artículos que él mismo creó.

Antes, la tarea 135 (asistente de incorporación y cableado en el alta, hallazgos O1+O2+O3 de la auditoría de flujos) quedó terminada, verificada en navegador y archivada el 2026-07-22. Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 139 (completar la migración a Nocturne: 12 archivos con el tema heredado) quedó terminada y archivada el 2026-07-22. Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 138 (migrar el `ErrorBoundary` al sistema Nocturne) quedó terminada, verificada en navegador real y archivada el 2026-07-22, en una sesión paralela. Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 137 (normalizar las filas ya cacheadas en IndexedDB, causa raíz de la tarea 136) quedó terminada, verificada en navegador real y archivada el 2026-07-22. Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 136 (la Bóveda no abría: "No se pudo cargar la aplicación", reportado por el usuario) quedó terminada y archivada el 2026-07-22. Ver el detalle en TAREAS_ARCHIVO.md. Su causa raíz quedó cerrada por la tarea 137.

Antes, la tarea 134 (Reemplazar equipo, hallazgos L2 + L3) quedó terminada, verificada en navegador real de punta a punta y archivada el 2026-07-22. Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 133 (Dar de baja con cascada, hallazgo L1) quedó terminada, verificada en navegador real de punta a punta y archivada el 2026-07-22. Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 132 (entidad Persona/Responsable, hallazgo T1) quedó terminada y archivada el 2026-07-22: código completo, 639 pruebas, typecheck, lint y build en verde, pero SIN verificar en navegador dentro de esa sesión (el entorno no tenía Supabase configurado en ese momento). Sigue pendiente que el usuario aplique el `schema.sql` actualizado en Supabase (tabla `personas`, columna `dispositivos.responsable_id`); la verificación en navegador queda como mejora opcional (el patrón es una copia casi literal de `ubicaciones`, ya probado). Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 131 (Fase de corrección de la auditoría de flujos: K1, S1, N1) quedó terminada, verificada en navegador real de punta a punta y archivada el 2026-07-22. Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 130 (re-autoría de la pantalla de bloqueo BloqueoAppGuard al sistema Nocturne) quedó terminada, verificada en navegador y archivada el 2026-07-21. Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 129 (Fase 0c de la revisión arquitectónica: iconos, estado de dispositivo y helpers compartidos) quedó terminada y archivada el 2026-07-21, repartida en 3 agentes en paralelo sobre worktrees aislados, cada uno en un frente ya diagnosticado contra el código real antes de repartir. **Con esto la Fase 0 de PROPUESTA_REVISION_ARQUITECTURA.md queda completa** (0a tarea 124, 0b tarea 126, 0c tarea 129). Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 128 (un null heredado atascaba un cambio para siempre en la cola de subida) quedó terminada y archivada el 2026-07-21, a partir del panel de sincronización que reportó el usuario: entre los 7 cambios rechazados, seis eran consecuencia directa del `schema.sql` sin aplicar, pero uno (`null value in column "es_ruta_inicio" ... violates not-null constraint`, 700 intentos) NO se habría arreglado aplicándolo. Causa real en `src/lib/tablas.ts`: `aEntidadLocal` convertía en `null` toda columna ausente en la fila descargada, así que un artículo bajado ANTES de que existiera `es_ruta_inicio` guardaba null donde el tipo local declara `boolean`; al subirlo, ese null violaba el NOT NULL y el cambio se reintentaba para siempre, porque además la regla anti pisado deja de refrescar una ficha con cambios pendientes (el null nunca se corregía solo). Corregido con `porDefecto` por tabla en `configTablas` (el contrato NOT NULL DEFAULT de `schema.sql` escrito de este lado), aplicado en las dos direcciones: al bajar evita crear el null, al subir cura los ya guardados. Cubre las 12 tablas, no solo `articulos`. Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 127 (el panel de sincronización se salía por arriba, reportado por el usuario) quedó terminada, verificada en navegador y archivada el 2026-07-21: la causa real era que `backdrop-filter` en la cabecera de Inicio crea un bloque contenedor para `position: fixed`, así que la capa del modal se dimensionaba contra esa cabecera de 166px en vez de contra la pantalla y la tarjeta quedaba con 480px fuera de la vista. Corregido de raíz renderizando `Modal` con `createPortal` a `document.body` (sirve para todos sus usos, no solo este). De paso, el panel agrupa los cambios fallidos por causa (antes repetía la misma explicación una vez por ficha) y muestra "Dato protegido"/"Ubicación" en vez del nombre técnico de la tabla. **El error en sí no es un fallo de código**: falta aplicar el `schema.sql` en Supabase, paso pendiente del usuario desde la tarea 116 (ver abajo). Ver el detalle en TAREAS_ARCHIVO.md.

**PASO PENDIENTE DEL USUARIO, ahora bloqueante**: aplicar `supabase/schema.sql` completo en el panel de Supabase (SQL Editor > New query > pegar todo > Run). Es idempotente y una sola ejecución cubre los TRES grupos acumulados sin aplicar: N3 (2026-07-17: `ubicaciones`, `dispositivos.ubicacion_id`, `credenciales.dispositivos`, `articulos.orden_ruta_inicio`, `categorias.color`), P1 (`campos_protegidos`, `accesos_boveda.entidad_tipo`, `credenciales.tipo`) y P5 (`credenciales.archivo`, el valor `'descargo'` y el bucket `archivos_boveda`). Hasta que se aplique, la pastilla de sincronización seguirá diciendo "Con error" y esos cambios no subirán. Guía en `supabase/INSTRUCCIONES.md` sección 1.

Antes, la tarea 126 (Fase 0b: sistema de color por categoría) quedó terminada, verificada en navegador y archivada el 2026-07-21: diez tokens OKLCH nuevos y `src/features/soluciones/coloresCategoria.ts` dan a cada categoría un color propio y estable (derivado de su `orden`, o el override manual de `categorias.color`, columna que existía desde N3 sin que nada la leyera). Se cablearon los chips y cabeceras de Soluciones (móvil y rail de escritorio), las rejillas de los editores de dispositivo y diagnóstico, los grupos de Diagnósticos y la ficha de categoría. Contraste mínimo medido: 7.53:1. **Decisión del usuario**: se conserva el color por tipo de documento (la propuesta pedía retirarlo, pero eso habría revertido la tarea 73, pedida un día después de escribirse la propuesta). Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 125 (borrado de `src/components/Seccion.tsx`, código muerto) quedó terminada y archivada el 2026-07-21, en una sesión paralela: el componente no lo importaba nadie desde que cada formulario arma sus bloques en línea con `TituloSeccion` de `src/components/nocturne.tsx`, y además seguía escrito con las clases del tema claro heredado (`slate-800`, `slate-900/50`, `slate-200`, `slate-500`) que la tarea 113 dio por extinguidas en todo el proyecto. Typecheck, lint, 588 pruebas y build en verde tras el borrado. Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 124 (Fase 0a de la revisión arquitectónica: primitivas de formulario y vocabulario compartido) quedó terminada, verificada en navegador y archivada el 2026-07-21: `src/components/campos.tsx` es ahora la fuente única de cómo se ve un campo (antes `CLASE_CAMPO` estaba redefinida en 13 archivos, más 3 copias inline, con deriva ya visible en pantalla) y `src/lib/vocabulario.ts` centraliza `valoresUnicos`. De paso se corrigieron dos defectos reales que la duplicación escondía: en la Bóveda, el campo Valor de "Otros datos protegidos" medía 26px de ancho, y su lista de categorías ofrecía "POS" y "pos" como opciones distintas. Ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 123 (deduplicación por hash de adjuntos, decisión D3 de PROPUESTA_JORNADA_TECNICO.md) quedó terminada y archivada el 2026-07-21: `src/lib/archivosPendientes.ts` calcula el SHA-256 del contenido antes de subir y arma la referencia como `compartidos/<hash>`, así que dos archivos idénticos (subidos por cualquier técnico, desde Adjuntos.tsx o la foto de un dispositivo) terminan en el mismo objeto de Storage en vez de duplicarse; `referenciaEnUso` evita subir de más y evita borrar del bucket un archivo que otra ficha todavía usa, todo sin esquema nuevo. Con esto **PROPUESTA_JORNADA_TECNICO.md queda 100 % cerrada**, incluidas sus tres decisiones abiertas. Pendiente: que el usuario pruebe en su teléfono subir el mismo archivo dos veces y vea el aviso de reutilización, ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 122 (bloque "Pendientes" en Inicio, decisión D5 de PROPUESTA_JORNADA_TECNICO.md) quedó terminada y archivada el 2026-07-21: Inicio suma una sección "Pendientes" derivada (mis borradores, credenciales por vencer/vencidas con permiso de bóveda, sugerencias del equipo sin revisar), sin tabla ni esquema nuevos. El usuario también resolvió D3 (deduplicación por hash de adjuntos: anotada como tarea futura, ver "Por hacer") y D4 (mantener "Soluciones", sin cambios). Con esto **PROPUESTA_JORNADA_TECNICO.md queda completa**; `PROPUESTA_BASE_CONOCIMIENTO.md` ya no tenía decisiones abiertas. Pendiente: que el usuario valide el bloque en su teléfono, ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 121 (Fase P5 de PROPUESTA_SEGURIDAD_DISPOSITIVO.md: cifrado de binarios para "Archivo seguro") quedó terminada y archivada el 2026-07-21: el archivo de un secreto tipo "Archivo seguro" ahora se cifra en el teléfono antes de subirse a un bucket de Storage nuevo y privado (`archivos_boveda`, RLS `puede_ver_boveda`, distinto del bucket `adjuntos` que cualquier autenticado puede leer), con encaje en la cola de subida offline y el cache existentes en vez de duplicarlos. Con esto **toda la propuesta PROPUESTA_SEGURIDAD_DISPOSITIVO.md queda implementada** (fases P0 a P5); no queda ningún punto pendiente del documento. Pendiente: que el usuario aplique el `schema.sql` actualizado (columna, CHECK y bucket nuevos) y pruebe el flujo completo en su teléfono, ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 120 (Fase P4 de PROPUESTA_SEGURIDAD_DISPOSITIVO.md: migración asistida de credenciales de equipo) quedó terminada y archivada el 2026-07-21: pantalla `/boveda/migrar` que detecta secretos vinculados a un solo equipo o con su misma IP heredada, muestra un informe previo (valores enmascarados, igual que el resto de la bóveda) y, al confirmar, mueve el contenido a "Seguridad" del equipo y elimina el secreto de la Bóveda. `BovedaPage.tsx` avisa cuando hay candidatas. De la propuesta original solo queda P5 (archivo seguro cifrado), anotada como opcional y sin agendar. Pendiente: que el usuario lo pruebe en su teléfono, ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 119 (Fase P3 de PROPUESTA_SEGURIDAD_DISPOSITIVO.md: los cinco tipos de secreto, validaciones y buscador) quedó terminada y archivada el 2026-07-21: `CredencialForm.tsx` tiene ahora un selector real de tipo (cuenta, red, llave, archivo, nota), la hoja "Crear" de la Bóveda ofrece los cinco, hay un aviso anti duplicidad que enlaza directo a crear el campo protegido en la ficha del equipo, y el buscador global suma las ubicaciones (los campos protegidos ya estaban indexados desde P1). Pendiente: que el usuario lo pruebe en su teléfono, ver el detalle en TAREAS_ARCHIVO.md.

Antes, la tarea 118 (el diálogo de sincronización se salía por arriba de la pantalla cuando había errores, reportado por el usuario) quedó terminada y archivada el 2026-07-21: `src/components/Modal.tsx` no limitaba el alto de la tarjeta ni hacía scroll interno, así que en móvil el contenido largo (varios errores de sincronización) empujaba la tarjeta fuera del viewport por arriba, e inalcanzable porque el propio modal bloquea el scroll del fondo. Corregido con `max-h-[calc(100dvh-2rem)] overflow-y-auto` en la tarjeta. El error que disparaba el diálogo no es un bug: falta aplicar el `schema.sql` actualizado de la fase P1 en el Supabase real del usuario (la tabla `campos_protegidos` aún no existe ahí), paso ya pendiente desde la tarea 116. Pendiente: que el usuario confirme en su teléfono que el panel ya se ve completo.

Antes, la tarea 117 (Fase P2 de PROPUESTA_SEGURIDAD_DISPOSITIVO.md: vínculo polimórfico "Vincular información protegida" en pasos y tareas, más el grafo) quedó terminada, verificada y archivada el 2026-07-21. Cierra el hueco que había dejado P1 (dispositivos con campos protegidos huérfanos al eliminarse): verificado en navegador real que el diálogo de eliminar un equipo ahora avisa "Se usa en 1 artículo y 1 dato protegido".

Sigue pendiente el paso del usuario de la tarea 116 (Fase P1): aplicar `supabase/schema.sql` en el panel de Supabase (ver la sección 1 de `supabase/INSTRUCCIONES.md`, apartado "Actualización del 2026-07-21"). Hasta que se aplique, la sección Seguridad funciona en local pero `campos_protegidos` no sincroniza (el motor aísla su fallo, el resto de tablas sigue bajando con normalidad).

Antes, la tarea 116 (Fase P1 de PROPUESTA_SEGURIDAD_DISPOSITIVO.md: campos protegidos del dispositivo) quedó terminada, verificada y archivada el 2026-07-21.

Antes, la tarea 115 (Fase P0 de PROPUESTA_SEGURIDAD_DISPOSITIVO.md: quitar el preset "Equipo o servicio" y el campo IP del editor de la Bóveda, renombrar a "Secretos") quedó terminada, verificada y archivada el 2026-07-21.

Antes, la tarea 114 (`dispositivos.foto` faltaba en `adjuntosOffline.ts`) quedó terminada, verificada y archivada el 2026-07-20.

Antes, la tarea 101 (Fase 5 de la auditoría de limpieza: opcionales restantes) quedó terminada y archivada el 2026-07-20, con los 3 puntos resueltos según la decisión del usuario: (1) borrado de `diseno/` y el zip, dejado como paso del usuario (borrado permanente e irrecuperable, instrucciones exactas dadas); (2) script de huérfanos de Storage construido y verificado; (3) migración de `text-[Npx]` a `rem`, declinada por ahora. Del script salió un hallazgo nuevo: `src/lib/adjuntosOffline.ts` no incluye `dispositivos.foto` entre las referencias que cachea "Descargar todo para offline" (tarea 114, en "Por hacer").

Antes, la tarea 79 (Modo Asistente Fase 2: captura de evidencia por paso) quedó terminada, verificada y archivada el 2026-07-20, aplicando la opción recomendada (b): intervención en el historial del equipo afectado, sin esquema.

Antes, la tarea 113 (Historial al sistema Nocturne) quedó terminada, verificada y archivada el 2026-07-20. Se dio por hecho entonces que con ella TODA la app quedaba en Nocturne, "sin ningún resto del tema heredado en todo el proyecto". **Corregido el 2026-07-22** (al migrar el `ErrorBoundary`, tarea 138): esa afirmación no era exacta. Están migradas todas las PANTALLAS, pero quedan 12 archivos de componentes compartidos y de autenticación con `slate-*`/`sky-*`, más el color de marca de `index.html` y del manifiesto. Lista completa y método en la tarea 139, en "Por hacer".

Antes, la tarea 77 (migrar la ficha de categoría a Nocturne) quedó terminada, verificada y archivada el 2026-07-20. Con ella se eliminó el `Layout.tsx` heredado y sus dos componentes huérfanos. De paso se repuso el acceso a Cuenta desde el teléfono, que el Layout era el único en ofrecer.

Antes, la tarea 107 (overrides de alto de botones, hallazgo de la tarea 106) quedó terminada, verificada y archivada el 2026-07-20: los 13 botones afectados quedaron altos de verdad (44px), la opción que el usuario eligió.

Antes, la tarea 112 (Fase J5 de la jornada del técnico: editor de artículo en pestañas) quedó terminada, verificada y archivada el 2026-07-20. Con ella se cerró también la tarea 74, absorbida: sus 4 funciones ocultas quedaron repuestas. Las cinco fases J1 a J5 de [PROPUESTA_JORNADA_TECNICO.md](PROPUESTA_JORNADA_TECNICO.md) están implementadas; del documento quedan pendientes solo las decisiones D3 a D5 del usuario.

Antes, la tarea 111 (Fase J4 de la jornada del técnico: Etiquetas reutilizables) quedó terminada, verificada y archivada el 2026-07-20.

Antes, la tarea 110 (Fase J3 de la jornada del técnico: Completitud de la ficha del dispositivo) quedó terminada, verificada y archivada el 2026-07-20.

Antes, la tarea 109 (Fase J2 de la jornada del técnico: Actividad reciente del equipo) quedó terminada, verificada y archivada el 2026-07-20, aplicando la decisión D2 con su opción recomendada (solo bloque en Inicio, sin pantalla completa todavía).

Antes, la tarea 108 (Fase J1 de la jornada del técnico: Favoritos, primera fase de [PROPUESTA_JORNADA_TECNICO.md](PROPUESTA_JORNADA_TECNICO.md)) quedó terminada, verificada y archivada el 2026-07-20; con ella la decisión D1 quedó aplicada con la opción recomendada (favoritos personales por dispositivo, tabla local Dexie versión 11, sin esquema en Supabase; si el equipo luego los quiere compartidos, el salto a esquema se agrupa en un lote futuro). Dos tareas se hicieron en paralelo (sesiones distintas) y quedaron terminadas, verificadas y archivadas el 2026-07-19: la 97 (migrar Cuenta, Seguridad y Sugerencias del equipo a Nocturne, sin mockup; del Layout heredado solo queda `CategoriaPage`, tarea 77, y la redirección de `/notas`) y la 106 (variantes de color del botón fantasma: los `text-noct-error`/`text-noct-accent` escritos detrás de `BTN_GHOST` no se aplicaban; generaliza a 9 pantallas la corrección puntual que la tarea 105 había hecho con `BTN_ICONO_PELIGRO`). De la tarea 106 salió la tarea 107, en "Por hacer": el mismo empate de especificidad afecta a los overrides de alto de los botones.

Antes, la tarea 105 (re-autoría de la Ficha de Credencial a Nocturne, a partir del handoff `Ficha de Credencial.dc.html`) quedó terminada, verificada y archivada el 2026-07-19. Con esto TODA la Bóveda está en Nocturne y ninguna de sus pantallas pasa ya por el Layout heredado; de la tarea 97 quedan solo Cuenta, Seguridad y Sugerencias del equipo (sin mockup).

Antes, la tarea 104 (re-autoría del Editor de Credencial a Nocturne con los presets de campos por tipo de D-018, a partir del handoff `Editor de Credencial.dc.html`) quedó terminada, verificada y archivada el 2026-07-19. Con esto de la Bóveda solo queda por re-autorizar la Ficha de Credencial (`CredencialPage`, handoff `Ficha de Credencial.dc.html`), dentro de la tarea 97.

Antes, la tarea 103 (Bóveda: hojas "Crear" y menú de acciones por fila, que completan las dos hojas inferiores interactivas del handoff `Bóveda.dc.html`) quedó terminada, verificada y archivada el 2026-07-19. Con esto la pantalla principal de la Bóveda queda 100 % a la par del mockup.

Antes, la tarea 102 (re-autoría de la Bóveda -lista y pantalla de bloqueo- al sistema Nocturne, a partir del handoff `Bóveda.dc.html`) quedó terminada, verificada y archivada el 2026-07-19. Con esto la tarea 97 (migrar el resto del Layout heredado) queda reducida: falta solo Cuenta, Seguridad, Sugerencias del equipo y las subpantallas de credencial (Ficha/Editor de Credencial, que necesitan su propio handoff).

Antes, la tarea 100 (auditoría técnica de limpieza, Fase 5: useDeferredValue en el buscador de Inicio) quedó terminada, verificada y archivada el 2026-07-19. De los 4 puntos opcionales de la Fase 5, el usuario eligió solo este; los otros 3 quedan en "Por hacer" (tarea 101).

Antes, la tarea 98 (auditoría técnica de limpieza, Fase 4: endurecimiento del motor de sincronización) quedó terminada, verificada y archivada el 2026-07-19.

Antes, la tarea 96 (auditoría técnica de limpieza, Fase 3: poda de TAREAS.md) quedó terminada y archivada el 2026-07-19. El historial completo de tareas ya archivadas vive únicamente en [TAREAS_ARCHIVO.md](TAREAS_ARCHIVO.md); esta sección ya no repite esos párrafos (ver la tarea 96 en el archivo para el detalle de la poda y dos huecos de archivado que corrigió).

## Por hacer

**REDISEÑO DEL FLUJO DEL TÉCNICO (auditoría del 2026-07-23, [AUDITORIA_FLUJO_INSTALACION.md](AUDITORIA_FLUJO_INSTALACION.md)).** Tareas registradas con el formato de la regla 19. **Fases 1, 2 y 3 IMPLEMENTADAS y verificadas el 2026-07-23** (H1 a H6 completadas; H5 es una decisión aplicada). El usuario pidió construir también H6 (deferida en la auditoría original) tras revisar el análisis. Verificación conjunta: lint, typecheck y build en verde; 1621 pruebas pasan (los mismos 4 fallos preexistentes y ajenos de `archivosPendientes.test.ts`, RLS de Storage con el `.env` real de esta sesión). DOCUMENTACION_FUNCIONAL.md e Historial de cambios actualizados en la misma tarea. **Sin verificar en navegador** (la app exige login de técnico real, no disponible en esta sesión).

**Paso del usuario RESUELTO (2026-07-24)**: se aplicó `supabase/schema.sql` completo en el SQL Editor de Supabase. Verificado por REST que `articulos.aplica_a` ya existe (consulta a la columna ya no devuelve "column does not exist"). Con esto la tarea 166 (H6) queda sin ningún paso pendiente y el guardado de artículos vuelve a funcionar con normalidad.

**DEUDA DETECTADA EN LA AUDITORÍA DE DOCUMENTACIÓN (tarea 167, 2026-07-24).** Verificación código vs documentación con 6 agentes; las discrepancias documentales se corrigieron en la misma tarea 167 (ver [CHANGELOG.md](CHANGELOG.md)). Lo que sigue es la deuda de CÓDIGO detectada de paso, registrada aquí sin implementarla (la tarea 167 es solo de documentación).

**REDISEÑO DE LA SECCIÓN SOLUCIONES (handoff "Auditoría de Soluciones TI", autorizado el 2026-07-27).** El `.dc.html` documenta cinco pantallas con su antes y después. **P1 implementada (tarea 171, archivada).** Las cuatro restantes van abajo como tareas 172 a 175, una por pantalla (regla 15: cada pantalla es su propia tarea, una "En proceso" a la vez). Todas comparten las reglas visuales R1 a R7 de [DECISIONES.md](DECISIONES.md) AD-019 y los componentes ya creados en la 171. El handoff vive en `Auditoría de Soluciones TI-handoff.zip` en la raíz del repo.

### 172. Rediseño P2: ficha de artículo (`/soluciones/:categoria/:articulo`)

- **Descripción:** aplicar las 11 decisiones de la auditoría para P2. Una sola acción dominante fija abajo con etiqueta contextual ("Empezar" / "Seguir en el paso 3 de 6" / "Repetir") en vez de "Ejecutar" y "Editar" con el mismo peso arriba; cabecera de tres controles de 44 px; kicker de tipo sobre el título en su matiz; metadatos como lista de definición con rótulo (Tiempo, Dificultad, Aplica a, Versión); pasos plegados salvo el actual; progreso segmentado junto al título en vez de barra pegajosa aparte; casillas de 24 px en filas de 44 y texto de tarea a 15 px; la verificación final se anuncia en vez de mostrarse apagada; el aviso lleva su palabra ("Precaución.") además del color; datos protegidos con acción de ancho completo; estado una sola vez.
- **Motivo:** con 6 pasos, avisos, imágenes y credenciales el documento pasa de 4.000 px, y "Ejecutar" (lo que se toca de pie frente al equipo) está arriba, en la zona menos alcanzable del pulgar.
- **Impacto:** el documento cabe en dos pantallas; la acción principal queda al alcance del pulgar y dice qué va a pasar.
- **Prioridad:** Alta. **Estado:** Pendiente.
- **Área afectada:** `src/features/soluciones/ArticuloPage.tsx` (526 líneas), `src/features/soluciones/ProcedimientoVista.tsx` (959 líneas). Componentes nuevos que salen aquí: `BarraAccionFicha` (barra inferior de una acción, la comparte con P5) y migrar `ContadorSubProgreso` a `IndicadorAvance` (CAND-7).
- **Dependencias:** ninguna bloqueante; reutiliza `IndicadorAvance` y `PastillaEstado` de la tarea 171.

### 173. Rediseño P3: modo ejecución del asistente (`/soluciones/:cat/:art/ejecutar`)

- **Descripción:** aplicar las 9 decisiones de la auditoría para P3. Navegación fija abajo (retroceso en cuadrado de 52 px, avance ocupando el resto); el botón dice qué falta y qué viene ("Falta 1 tarea para poder avanzar" encima, "Sigue: calibrar la etiqueta" dentro) en vez de un control apagado y mudo al 30 % de opacidad; "Guardar y salir" en lugar de "Salir"; progreso segmentado de N marcas que distingue hecho (verde) de actual (acento) y sirve para volver a un paso; escala de campo (título 21 px, tareas 16 px, casillas de 26 px en filas de 52); cronómetro en lenguaje humano ("7 min de 25"); una foto por fila a ancho completo; la evidencia fotográfica sube a acción de bloque con borde punteado; tarea completada tachada y en gris con la casilla rellena en acento.
- **Motivo:** es la pantalla que se usa de pie, con guantes, a contraluz, y hoy tiene tipografía de escritorio, el avance al final del scroll y un "Siguiente" que se apaga sin decir por qué.
- **Impacto:** el técnico deja de recorrer toda la pantalla para avanzar y entiende siempre por qué no puede.
- **Prioridad:** Alta. **Estado:** Pendiente.
- **Área afectada:** `src/features/soluciones/AsistenteVista.tsx` (632 líneas), `src/features/soluciones/AsistentePage.tsx`, `src/features/soluciones/useProcedimientoEjecucion.ts`.
- **Dependencias:** conviene hacerla después de la 172, que define `BarraAccionFicha`.

### 174. Rediseño P4: ficha de categoría (`/soluciones/:categoria`)

- **Descripción:** aplicar las 8 decisiones de la auditoría para P4. "Crear" en toda la sección (hoy aquí dice "Artículo" y en la lista "Crear"), con hoja que pregunta qué crear (artículo, equipo o diagnóstico) dentro de la categoría; **usar `FilaDispositivo` en vez de la copia a mano** (cierra CAND-2); **un solo indicador de avance, el anillo** (`IndicadorAvance`), que no cambia el ancho de la fila y reemplaza las tres variantes actuales (cierra CAND-7); "Sin terminar · paso 4 de 6" en palabras en la segunda línea; grupos con su icono de tipo, su conteo y su "Agregar"; los conteos de la cabecera como accesos a su bloque; la identidad de la categoría extendida a una regla de 2 px bajo la cabecera en su matiz; estado de equipo como `PastillaEstado` de contorno (cierra el resto de CAND-1).
- **Motivo:** la fila de equipo está escrita a mano aquí y con `FilaDispositivo` en Dispositivos y Red, y ya divergen en radios, tamaños de icono y punto de estado. El chip de avance solo aparece si hay avance, así que la columna derecha baila entre filas.
- **Impacto:** cierra tres candidatos de duplicación visibles en pantalla (CAND-1, CAND-2, CAND-7) y unifica el vocabulario de creación de la sección.
- **Prioridad:** Alta. **Estado:** Pendiente.
- **Área afectada:** `src/features/soluciones/CategoriaPage.tsx` (245 líneas; `AvanceArticulo` en las líneas 225-245 se elimina), `src/components/FilaDispositivo.tsx`, `src/features/soluciones/FilaArticulo.tsx` (migrar esta pantalla a él; hoy solo lo usa `SolucionesPage`).
- **Dependencias:** `FilaArticulo`, `IndicadorAvance` y `PastillaEstado` ya existen (tarea 171). Al migrar hay que decidir qué pasa con la **miniatura de portada** que hoy muestra esta fila y que `FilaArticulo` no soporta (la auditoría especifica recuadro neutro + glifo de tipo).

### 175. Rediseño P5: editor de artículo (`/soluciones/:categoria/nuevo`)

- **Descripción:** aplicar las 11 decisiones de la auditoría para P5. Barra inferior de una sola fila (48 px + seguro) con el anillo de completitud y "N por hacer" que abre la lista en hoja, en vez de los 190 px que come hoy con las sugerencias abiertas; pendientes contados en la pestaña con una pastilla numérica legible en vez de un punto de 5 px en `neutral-600`; el título primero, con ayuda de redacción; "obligatorio" escrito en ámbar en vez de un asterisco rojo; rejilla de tipo neutra con check en la elegida (el color del tipo se reserva para donde identifica contenido); vincular equipos con hoja de búsqueda en vez de `<select>` nativo; etiquetas con "+ Agregar" visible y las del equipo ofrecidas debajo; "Guardado en este equipo" en la cabecera; "Descartar" con X en lugar de "Cancelar"; "Vista previa" al menú "···"; copy que explica la consecuencia.
- **Motivo:** en un teléfono de 667 px quedan unos 330 px de formulario visible, la validación llega tarde y sin mover el foco, y dos `<select>` nativos con borde punteado parecen campos deshabilitados.
- **Impacto:** más formulario visible, menos dependencia del teclado móvil y la promesa explícita de que nada se pierde.
- **Prioridad:** Media. **Estado:** Pendiente.
- **Área afectada:** `src/features/soluciones/ArticuloForm.tsx` (1389 líneas, la más grande de la sección), `src/features/soluciones/completitudArticulo.ts`, `src/features/soluciones/etiquetas.ts`. Componente nuevo: `SelectorEntidad` (buscar y vincular en hoja; camino hacia CAND-6).
- **Dependencias:** `BarraAccionFicha` de la tarea 172. **No incluye** `PasosEditor.tsx` ni `VistaPreviaArticulo.tsx`: la auditoría los dejó fuera explícitamente (ver tarea 177).

### 176. Rediseño de escritorio de las cinco pantallas de Soluciones

- **Descripción:** la auditoría resolvió las cinco pantallas solo a 448 px y dejó el escritorio como pendiente explícito. Falta rediseñar el rail de categorías y la rejilla de 2 y 3 columnas de `SolucionesPage` a partir de 1280 px, y la ficha de artículo a dos columnas.
- **Motivo:** al implementar P1 se conservó el layout de escritorio existente (decisión del usuario, [DECISIONES.md](DECISIONES.md) AD-021), así que durante un tiempo conviven dos criterios en el mismo componente: el eje "Tipo" va plegado en hoja en móvil y desplegado en el rail en escritorio.
- **Impacto:** un solo criterio visual en todas las anchuras; hoy la incoherencia es interna y no rompe nada.
- **Prioridad:** Media. **Estado:** Pendiente.
- **Área afectada:** las cinco pantallas de `src/features/soluciones/`, en sus tramos `xl:` y `@lg`/`@4xl`.
- **Dependencias:** conviene hacerla al final, cuando las cinco pantallas móviles estén cerradas.

### 177. Pendientes que la auditoría de Soluciones dejó fuera a propósito

- **Descripción:** ocho frentes que la auditoría nombró como no resueltos. (a) **Editor de pasos** (`PasosEditor.tsx`, 1035 líneas) y **vista previa** (`VistaPreviaArticulo.tsx`): son la mitad de la pestaña Pasos y merecen su propia pasada. (b) **Diálogo de eliminar** con contraseña maestra y aviso de impacto: es transversal, se ve con Bóveda. (c) **Procedimientos anidados** (subprocedimiento, solución y decisión dentro de un paso): la regla de un solo nivel funciona, pero apilan tres marcos de color y hay que decidir su jerarquía. (d) **Historial al pie de cada ficha**: componente compartido por 8 entidades, se audita completo aparte. (e) **Lectura sin conexión de adjuntos**: hoy el hueco dice "usa Descargar todo para offline" y falta el camino desde ahí. (f) **Accesibilidad de lector de pantalla**: el stepper usa `button role="checkbox"` sin agrupar; queda revisar el orden de lectura y los `aria-label` de la ficha. (g) **Modo alto contraste / texto grande** para trabajo a pleno sol, como ajuste global y no por sección. (h) **Miniatura de portada en la fila de artículo**, que `FilaArticulo` no soporta hoy (ver tarea 174).
- **Motivo:** dejarlos registrados para que no se pierdan al cerrar el handoff, en vez de descubrirlos otra vez más adelante.
- **Impacto:** varios son transversales (accesibilidad, alto contraste, historial) y afectan a más secciones que Soluciones.
- **Prioridad:** Media (f y g suben a Alta si el equipo reporta problemas de lectura en campo). **Estado:** Pendiente.
- **Área afectada:** ver cada punto.
- **Dependencias:** (b) depende del rediseño de Bóveda; (d) de la auditoría del historial.

### 178. Worktree obsoleto que ensucia `npm test`

- **Descripción:** `.claude/worktrees/dazzling-benz-13478d` (rama `claude/dazzling-benz-13478d`, commit `330ab47`) es un worktree de una sesión anterior que sigue en el disco. Vitest recoge sus archivos de prueba, así que `npm test` ejecuta **toda la suite dos veces** y reporta 4 fallos en vez de 2 (los mismos de `archivosPendientes.test.ts`, duplicados). Revisar si la rama tiene trabajo que rescatar y, si no, retirar el worktree con `git worktree remove`; en cualquier caso, excluir `.claude/**` de `test.exclude` en `vite.config.ts`.
- **Motivo:** hoy `npm test` tarda el doble y su salida confunde sobre cuántos fallos hay realmente y de dónde vienen.
- **Impacto:** pruebas más rápidas y un informe de fallos fiable. Sin riesgo para producción.
- **Prioridad:** Baja. **Estado:** Pendiente.
- **Área afectada:** `vite.config.ts` (bloque `test`, líneas ~84-85) y el worktree del disco.
- **Dependencias:** ninguna. **Cuidado:** no borrar el worktree sin revisar antes que su rama no tenga commits sin integrar.

### 168. Extraer los componentes duplicados detectados (candidatos CAND-1 a CAND-7)
- Descripción: extraer a componentes compartidos los patrones hoy duplicados a mano (detalle en [COMPONENTES_UI.md](COMPONENTES_UI.md), sección 5): CAND-1 "punto de estado + etiqueta" (5 copias + `pillEstado` paralelo en `DispositivoPage`) a un `IndicadorEstado` con variante `texto|pastilla|punto`; CAND-2 `CategoriaPage` reusa `FilaDispositivo`; CAND-3 unificar "copiar con confirmación"; CAND-4 `FormularioDesbloqueo` compartido entre `CredencialEnPaso` y `SeguridadDelEquipo`; CAND-5 `HojaInferior` de `BovedaPage` sobre `Modal` con `createPortal`; CAND-6 `SelectorEntidadConAlta` para `SelectorUbicacion`/`SelectorPersona`/`FormularioConexion`; **CAND-7** (nuevo el 2026-07-27) el avance "X de Y pasos" dibujado por separado en `AvanceArticulo`, `ContadorSubProgreso` y la barra pegajosa.
- Motivo: la duplicación ya causó deriva visual en el pasado (tareas 124, 129); CAND-1 obliga a mantener dos mapas de estado en paralelo; CAND-5 está expuesto al mismo bug de portal que `Modal` ya resolvió.
- Impacto: mantenibilidad; menos superficie para deriva. Sin cambio de comportamiento visible.
- Prioridad: **Media**. Estado: **En progreso parcial** (2026-07-27). Área afectada: `src/components/`, `src/features/dispositivos/`, `src/features/boveda/`, `src/features/red/`, `src/features/soluciones/CategoriaPage.tsx`. Dependencias: ninguna.
- **Avance del 2026-07-27 (tarea 171, rediseño de P1):** los componentes que pedían CAND-1 y CAND-7 **ya existen** y están en uso: `PastillaEstado` (el `IndicadorEstado` de CAND-1, hoy con los estados de artículo) e `IndicadorAvance` (CAND-7, con variantes `anillo|barra|texto`). Lo que falta de cada uno es **migrar las copias**, y eso está agendado dentro del rediseño de la sección: CAND-1 (estado de equipo) y CAND-2 en la tarea 174, CAND-7 en las tareas 172 y 174. CAND-3, CAND-4, CAND-5 y CAND-6 siguen intactos; el `SelectorEntidad` de la tarea 175 es el primer paso de CAND-6.

### 169. Deuda técnica de seguridad detectada en la auditoría
- Descripción: (TD-1) la política INSERT de `historial` usa `with check (true)` sin distinguir `entidad_tipo`, mientras que su SELECT sí exige `puede_ver_boveda()` para `credencial`/`campo_protegido` (`supabase/schema.sql`); alinear el INSERT con el SELECT. (TD-2) el bucket de Storage `adjuntos` permite a cualquier autenticado actualizar o borrar objetos de otros sin comprobación de propietario; decidir explícitamente si se restringe.
- Motivo: TD-1 es una grieta de integridad del registro de auditoría (no de confidencialidad); TD-2 es coherente con un equipo de confianza de 5 pero conviene decidirlo a conciencia, no como efecto colateral.
- Impacto: robustez de la auditoría y de Storage. Cambios de RLS: requieren editar `schema.sql` y reaplicarlo (regla 17).
- Prioridad: **Media**. Estado: **Pendiente**. Área afectada: `supabase/schema.sql` (políticas de `historial` y del bucket `adjuntos`). Dependencias: ninguna.

### 170. Mejoras del buscador detectadas en la auditoría
- Descripción: (a) pintar la miniatura de portada en los resultados: `portadaRef` ya se calcula y viaja en `ResultadoBusqueda` pero `FilaResultado` (`InicioPage.tsx`) usa solo un icono genérico (posible regresión respecto de lo documentado). (b) unificar las 3 funciones de normalización de acentos hoy separadas (`texto.ts`, `sinonimos.ts`, `iconosSoluciones.ts`). (c) evaluar un tope de resultados en el buscador global (hoy no hay `slice` ni paginación). Detalle en [BUSCADOR.md](BUSCADOR.md), sección 12.
- Motivo: (a) el usuario ve un icono genérico donde debería ver la portada; (b) tres implementaciones equivalentes son deuda de mantenibilidad; (c) sin tope, un crecimiento grande del contenido degradaría el render.
- Impacto: fidelidad visual del buscador y mantenibilidad. Bajo riesgo.
- Prioridad: **Baja**. Estado: **Pendiente**. Área afectada: `src/features/inicio/InicioPage.tsx`, `src/features/busqueda/useIndiceBusqueda.ts`, `src/lib/texto.ts`, `src/features/soluciones/iconosSoluciones.ts`. Dependencias: ninguna.

### 160. Procedimientos e incidencias por categoría en la ficha del equipo (hallazgo H1)
- Descripción: en la ficha del dispositivo, además de los procedimientos/incidencias vinculados por `dispositivosAfectados` ("específicos de este equipo"), mostrar "De esta categoría": los publicados cuyo `categoria_id` coincide con la categoría del equipo. Consulta derivada, sin esquema. Separar visualmente lo específico de lo general; "Ver todos" a `/soluciones?categoria=<id>`.
- Motivo: hoy un procedimiento genérico ("Instalar impresora de red") no aparece en una impresora concreta salvo que se vincule equipo por equipo. El diagnóstico ya se ofrece por categoría (inconsistencia).
- Impacto: cierra el paso 7 del caso de uso; el técnico resuelve desde la ficha sin cambiar de módulo; escala sin vínculos manuales.
- Prioridad: **Alta**. Estado: **Completada** (2026-07-23): `procedimientosDeCategoria`/`problemasDeCategoria` + sub-rótulo "De la categoría {X}" con "Ver todos". 8 pruebas nuevas en verde.
- Área afectada: `src/features/dispositivos/procedimientosDeDispositivo.ts`, `problemasDeDispositivo.ts`, `ProcedimientosDelEquipo.tsx`, `ProblemasDelEquipo.tsx`, `DispositivoPage.tsx`.
- Dependencias: ninguna.

### 161. El escáner precarga el código leído al registrar un equipo (hallazgo H3)
- Descripción: "Registrar equipo" desde el escáner abre `/dispositivos/nuevo?serial=<código>` y `DispositivoForm` precarga el serial (editable).
- Motivo: hoy se reescribe el serial/placa que la app ya leyó (viola "nunca escribir dos veces").
- Impacto: un dato menos que teclear; flujo escáner -> alta sin fricción.
- Prioridad: **Media**. Estado: **Completada** (2026-07-23).
- Área afectada: `EscanerPage.tsx`, `DispositivoForm.tsx`.
- Dependencias: ninguna.

### 162. Inicio: atajo "Registrar equipo" y "Crear" ante búsqueda sin resultados (hallazgo H9)
- Descripción: añadir un atajo "Registrar equipo" a la rejilla de atajos de Inicio; en el estado "Sin coincidencias" del buscador global, ofrecer "Crear dispositivo" (con el texto buscado como nombre).
- Motivo: el técnico que recibe hardware no tiene arranque directo; el buscador sin resultados no ofrece crear.
- Impacto: menos clics y menos navegación al empezar un trabajo.
- Prioridad: **Media**. Estado: **Completada** (2026-07-23).
- Área afectada: `InicioPage.tsx`.
- Dependencias: ninguna.

### 163. Reutilización de procedimientos más visible en el editor (hallazgo H4)
- Descripción: subir la descubribilidad de "Reutilizar procedimiento" (subprocedimiento) en `PasosEditor`, hoy en la sección plegada "Vínculos del paso". Solo presentación; la composición ya existe y funciona.
- Motivo: riesgo de copiar pasos en vez de referenciarlos por desconocimiento de la función.
- Impacto: fomenta la composición; menos duplicación.
- Prioridad: **Media**. Estado: **Completada** (2026-07-23): botón "Reutilizar" en la fila de bloques del paso.
- Área afectada: `PasosEditor.tsx`.
- Dependencias: ninguna.

### 164. Elegir "para este equipo" o "para la categoría" al documentar desde la ficha (hallazgo H2)
- Descripción: al "Documentar procedimiento" desde la ficha del equipo, ofrecer explícitamente crear el artículo vinculado al equipo o solo a su categoría. NO agregar marca/modelo como campos del artículo (sería duplicación; el artículo referencia el equipo).
- Motivo: aprovechar H1 para que un procedimiento nazca ya aplicable a toda la categoría cuando corresponde.
- Impacto: coherencia con el principio; menos campos que llenar.
- Prioridad: **Media**. Estado: **Completada** (2026-07-23): la ayuda de "Equipos donde aplica" aclara la aplicación por categoría (aprovecha H1); no se agregaron marca/modelo al artículo (sería duplicación). Depende de la 160 (H1).
- Área afectada: `DispositivoPage.tsx`, `ArticuloForm.tsx`.

### 165. Decisión: mantener 4 pestañas en el editor de artículo (hallazgo H5)
- Descripción: NO convertir el editor a 7 pestañas (empeoraría móvil; el flujo lineal ya existe dentro de las 4). Ajuste opcional: mover "Equipos donde aplica" de General a Detalles una vez implementado H1.
- Motivo: mobile-first; evitar un retroceso de UX.
- Impacto: se conserva la ergonomía móvil.
- Prioridad: **Baja** (decisión, no cambio). Estado: **Completada** (2026-07-23): decisión aplicada de MANTENER 4 pestañas (registrada en la auditoría y en el Historial de cambios). No se movió "Equipos donde aplica" (ajuste opcional descartado por churn marginal).
- Área afectada: `ArticuloForm.tsx`, `completitudArticulo.ts`.

### 166. "Aplica a" por marca/modelo/versión (hallazgo H6, con esquema, DEFERIDA)
- Descripción: criterio `aplica_a` (JSON con marca/modelo/versión opcionales) en el artículo, emparejado contra el equipo, para procedimientos que aplican a un modelo concreto dentro de una categoría.
- Motivo: precisión fina de aplicabilidad más allá de la categoría (H1).
- Impacto: marginal una vez implementado H1; la mayoría de procedimientos aplican a nivel categoría.
- Prioridad: **Baja** (originalmente deferida; el usuario pidió construirla el mismo día). Estado: **Completada** (2026-07-23). Columna `articulos.aplica_a` (jsonb nullable `{marca, modelo}`) en `tablas.ts` y `supabase/schema.sql`; módulo puro `src/features/soluciones/aplicaA.ts` (`aplicaAlDispositivo`, `aplicaADesdeFormulario`, `describirAplicaA`, con 8 pruebas); `procedimientosDeCategoria`/`problemasDeCategoria` aceptan un criterio `{marca, modelo}` opcional (5 pruebas nuevas); editor con dos campos "Restringir a marca o modelo (opcional)" con sugerencias del inventario; ficha del artículo muestra una etiqueta cuando restringe; historial con texto legible. **Paso del usuario aplicado (2026-07-24)**: `supabase/schema.sql` ya está aplicado en Supabase, verificado por REST que `articulos.aplica_a` existe.
- Área afectada: `src/lib/db.ts`, `src/lib/tablas.ts`, `supabase/schema.sql`, `src/lib/repositorio.ts`, `src/features/historial/textoHistorial.ts`, `src/features/soluciones/aplicaA.ts` (nuevo), `ArticuloForm.tsx`, `ArticuloPage.tsx`, `ProcedimientosDelEquipo.tsx`, `ProblemasDelEquipo.tsx`, `DispositivoPage.tsx`, `procedimientosDeDispositivo.ts`, `problemasDeDispositivo.ts`.
- Dependencias: 160 (H1).

**AUDITORÍA INTEGRAL DE FLUJOS (2026-07-21)**: recorrido de procesos reales de TI (no pantalla por pantalla), documentado en [AUDITORIA_FLUJOS_TI.md](AUDITORIA_FLUJOS_TI.md). Se hizo con 3 agentes en paralelo (red, ciclo de vida/bóveda, base de conocimiento) más el análisis del modelo completo, todo anclado a `archivo:linea` y contrastado contra las propuestas para separar lo NUEVO de lo ya planeado. 30 hallazgos con ID estable. Veredicto: el principio "cada dato una sola vez" se cumple ~85%; los huecos reales son estructurales y de alto valor. **La Fase de corrección (K1, S1, N1) quedó completa el 2026-07-22, tarea 131**, y los cuatro hallazgos ALTA que el usuario ordenó ese mismo día también: T1 (tarea 132), L1 (tarea 133), L2/L3 (tarea 134) y O1/O2/O3 (tarea 135). **El grupo K completo (K1 a K6, base de conocimiento) quedó resuelto el 2026-07-22 en las tareas 131, 140, 141 y 142: ningún hallazgo ALTA sigue pendiente.** El flujo 4 completo (S1 a S6, secretos y bóveda) quedó resuelto el 2026-07-23 (tareas 147 a 151), el grupo de red e incorporación (N2 a N5 y D1) se resolvió el mismo día (tareas 152 a 156), y D2 también (tarea 157). Lo que resta son D3, D4, T2, T3 y demás, ninguno de los cuales bloquea el uso diario.

[PROPUESTA_SEGURIDAD_DISPOSITIVO.md](PROPUESTA_SEGURIDAD_DISPOSITIVO.md) quedó COMPLETA el 2026-07-21: las seis fases (P0 a P5) están implementadas, verificadas y archivadas (tareas 115 a 121). Sin código pendiente; solo falta que el usuario aplique en Supabase los `schema.sql` acumulados de las fases con esquema (P1 y P5) y pruebe el flujo completo en su teléfono. Detalle de cada fase en TAREAS_ARCHIVO.md.

[PROPUESTA_REVISION_ARQUITECTURA.md](PROPUESTA_REVISION_ARQUITECTURA.md), revisión arquitectónica completa del 2026-07-17, quedó **COMPLETA el 2026-07-23**. No proponía funciones nuevas: proponía converger y extraer piezas compartidas. Estado final, verificado contra el código:
- **Fase 0 (fundaciones compartidas): HECHA COMPLETA.** 0a (tarea 124): primitivas de formulario en `src/components/campos.tsx` y `valoresUnicos` en `src/lib/vocabulario.ts`, con el editor clave/valor unificado entre Dispositivos y Bóveda. 0b (tarea 126): sistema de color por categoría (diez tokens OKLCH + `coloresCategoria.ts`), con la columna `categorias.color` por fin cableada. 0c (tarea 129, 2026-07-21): motor compartido `iconoPorPalabraClave` para los dos `iconoDeCategoria` (Soluciones y Bóveda) y rama muerta de `BotonVolver` eliminada; una sola fuente de los 4 estados de dispositivo en `topologiaVisual.ts` (huérfanos `infoDeEstado`/`IndicadorEstado.tsx` borrados); `texto()` extraída a `src/lib/texto.ts` y el `claseCampo` de `TopologiaEquipoPage.tsx` unificado con `campos.tsx`.
- **Fase 1 (convergencia de diseño): HECHA.** Todas las PANTALLAS están en Nocturne desde la tarea 113; los componentes compartidos, no del todo (tarea 139: quedan 12 archivos con `slate-*`/`sky-*`, hallazgo de la tarea 138, sin agendar). Los remates que faltaban se cerraron en la tarea 145 (2026-07-23): `esDeRed()`/`idsDeRed()` en `src/lib/categorias.ts` son ya el único dueño de la regla `es_red` (no queda ningún `.esRed` suelto), los 3 buscadores locales comparten `incluyeTexto()` de `src/lib/texto.ts`, y `<FilaDispositivo>` (`src/components/FilaDispositivo.tsx`) unifica la fila de Dispositivos y Red. Dos puntos de la propuesta se descartaron con motivo escrito: unificar esos buscadores CON EL ÍNDICE GLOBAL (rompería los borradores visibles en Soluciones, que el índice excluye a propósito) y `<FilaArticulo>` (no comparte interior con las otras dos filas). Ver el detalle en TAREAS_ARCHIVO.md.
- **Fase 2 (explotar el valor recolectado): HECHA.** El bucle sugerencia -> borrador de artículo (tarea 140, 2026-07-22). El anti duplicados de IP (tarea 144, 2026-07-23, `DispositivoForm.tsx:216`). El tablero de insights sobre `ejecuciones_diagnostico` (tarea 146, 2026-07-23: mismo punto que F3 de PROPUESTA_MODULOS.md, ver el párrafo de esa propuesta más abajo). Los avisos de vencimiento de credenciales en Inicio, en la tarea 122. Queda sin hacer solo un punto menor no listado en la propuesta original como bloqueante: las "recientes derivadas del historial" siguen siendo registro manual (nadie lo pidió como tarea aparte).
- **Fase 3 (evoluciones de esquema): HECHA** dentro del grupo N3 (tarea 70).

Pregunta abierta desde la tarea 62, nunca resuelta: decidir si el botón "Crear" de `src/features/red/RedPage.tsx` (hoy va a `/dispositivos/nuevo`, genérico) debería abrir en cambio un flujo dedicado a crear una conexión de red.

### 15. Respaldo automático de datos
- Descripción: workflow de GitHub Actions que cada domingo exporta las tablas de Supabase (el plan gratuito no tiene copias de seguridad), cifra el resultado con AES-256 y lo guarda como artefacto del workflow por 90 días. Lee los datos con un usuario dedicado de respaldo (respetando RLS, sin usar jamás la clave service_role, prohibida por REGLAS.md) cuyas credenciales viven en los secretos de GitHub Actions. El cifrado es obligatorio porque el repositorio es público y sus artefactos son descargables por cualquier usuario de GitHub.
- Prioridad: Media
- Ubicación: `scripts/respaldo-supabase.sh`, `.github/workflows/respaldo-supabase.yml`, guía en `supabase/RESPALDO.md`, nota en `ARQUITECTURA.md` sección 10.
- Avance: código completo y subido a GitHub (2026-07-03). Script `scripts/respaldo-supabase.sh` (inicio de sesión por la API de auth, exportación paginada de las tablas ordenadas por id, manifiesto con conteos, tar.gz cifrado con AES-256-CBC + PBKDF2 600k iteraciones; aviso si credenciales sale vacía por falta de puede_ver_boveda). Workflow con cron dominical, disparo manual y artefacto con retención de 90 días. Verificado localmente: sintaxis, fallo limpio sin variables, fallo limpio con credenciales falsas (HTTP 400 real contra Supabase), fusión de páginas, manifiesto, y ciclo completo de cifrado/descifrado con rechazo de clave equivocada (jq descargado temporalmente al scratchpad, no quedó en el proyecto).
- Avance (2026-07-10): la lista de tablas se actualizó a las 11 actuales (`conexiones`, `diagnosticos`, `ejecuciones_diagnostico` y `accesos_boveda` habían quedado fuera desde que se agregaron esas tablas); sintaxis verificada de nuevo.
- Bloqueada por (usuario): crear el usuario de respaldo en Supabase, darle puede_ver_boveda, cargar los 3 secretos en GitHub y disparar la primera ejecución manual, siguiendo `supabase/RESPALDO.md`. Al hacerlo, verificar desde aquí la ejecución real por la API de GitHub y archivar la tarea.

Propuestas presentadas al usuario el 2026-07-03, pendientes de que elija cuáles convertir en tareas: recordatorios de mantenimiento preventivo por dispositivo y reporte mensual de actividad desde el historial. (La tercera propuesta, cola de subida para adjuntos offline, ya se hizo: tarea 17 en el archivo.)

### 99. Protocolo de verificación de propagación entre PC y celular
- Descripción: propuesta del 2026-07-17 de endurecimiento del motor de sincronización colaborativa. Sus 3 mejoras de código (aislar errores de descarga por tabla, exponer el estado "sin sesión", detección de conflictos en el outbox) se hicieron en la tarea 98. Queda solo este punto (4 de la propuesta original), que es un paso del usuario, guiado: verificar en los dos dispositivos reales del equipo que un cambio hecho en uno se ve en el otro en segundos (canal de tiempo real) y que un conflicto real entre dos ediciones simultáneas aparece en el panel de sincronización de ambos.
- Prioridad: Baja
- Ubicación: paso del usuario; sin código pendiente.

Propuesta presentada al usuario el 2026-07-20: optimización de la jornada del técnico (encargo de 12 puntos "IT Brain como cerebro operativo"), analizada contra el código real en [PROPUESTA_JORNADA_TECNICO.md](PROPUESTA_JORNADA_TECNICO.md). Diagnóstico: 4 de los 12 puntos ya están hechos (dispositivo como nodo 360°, navegación cruzada, historial técnico, progreso de procedimientos), 2 son decisiones (entidad Archivo: se recomienda NO; nombre del módulo Soluciones: se recomienda mantener) y el trabajo real son 5 fases sin esquema. Las CINCO fases quedaron implementadas y archivadas el 2026-07-20, aplicando D1 y D2 con sus opciones recomendadas: J1 favoritos (tarea 108), J2 actividad reciente del equipo (tarea 109), J3 completitud de la ficha del dispositivo (tarea 110), J4 etiquetas reutilizables (tarea 111) y J5 editor en pestañas (tarea 112, que absorbió y cerró la tarea 74). Las decisiones D3 a D5 de la sección 5 del documento quedaron resueltas el 2026-07-21 (tarea 122): D4 (mantener "Soluciones") y D5 (bloque "Pendientes" en Inicio) con la opción recomendada; D3 (deduplicación por hash de adjuntos) se anotó primero como tarea futura y el usuario pidió construirla el mismo día, implementada en la tarea 123. **Con esto PROPUESTA_JORNADA_TECNICO.md queda 100 % cerrada**: sin fases ni decisiones pendientes. Pendiente: que el usuario valide en uso real lo construido.

Propuesta presentada al usuario el 2026-07-15 y hoy EJECUTADA (revisado el 2026-07-21): rediseño arquitectónico "base de conocimiento inteligente" (principio: cada dato existe una sola vez, todo lo demás lo referencia), analizada contra el código real en [PROPUESTA_BASE_CONOCIMIENTO.md](PROPUESTA_BASE_CONOCIMIENTO.md). Diagnóstico: el principio ya se cumple en un ~80 %; quedan 4 grietas estructurales: (1) ubicación como texto libre sin entidad, (2) copias de referencia sin regla de frescura (por ejemplo `lib/conexiones.ts:22-25` muestra nombres congelados), (3) falta el inverso universal "¿qué referencia a esto?" (se propone un grafo de referencias DERIVADO en `src/lib/grafo.ts`, nunca almacenado), (4) enlaces faltantes credencial↔dispositivo y artículo↔dispositivo (absorbe la tarea 39 fase 2). Fases N0 a N5; solo N3 (tabla `ubicaciones` + vínculos de credenciales + tipo 'relacionado' en conexiones) toca el esquema, agrupado en una sola intervención. **Decisiones RESUELTAS por el usuario el 2026-07-17** (fijan la forma de N3): (1) ubicaciones CON jerarquía, `padre_id` opcional; (2) vínculo credencial↔dispositivo SIN cifrar (lista `{id, nombre}` en claro dentro de `credenciales`, ya protegida por la RLS de bóveda; el contenido secreto sigue cifrado); (3) orden de las rutas de inicio en una COLUMNA de `articulos` (no en el JSON `procedimiento`, para que también ordene manuales sin pasos); (4) SÍ incluir el tipo `'relacionado'` en `conexiones` (relacionar equipos no-red) dentro de N3. TODAS las fases están construidas: N0/N1 (tarea 54), N2 (tarea 60), N4 (tarea 61) y N3, el único grupo con esquema (tarea 70, 2026-07-17), que incluyó la columna `color` de categorías. Sin código pendiente de la propuesta; solo restan adornos opcionales de N4/N5 sin esquema y el paso del usuario de aplicar el `schema.sql` de N3 en Supabase. Nota: el USO de la columna `color` (color derivado del `orden` + editor del override, Fase 0 de [PROPUESTA_REVISION_ARQUITECTURA.md](PROPUESTA_REVISION_ARQUITECTURA.md)) sigue SIN cablear: la columna existe pero nada la lee todavía.

Propuesta presentada al usuario el 2026-07-09 y ejecutada EN SU MAYOR PARTE (revisado el 2026-07-21): mejoras por módulos (Soluciones, Diagnóstico, Dispositivos, Red y Bóveda; 71 puntos en 5 documentos del usuario), analizadas contra el código real en [PROPUESTA_MODULOS.md](PROPUESTA_MODULOS.md). Unos 25 puntos ya estaban resueltos por las tareas 38 a 47. HECHAS y archivadas: S1 (tarea 48), D1 (tarea 49), Dis1+B1 (tarea 50), R1 (tarea 51), el GRUPO ESQUEMA (tarea 52) y D2 (tarea 53); la tarea 39 (vista 360° del dispositivo) quedó cubierta por N2/N3. De las 6 decisiones abiertas de la sección 8, CUATRO están resueltas: etiquetas reactivadas (SÍ, 2026-07-09, más la fase J4), "Notas" renombrado a "Bóveda" (2026-07-09), el nombre oficial sigue siendo "Soluciones IT" ("IT Brain" queda informal) y 3 estados de artículo sin "en revisión". QUEDAN ABIERTAS solo dos: nodos compartidos entre diagnósticos (se recomienda NO; confirmar que duplicar la pregunta es aceptable) y si se aprueba R2 (mapa interactivo de red, "muy alto" esfuerzo) o el árbol de R1 alcanza. **D4+F3 (problemas frecuentes en Inicio + tablero de estadísticas sobre `ejecuciones_diagnostico`) quedó HECHO el 2026-07-23, tarea 146** (ver el detalle en TAREAS_ARCHIVO.md). De las fases de la sección 7 solo queda pendiente S2 (adaptación del formulario por tipo + recomendaciones extra).

Propuesta presentada al usuario el 2026-07-08, aprobada para ejecutarse por fases: mejoras de UX de procedimientos (18 puntos del usuario) y Modo Diagnóstico Inteligente, analizadas contra el código real en [PROPUESTA_UX_DIAGNOSTICO.md](PROPUESTA_UX_DIAGNOSTICO.md). TODAS las fases principales están terminadas y archivadas: A (tarea 41), B (tarea 42), C1 (tarea 43, visor de imágenes con zoom), D (tarea 44, modo asistente), E (tarea 45, sinónimos y anti duplicados) y F (tarea 46, Modo Diagnóstico Inteligente; pendiente solo de que el usuario aplique el schema.sql actualizado en Supabase, ver la tarea en el archivo). Quedan como opcionales a decidir: C2 (anotación de imágenes: flechas, círculos, texto) y F3 (tablero de estadísticas sobre las ejecuciones de diagnóstico). La tarea 40 (vínculos por instrucción, fase 1: credencial de la bóveda) se hizo y quedó archivada el 2026-07-09; otros vínculos por instrucción (IP/URL, nota, subprocedimiento o solución) quedan pendientes de que el usuario los pida.

### 2. Backend en Supabase (pasos del usuario, guiados)
- Descripción: aplicar el esquema y dar de alta al equipo en el proyecto de Supabase.
- Prioridad: Alta
- Ubicación: `supabase/schema.sql`, `supabase/INSTRUCCIONES.md`
- Avance: esquema completo escrito (tablas, historial inmutable con `recibido_en`, triggers de updated_at, RLS con permiso especial para la bóveda, bucket de adjuntos y categorías iniciales). Credenciales configuradas en `.env` local.
- Avance: paso 1 HECHO. Verificado por REST (2026-07-03) que `articulos.procedimiento` ya existe (antes daba error 42703); el esquema actualizado quedó aplicado en Supabase.
- Avance: paso 2 EN PARTE. 1 de los 5 usuarios creado (2026-07-03); el usuario creará los otros 4 más adelante.
- Pendiente (usuario, guiado en sesión): (2) crear los 4 usuarios restantes con Auto Confirm; (3) autorizar la bóveda con `puede_ver_boveda`; (4) desactivar el registro público. Al terminar cada paso se verifica desde aquí lo que sea posible.

### 10. Pulido móvil y puesta en marcha (solo quedan pasos del usuario)
- Descripción: optimización de rendimiento (dividir el bundle, que hoy supera los 700 kB por supabase-js + react-markdown + minisearch), compresión de fotos al subirlas, botón "Descargar todo para offline", icono definitivo de la app, pruebas en los teléfonos reales del equipo y guía de instalación de la PWA.
- Prioridad: Media
- Ubicación: general
- Avance: división del bundle HECHA. Ya no hay un único archivo de 766 kB; se parte en 30+ trozos con carga diferida por ruta (`React.lazy` en `src/App.tsx`, límite de Suspense en `src/app/Layout.tsx`, fallback compartido en `src/components/Cargando.tsx`) y separación de vendors estables (`supabase`, `react-vendor`, `dexie`) vía `build.rollupOptions.output.advancedChunks` en `vite.config.ts`. El chunk de entrada bajó de ~110 kB a ~15 kB; react-markdown (155 kB) solo carga al abrir un artículo y minisearch solo en Inicio; la pantalla de login no arrastra ninguno. Verificado en build de producción real (`vite preview`, config nueva en `.claude/launch.json`): el service worker precachea los 25 trozos JS (cache-first, funcionan offline) y tiene NavigationRoute a index.html (recarga de URL profunda offline sirve el shell SPA). 42 pruebas, lint y build en verde.
- Avance: compresión de fotos HECHA. Lógica pura y testable en `src/lib/comprimirImagen.ts` (`debeComprimir`, `calcularDimensiones`) separada de la parte que usa APIs del navegador (`comprimirImagen`, con `createImageBitmap` + `canvas`). Integrada en `src/components/Adjuntos.tsx`: antes de subir, la foto se redimensiona a un lado máximo de 1600 px y se recodifica como JPEG (calidad 0.82); se omiten PDF, SVG (vectorial), GIF (podría ser animado) y archivos ya livianos (≤300 KB); si algo falla o el resultado no queda más liviano, se sube el archivo original sin tocar (nunca bloquea la subida). 10 pruebas nuevas (52 en total) para la lógica de decisión y de escalado. Verificado en el navegador real con una foto sintética de 4032×3024 (7.5 MB): quedó en 1600×1200 y 47.9 KB; y con los tres casos de exclusión (imagen pequeña, PDF, SVG), que devuelven el archivo original intacto. Build, lint y bundle-splitting sin cambios.
- Avance: botón "Descargar todo para offline" HECHO. `src/lib/adjuntosOffline.ts`: cache dedicado (Cache Storage, `adjuntos-offline-v1`) con clave sintética estable basada en la referencia de Storage (nunca la URL firmada, que expira a la hora); `estaDisponibleOffline`, `obtenerUrlOffline` (URL `blob:` local), `cachearSiHaceFalta` (guarda un adjunto la primera vez que se ve, cerrando el hueco de ARQUITECTURA.md sección 10 que prometía esto pero no estaba implementado) y `descargarTodoOffline` (recorre todos los adjuntos, descarga los que falten, sigue adelante si alguno falla) con estado observable (`ProgresoDescarga`) para la interfaz. Botón nuevo en `src/components/DescargarOffline.tsx`, integrado en `src/features/inicio/InicioPage.tsx`. `AdjuntoItem` en `src/components/Adjuntos.tsx` ahora muestra primero desde el cache offline si existe, sin tocar la red. 4 pruebas nuevas (56 en total) para la clave sintética y el estado inicial. Verificado en navegador real: ida y vuelta completa del cache (descargar, detectar disponible, servir como blob, no volver a descargar), y el flujo de error real contra Supabase (URL de un archivo inexistente devolvió 400, se contó como "fallido" sin romper el resto y la interfaz lo mostró reactivamente). Build, lint y 56 pruebas en verde.
- Avance: guía de instalación HECHA. [INSTALACION.md](INSTALACION.md) nuevo, en la raíz del proyecto: pasos de instalación para Android (Chrome), iPhone/iPad (Safari, con la advertencia de que solo funciona desde Safari) y PC (Chrome/Edge); nota sobre el borrado de datos de iOS tras semanas sin abrir la app (ya documentado en ARQUITECTURA.md, aquí en lenguaje para el usuario final); recordatorio de usar "Descargar todo para offline" antes de salir a un mantenimiento; cómo se actualiza la app; problemas comunes. Enlazada desde `README.md` y `CLAUDE.md`.
- Avance: ícono definitivo HECHO. `public/icon.svg` rediseñado: escudo en degradado azul (`#38bdf8` a `#0284c7`) con un checkmark "recortado" en el mismo tono del fondo oscuro (`#0f172a`), sin texto (se lee bien incluso muy chico), sobre el cuadrado redondeado de siempre. Todo el arte cabe dentro de la "zona segura" de los íconos maskable (círculo del 80% central del lienzo), algo que el placeholder anterior (letras "IT" a todo el ancho) no respetaba y que Android podía recortar mal al aplicar su máscara. Verificado por muestreo de píxeles sobre un canvas real (esquinas transparentes, escudo sin tocar los bordes, checkmark visible cerca del centro) porque la herramienta de captura de pantalla falló por un problema del entorno, no del código. Se agregó además `public/apple-touch-icon.png` (180×180, sin transparencia ni esquinas redondeadas propias, seguiendo la convención de Apple de que iOS aplica su propio recorte) generado con `sharp` instalado temporalmente (`npm install --no-save`, desinstalado después; no quedó como dependencia). `index.html` y `vite.config.ts` actualizados para usarlo. Sigue siendo una sola pieza de arte SVG (liviano, no afecta la fluidez ni el peso de la app: el PNG nuevo pesa ~3.8 KB). Build, lint, bundle-splitting y 56 pruebas sin cambios.
- Avance: despliegue en Vercel OPERATIVO EN PARTE. El usuario montó el proyecto en Vercel (https://soluciones-it-psi.vercel.app, desplegado desde GitHub). Se detectó y corrigió: (1) GitHub estaba 6 commits atrás del repo local, por eso producción tenía un build viejo; ya se subió todo. (2) Las rutas internas daban 404 al abrirlas directo; se agregó `vercel.json` con la reescritura SPA a `index.html`. (3) Verificado por HTTP: el esquema de Supabase SÍ está aplicado (la tabla `categorias` responde y RLS oculta filas a usuarios sin sesión). (4) El usuario agregó `VITE_SUPABASE_URL` y `VITE_SUPABASE_ANON_KEY` en Settings > Environment Variables, pestaña "Project" (confirmado en pantalla que quedaron guardadas), pero el botón "Redeploy" del panel de Vercel dio "Unexpected error (500)" de forma repetida y no llegó a crear un despliegue nuevo (verificado: la lista de Deployments no muestra un intento adicional, y el build en producción seguía sin la URL de Supabase). Se disparó un despliegue nuevo por la vía de Git (commit a `main`) como alternativa al botón del panel, que sí dispara el build automático de Vercel con las variables ya guardadas.
- Avance: despliegue por Git VERIFICADO. El build de producción ya incluye la URL de Supabase; probado en navegador real contra el dominio: redirige a /login, sin aviso de "no conectado", y un intento de login llegó hasta Supabase y devolvió el error esperado en español. Producción conectada de punta a punta.
- Bloqueada por (usuario): (1) confirmar que los 5 usuarios del equipo estén creados en Supabase Authentication; (2) pruebas en los teléfonos reales del equipo con la guía de INSTALACION.md, que ya incluye la dirección real.

### 39. Dependencias por equipo (información conectada)
- Descripción: al abrir un dispositivo puntual, ver conectado todo lo suyo: procedimientos, credenciales autorizadas, ubicación, dispositivos conectados, historial y problemas frecuentes de ese equipo (no solo de su categoría). Para red ya existe gran parte (conexiones + topología); lo nuevo es vincular un equipo concreto a procedimientos, credenciales y problemas frecuentes específicos. La más ambiciosa; conviene hacerla por fases y apoyada en las tareas 36 y 38. Cubre la idea #7 del análisis del 2026-07-07.
- Prioridad: Baja
- Avance (2026-07-10, fase 1, SIN esquema, elegida por el usuario): la ficha del dispositivo (`src/features/dispositivos/DispositivoPage.tsx`) se reorganizó en vista 360° por secciones (Información, Procedimientos y diagnóstico, más los bloques autotitulados de Conexiones, Adjuntos e Historial) y se agregó "Problemas frecuentes de este equipo" (`ProblemasDelEquipo.tsx` + función pura `problemasDeDispositivo` en `problemasDeDispositivo.ts`, con 5 pruebas): el inverso del vínculo `dispositivosAfectados` (tarea 38), que hoy solo se veía del artículo hacia el equipo. Filtra a incidencias publicadas (excluye borrador/obsoleto/eliminado). Verificado en navegador real (sesión simulada, 4 artículos sembrados: 2 incidencias publicadas del equipo aparecen y enlazan bien, el borrador y el de otro equipo se excluyen; datos limpiados). 354 pruebas, typecheck, lint y build en verde.
- Fase 2 ENTREGADA por otras tareas: la parte sin esquema (generalizar `dispositivosAfectados` a todo tipo de artículo + "Procedimientos de este equipo") se hizo en N2 (tarea 60); la parte con esquema (credenciales de la bóveda vinculadas por id a un equipo, con "Credenciales de este equipo" en la ficha protegido por permiso de bóveda; y el tipo 'relacionado' en `conexiones` para equipos no-red) se hizo en el grupo N3 (tarea 70, archivada 2026-07-17). Con esto la tarea 39 queda cubierta; se conserva aquí solo como referencia del origen.
